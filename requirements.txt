# Quantum Allosteric Scanner — backend dependencies
fastapi==0.111.0
uvicorn[standard]==0.30.1
numpy==1.26.4
scipy==1.13.1
biopython==1.83
requests==2.32.3
# OPTIONAL — the web app does NOT need these. They only give rcsb_extract.py a faster CIF
# parser; without them it falls back to BioPython. They also don't build cleanly on
# Python 3.9, so they're left out of the core install. To enable the faster parser on
# Python ≥3.10:  pip install biotite gemmi
networkx==3.2.1
scikit-learn==1.5.0
pydantic==2.7.4
