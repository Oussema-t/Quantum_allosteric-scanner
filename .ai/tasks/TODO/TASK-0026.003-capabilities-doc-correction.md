# TASK-0026.003 capabilities-doc-correction (subtask of TASK-0026)

## Context

- ID: TASK-0026.003
- Title: `CAPABILITIES.md` honesty pass against what TASK-0026.001/.002
  actually shipped + draft `.claude/settings.json` allowlist entry for
  `agents-tools/capability-runner.sh`
- Status: TODO (blocked on TASK-0026.001, TASK-0026.002)
- Owner: Toolsmith
- Claimed By: —
- Claimed At: —
- Parent: [TASK-0026](TASK-0026-capability-runner-recovery.md)

## Intent Contract

- Outcome: `.ai/reference/CAPABILITIES.md` stops describing
  `agents-tools/capability-runner.sh` as a uniformly existing provider when
  it wasn't, before this task landed, real at all. After this subtask,
  every one of the 16 rows either (a) correctly points at a real, tested
  implementation from .001/.002, or (b) is explicitly marked as not yet
  implemented with a one-line reason, so a future reader never has to
  re-discover the original gap the way this session did.
- In Scope:
  - re-check every row TASK-0026.001 and TASK-0026.002 touched; for each,
    confirm the `Current Provider` / `Scope` / `Safety` / `Notes` columns
    still match actual behavior (flags, `--json` support, safety class).
  - for any row deliberately left unimplemented (see each subtask's Open
    Questions, e.g. a possible `repo.test.playwright-local` environment
    gap), rewrite its row honestly — do not leave it silently implying an
    existing provider.
  - draft (not apply) a `.claude/settings.json` allowlist entry for
    `agents-tools/capability-runner.sh` invocations, scoped to read-only
    subcommands first (the `repo.vcs.*` and other read-only rows), leaving
    write-scoped subcommands (`plan-next-commit --apply`,
    `cleanup-plan --apply` deletes, etc.) out of the allowlist so those
    still prompt — matches the `update-config` skill's existing pattern of
    scoping allowlist entries narrowly rather than allowlisting an entire
    script wholesale.
  - present the draft to a human for approval before it's applied, per
    `.ai/reference/LOCAL_AUTOMATION_MANUAL_ENVIRONMENTS.md`'s
    approval-gated-helper rule.
- Out Of Scope:
  - implementing anything — this subtask is documentation + a draft
    allowlist change only, no new script code.
  - the `agents-tools/` vs `.ai/tools/` directory-reconciliation question —
    left as TASK-0026's own Open Question, not resolved here.
- Constraints And Invariants:
  - keep `CAPABILITIES.md`'s existing table shape (columns, one row per
    capability) — this is a correction pass, not a reformat.
  - the allowlist draft must not grant unattended access to any
    write/destructive subcommand — read-only rows only, explicitly.
- Planned Validation: for every row this subtask touches, re-run the
  corresponding subcommand once more and confirm the row's own wording
  matches observed behavior exactly (same check TASK-0026.001/.002 already
  did once — this is a second independent pass, not a rubber stamp).

## In Progress

None

## TODO

- [ ] Wait for TASK-0026.001 and TASK-0026.002 to reach Done (or a defined
      partial-stopping point) before starting.
- [ ] Walk all 16 rows against actual shipped behavior; correct wording
      where needed.
- [ ] Draft the read-only-only `.claude/settings.json` allowlist addition.
- [ ] Get human approval before the allowlist change is applied.
- [ ] Update the subtask table status in TASK-0026 to reflect completion;
      update TASK-0026's own `Done` section once this is the last subtask
      to finish.

## Dependency

- [TASK-0026.001](TASK-0026.001-vcs-core.md)
- [TASK-0026.002](TASK-0026.002-packaging-remainder.md)

## Open Questions

- None yet beyond what the parent and sibling subtasks already track.

## Done

(not yet)
