# TASK-0079.005 Run end-to-end for the 3 mandatory targets + manual inspection (subtask of TASK-0079)

## Context

- ID: TASK-0079.005
- Title: Execute `.004`'s command for real against KRAS_G12C, BCR_ABL1,
  and CARDIAC_MYOSIN (live network + real computation), and manually
  inspect the three deliverables per target
- Status: TODO
- Owner: Implementer
- Parent: [[TASK-0079]] (End-to-end challenge run)
- Source: TASK-0079's own Planned Validation and Acceptance Scenarios —
  this subtask is where those are actually exercised, not asserted in
  the abstract.

## Intent Contract

- Outcome: three real output directories exist
  (`__WORK_IN_PROGRESS__/results/{KRAS_G12C,BCR_ABL1,CARDIAC_MYOSIN}/`),
  each containing a genuinely-computed connectivity matrix, hit list, and
  methodological report, inspected by hand against this task's own
  Acceptance Scenarios — not just "the script exited 0."
- In Scope:
  - run `.004`'s command for each of the three targets, live (real RCSB
    fetches, real `frozen_context`-gated computation).
  - for each target, confirm: the connectivity matrix file loads and is
    `(N, N)`, symmetric, zero diagonal; the hit list has exactly 5
    entries (or fewer only if genuinely fewer candidates exist,
    documented why) with real resnums, not placeholder indices; the
    report renders with **no** `DEV/CEILING` banner (a real frozen stamp
    was issued) and states a real, non-`N/A` value for every headline
    key `.001`'s assembly is supposed to populate.
  - for each target, read `diagnostics.classify_failure`'s verdict
    (surfaced via `.003`'s `"_diagnosis"` field) and record it honestly
    in this file's Done section — including if a target comes back
    `NO_SIGNAL_IN_APO`/`BEATS_CHANCE_NOT_FLOOR`. A negative result on a
    real target is exactly the kind of finding `PLAN.md`'s "gates before
    build" framing wants surfaced, not hidden.
  - cross-check at least one target's connectivity-matrix/hit-list output
    against a known reference if one exists (e.g. KRAS_G12C's
    `pocket_full[4.5]` set from `config/targets.yaml`, already used as a
    pinning reference by [[TASK-0047]]) — do the top-5 hits land anywhere
    near the known allosteric pocket, or is the result a legitimate
    negative that should be reported as such?
- Out Of Scope: fixing anything found broken here beyond a trivial,
  obviously-safe patch — a real defect found during this run should be
  filed as its own follow-up task (mirroring TASK-0052→TASK-0087,
  TASK-0055→TASK-0088's pattern this session), not silently patched
  inline in a validation-pass subtask.
- Constraints And Invariants: this subtask requires live network access
  and real compute time — not mockable, matching TASK-0047's own
  precedent for network-gated real-target validation in this package.
- Planned Validation: this subtask *is* the validation step — its own
  Done section is the evidence.

## Dependency

- [[TASK-0079.004]] — the command this subtask runs.

## Open Questions

- None yet — surface only if a real run reveals something `.001`-`.004`
  didn't anticipate (expected, given this is the first real end-to-end
  exercise of the whole Phase 0/1 fix set together).

## Done

(not yet)
