# TASK-0296 — 6 backend tests fail on stale KRAS_G12C golden values

- Status: Done
- Priority: Medium — not a regression, but a red suite hides the next real one
- Filed: 2026-08-29 by Reviewer thread (promoting [[TASK-0290]]'s flagged-but-unfixed finding out of a DONE file)
- Related: [[TASK-0290]], [[TASK-0270]]

## What

`backend/test_analysis_characterization.py` has **6 failing tests**,
pinned to KRAS_G12C golden values that went stale when the 2026-08-26
genotype fix swapped the apo structure **4OBE → 4LDJ** ([[TASK-0270]])
and the fixtures were never regenerated.

[[TASK-0290]] confirmed these are **not** a regression from its own
changes — identical failures with that task's edits fully reverted via
`git stash` — and correctly left them alone as outside LANE 1's scope.

## Why it matters

A permanently-red suite is indistinguishable from a newly-red suite. The
next real regression in this file will look exactly like today's noise.
[[TASK-0290]] and [[TASK-0293]] both had to reason around it this window.

## Scope

- [x] Regenerated the 6 golden values against `4LDJ` by calling the real
      functions (`gnm_context`/`site_potentials`/`quantum_seed_readiness`/
      `seed_readiness_shift`/`connectivity_change`) live and reading their
      output — not derived from the old numbers.
- [x] Recorded provenance **in the test file**: a new docstring block
      states the structure (`4LDJ`), the regenerating task (this one),
      and the date, plus a per-assertion comment on the one test whose
      *verdict* (not just its numbers) moved.
- [x] Confirmed the suite is green (`pytest backend/`: 37/37 passed).
      Checked for other silent 4OBE dependents (not assumed clean):
      `backend/test_analysis.py`'s own KRAS_G12C pin also uses `"4OBE"`,
      but as a literal hardcoded ID (not via `SYSTEMS`) -- self-consistent,
      never broken by TASK-0270's config change, confirmed still passing,
      left alone (not the same staleness class).

## Constraint

Regenerate from the current code path — do **not** hand-edit the expected
numbers to match. If a regenerated value looks wrong, that is a finding,
not a fixture to overwrite.

**Honored**: `quantum_seed_readiness`'s verdict flipped PARTIAL->RISKY,
not just its underlying numbers -- reported as a finding (code comment on
the assertion), not silently pinned to whatever the code now produces
without comment.

## Done (2026-08-30, Implementer A)

**All 6 regenerated by direct live computation** against
`SYSTEMS["KRAS_G12C"]` (already `apo=4LDJ`), each printed and read before
being written into the test file -- no value was inferred from the old
one or hand-adjusted.

**Cross-check that strengthens confidence beyond "the suite is green"**:
`seed_readiness_shift`'s `holo`-side dict came back byte-identical to the
original 2026-07-12 capture. `holo=6OIM` never changed -- only `apo`
did -- so an unchanged holo half alongside a moved apo half is a real
internal consistency check on the regeneration, not just "numbers came
out different, plausible enough."

**A genuine finding, not just fixture drift**: `quantum_seed_readiness`'s
verdict itself moved (PARTIAL->RISKY), consistent with the direction
every other re-scored KRAS_G12C quantity in this register has moved since
[[TASK-0270]] (e.g. [[TASK-0283]]'s diagnosis flip). Recorded as a comment
on the assertion, per this task's own Constraint.

**Checked, not assumed, for other 4OBE dependents**: found
`backend/test_analysis.py`'s own separate KRAS_G12C pin, which also
targets `"4OBE"` -- but literally, not through `SYSTEMS`, making it
self-consistent and unaffected by TASK-0270's config change (confirmed
still passing). Left alone; flagged in `RESULTS.md` since its numbers now
coincidentally duplicate the *pre-fix* values in the file this task just
changed, which could otherwise read as connected.

**Suite**: `pytest backend/` -- 37/37 passed (was 31/37).

**Moved TODO/IN_PROGRESS -> DONE.**
