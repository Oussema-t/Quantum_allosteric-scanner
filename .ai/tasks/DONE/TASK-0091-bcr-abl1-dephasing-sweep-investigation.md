# TASK-0091 (RE-FILED 2026-07-13) Does `H_new`'s ground state localize on *distal* pockets?

## Context

- ID: TASK-0091
- Title: Test directly whether `H_new`'s ground-state localization (the
  quantity `propagators.heat`/its post-TASK-0095 rename actually computes)
  detects distal allosteric pockets — not just proximal ones — on
  BCR_ABL1's real 0.731 result, **with TASK-0094's proximity floor
  applied**.
- Status: Done
- Owner: Implementer
- Source: **Re-filed 2026-07-13** per
  `__WORK_IN_PROGRESS__/REVIEW-2026-07-13-proximity-confound-and-propagator-semantics.md`,
  finding **P1-B**, which retracts this task's original framing. Original
  framing (a calibrated Haken-Strobl dephasing sweep testing whether
  decoherence "recovers" the 0.731 heat-kernel score) **rested on a false
  premise**: `heat(H_new)` is dominated by `H_new`'s *diagonal* terms
  (it is the operator's ground-state density, not a diffusion process —
  see TASK-0095), while Haken-Strobl dephasing is generated by the
  *off-diagonals*. The dephasing sweep is structurally incapable of
  converging toward `heat(H_new)`'s value — it cannot "recover" 0.731,
  and if AUC happened to rise under such a sweep, attributing it to
  ENAQT/decoherence would have been an unsupported causal claim. **Do not
  run the original framing as filed below this line in history.**

### The real, still-open question

BCR_ABL1's real run produced `AUC_ctqw_mean = 0.525` (chance) but
`AUC_heat_mean = 0.731` (well above chance) on the **same** `H_new`
operator (`apo`, `enm_cutoff=8.0`). The review's own non-findings section
already ran the obvious first check — is this just proximity in a
different disguise? — and found: **not conclusively**. On the review's
synthetic 170-residue globule, `heat` occupation correlates with
−Euclidean distance from seed at Spearman **+0.589** (weaker than CTQW's
0.853, but *not immune*), and a distal-pocket AUC for `heat` is **0.048**
— just as anti-correlated as CTQW's 0.035. So ground-state localization
is *not* proven to escape the proximity confound on the synthetic case —
but BCR_ABL1's real 0.731 is a real, unexplained, and potentially
genuine result on an *actually distal* pocket (myristoyl site, ~25 Å from
the active site) that the synthetic case does not settle either way.
**This is the one open line of inquiry from the review that is not
proximity in disguise, per the review's own sequencing note** — but it
must be tested, not assumed.

## Intent Contract

- Outcome: an explicit answer to whether BCR_ABL1's real 0.731 ground-state-
  localization score clears TASK-0094's proximity floor (i.e., beats
  `euclid_from_seed_centroid`/`hop_from_seed` on the same real target,
  not just chance) — and, if it does, whether the localization is
  genuinely concentrated on/near the distal myristoyl pocket rather than
  merely closer to the seed than random chance would predict.
- In Scope:
  - Requires **TASK-0094 landed first** (the proximity floor must exist
    to test against).
  - Requires **TASK-0095 landed first** (or run concurrently with full
    awareness of its finding) — use the renamed function
    (`ground_state_relaxation` or whatever TASK-0095 lands as) and
    respect its indefinite-operator guard; do not reintroduce the
    "classical diffusion" framing anywhere in this task's own write-up.
  - Rebuild BCR_ABL1's real winning `H_new` operator
    (`hamiltonians.build_H_new(apo.coords, apo.bfactors, cutoff=8.0)`)
    against a freshly re-fetched 1OPL, matching TASK-0079.005's original
    run exactly (not a cached/guessed operator).
  - Compute `euclid_from_seed_centroid`/`hop_from_seed` (TASK-0094) on
    this same target and compare against the 0.731 ground-state score.
  - If the score clears the proximity floor, additionally check where
    the top-scoring residues actually sit relative to the myristoyl
    pocket (real distance/hop count) — a floor-clearing AUC that still
    turns out to be "close but not distal enough to matter" should be
    reported as such, not oversold.
- Out Of Scope: the original dephasing-sweep mechanics (retracted, do not
  run); implementing any adaptation based on the answer (a follow-up task
  if the answer is "yes, real distal signal") — this task answers the
  diagnostic question only.
- Constraints And Invariants: real-network, real-compute investigation
  (matches TASK-0079.005's own "not mockable" precedent) — report actual
  numbers, not synthetic stand-ins; every framing must use TASK-0095's
  corrected propagator semantics (no "classical diffusion" language).
- Planned Validation: a comparison table (ground-state score vs. both
  proximity baselines, on real BCR_ABL1 data) plus an explicit real/not-
  real-distal-signal conclusion in this task's Done section, per this
  session's "state a conclusion, don't leave it implicit" convention.

## Dependency

- **Hard blocked on TASK-0094** (proximity floor must exist).
- **Hard blocked on TASK-0095** (must use corrected propagator naming/
  semantics, not the retracted "classical diffusion" framing).
- [[TASK-0079.005]] — the real run and the original finding.
- Superseded original scope: a calibrated Haken-Strobl dephasing sweep —
  **retracted**, do not execute; kept below only as a historical record
  of what was originally filed, not as instructions.

## Open Questions

- If BCR_ABL1's 0.731 does clear the proximity floor: is there a
  mechanistic reason (specific to `H_new`'s ground state vs. the
  time-averaged CTQW) that would explain *why* ground-state localization
  might succeed where the walk average doesn't? Worth a follow-up
  investigation task if this task's answer is affirmative — don't
  speculate inline here.

---

## Historical record — original framing (retracted 2026-07-13, do not execute)

The following was this task's original content, filed from
TASK-0079.005's findings before REVIEW-2026-07-13 identified the false
premise (P1-B). Kept verbatim for audit trail only.

> Run `analysis.dephasing_sweep` (calibrated, not blind) on BCR_ABL1's
> real winning `H_new` operator to determine whether CTQW's near-chance
> performance is a coherence problem (fixable by calibrated dephasing) or
> a propagator-independent ceiling. Reuse
> `test_kras_g12c_dephasing_flat_survives_kappa_calibration`'s exact
> calibration recipe (`calibrate_kappa` → `anm_modes` → `mode_energetics`'s
> `relaxation_time` → `gamma_scale = 1 / mean(relaxation_time[:20])`,
> sweep `{0.5, 1, 2} * gamma_scale`), expecting AUC to rise from ~0.525
> toward ~0.731 if decoherence "recovers" the heat-kernel's signal.
> **This premise is false**: `propagators.haken_strobl`'s γ→∞ limit
> cannot converge to `heat(H_new)`'s value (see TASK-0095) — the sweep as
> designed could never have answered the question it was built to answer.

## Done

**Real run, 2026-07-13** — real network fetch (1OPL apo / 5MO4 holo), real
`run_frozen_verdict` (paying its full `select_frozen_config`/
`unsupervised_score` selection cost, ~20-25 CPU-minutes across two runs —
not cached/guessed), reusing `scripts/run_challenge.py`'s own
`_load_apo_holo`/`_make_candidates_builder` directly rather than
re-deriving the recipe by hand. Session-local script (not committed),
cached intermediate results (`winner_H`/`occ_heat`/floor arrays) to
`/tmp/.../task_0091_cache.npz` for any follow-up without repaying the
selection cost.

**Reproduction check (exact):** `AUC_ctqw_mean=0.5250`,
`AUC_heat_mean=0.7315` (matches `RESULTS.md`'s recorded 0.731),
`_diagnosis=NO_SIGNAL_IN_APO`, winner=`H_new_default` — confirms this run
reconstructs TASK-0079.005's original result, not a different one.

**Question 1 answered: does 0.731 clear the proximity floor? YES, decisively.**

| Floor candidate | AUC | Ground-state (0.7315) clears it by |
|---|---|---|
| `degree_centrality` | 0.4933 | +0.2381 |
| `euclid_from_seed_centroid` | 0.5412 | +0.1902 |
| `hop_from_seed` (strongest) | 0.5652 | +0.1663 |

This is the widest floor-clearing margin measured for any mandatory
target's headline number so far — contrast with KRAS_G12C's own apo
`AUC_ctqw_mean` (0.779), which **failed** to clear its own floor
(max floor 0.798, TASK-0094's Done section). `ground_state_relaxation`'s
0.731 is real, floor-beating signal on a genuinely distal (~32 Å
seed-to-pocket) target — not proximity in disguise, on this evidence.

**Question 2 answered: is the localization actually on the pocket? YES in
aggregate, NO at hit-list sharpness — diffuse, not sharp, and this matters.**

- Top-5/top-10/top-20/top-30 ranked residues by `ground_state_relaxation`
  occupation: **0 of 16 pocket residues in any of them.** The exact
  residues a "top-5 hit list" deliverable would report (390, 389, 388,
  342, 341) sit 12.6-17.1 A from the pocket centroid — closer than the
  seed (32.3 A) but not on the pocket.
- The AUC is nonetheless real and not an artifact: pocket residues' ranks
  span the 8.6th-54.8th percentile of the full 451-residue ranking,
  median 20.1st percentile (vs. 50th expected by chance) -- genuine,
  broad-based enrichment. Precision@50 = 6%, precision@75 = 31% (vs. ~3.5%
  base rate at either cutoff under a null ranking) -- enrichment becomes
  visible only once the net is cast wide.
- Nearest-neighbor check: the top-20-occupation cluster (centered near
  resnums 341-343/365-366/388-390) sits close (5.2-11.3 A) to *one side*
  of the 16-residue pocket (351-363, 448-456) but far (14.6-18.9 A) from
  the other side (481-487) -- consistent with the ground state
  highlighting an approach/entrance region adjacent to part of the
  myristoyl site, not the full binding pocket.

**Conclusion (explicit, per this task's own Planned Validation):**
BCR_ABL1's ground-state localization is a **real, floor-clearing,
genuinely distal signal** -- the strongest, most decisive floor-clearance
of any mandatory-target number measured this session, and not explained
by any of the three proximity baselines. But it is a **diffuse regional
enrichment, not a sharp discovery** -- a hit-list built from
`ground_state_relaxation`'s top-5/10/20/30 would completely miss the
labeled pocket despite the good AUC. Report both halves together, always:
"floor-clearing distal signal" without the hit-list caveat would be
oversold; "0/5 top hits" without the AUC/rank context would bury a real
finding. Neither number alone is the honest headline.

**Practical consequence, explicitly out of this task's own scope (per
Intent Contract) but flagged for whoever picks it up:** switching the
challenge's reported "top-5 ranked hit list" from CTQW's occupation to
`ground_state_relaxation`'s would **not** improve BCR_ABL1's hit list
(still 0/5) even though it would improve the reported AUC -- these are
different deliverables responding differently to the same operator. Any
future task proposing to swap the ranking metric per-target based on AUC
alone must check hit-list precision separately, not assume AUC gains
transfer.

**Answers the Open Question above:** a mechanistic "why" (ground-state
vs. time-averaged CTQW) is not established by this task -- flagging, not
speculating, per this task's own instruction not to speculate inline.
