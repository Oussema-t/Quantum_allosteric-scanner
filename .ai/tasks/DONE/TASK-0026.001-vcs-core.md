# TASK-0026.001 vcs-core (subtask of TASK-0026)

## Context

- ID: TASK-0026.001
- Title: `agents-tools/capability-runner.sh` dispatcher entry point +
  `repo.vcs.*` read-only primitives + `repo.packaging.snapshot` composite
- Status: Done
- Owner: Toolsmith
- Claimed By: —
- Claimed At: —
- Parent: [TASK-0026](TASK-0026-capability-runner-recovery.md) — read its
  locked Decisions before starting (re-derive from spec, no source port;
  `agents-tools/` is a real tracked directory).
- Scope: the foundational slice — everything else in TASK-0026.002 composes
  on top of these primitives, so this subtask has no dependency on the
  others and can start immediately.

## Intent Contract

- Outcome: `agents-tools/capability-runner.sh <capability-name> [args]
  [--json]` exists, is executable, and correctly backs these
  `.ai/reference/CAPABILITIES.md` rows:
  - `repo.vcs.current-branch` — read current branch name
  - `repo.vcs.short-status` — read short working-tree status
  - `repo.vcs.staged-diff-stat` — summarize staged diff size
  - `repo.vcs.staged-files` — list staged file paths (deterministic, sorted)
  - `repo.vcs.changed-files` — list complete changed-file set (`--scope
    all|staged`; deterministic, sorted)
  - `repo.packaging.snapshot` — composite over the above (fields include at
    least `branch`, `staged.diffStat`; supports `--include-working-tree`)
- In Scope:
  - the dispatcher itself: argument parsing, capability-name routing,
    shared `--json` output plumbing, non-zero exit on unknown capability
    name or git-command failure.
  - the five `repo.vcs.*` subcommands and the `repo.packaging.snapshot`
    composite, exactly as CAPABILITIES.md currently describes them.
- Out Of Scope:
  - every other `repo.packaging.*` row, `repo.maintenance.*`,
    `repo.test.playwright-local` — TASK-0026.002.
  - correcting CAPABILITIES.md wording — TASK-0026.003, after both build
    subtasks land (that task needs to see what actually shipped here vs.
    was deferred).
- Constraints And Invariants:
  - Python stdlib or POSIX shell only, no new runtime dependency.
  - `repo.vcs.staged-files` and `repo.vcs.changed-files` output must be
    sorted and stable across repeated calls against unchanged state —
    CAPABILITIES.md already promises determinism for these two.
  - `--json` must be supported on every subcommand in this slice; plain
    output should stay human-readable (not raw JSON dumped by default).
  - must not shell out to anything beyond `git` plus stdlib
    file/subprocess handling.
- Planned Validation: run each subcommand against this repo's actual
  current working tree and confirm output matches direct `git status`,
  `git diff --staged --stat`, `git diff --staged --name-only`, and `git
  diff --name-only` (plus staged) respectively — the runner must not
  disagree with git itself. Re-run twice back to back with no intervening
  change and confirm identical output (determinism check).

## In Progress

None

## TODO

- [x] Design the dispatcher's argument/exit-code contract (small enough to
      write inline in the script's own header comment, not a separate doc).
- [x] Implement dispatcher + `repo.vcs.current-branch`. — surfaced as the
      `branch` field of `repo.packaging.snapshot`, not a standalone
      subcommand, matching CAPABILITIES.md's existing Provider column
      exactly (see Open Questions for why this wasn't treated as a gap).
- [x] Implement `repo.vcs.short-status`. — surfaced as
      `workingTree.shortStatus` in the snapshot's `--include-working-tree`
      output, same reasoning.
- [x] Implement `repo.vcs.staged-diff-stat`. — surfaced as
      `staged.diffStat` in the snapshot output (structured: filesChanged/
      insertions/deletions parsed from `git diff --staged --numstat`, plus
      the raw `--stat` text for human-readable display).
- [x] Implement `repo.vcs.staged-files` (sorted, deterministic).
- [x] Implement `repo.vcs.changed-files` (`--scope all|staged`, sorted,
      deterministic).
- [x] Implement `repo.packaging.snapshot` composing the above.
- [x] Run Planned Validation against this repo's real working tree.
- [x] Update the subtask table status in TASK-0026 to reflect completion.

## Dependency

- None (foundational slice) — TASK-0026.002 depends on this landing first
  where it reuses these primitives.

## Open Questions

- None yet beyond what TASK-0026's parent file already tracks. **Resolved
  in this pass:** whether `current-branch`/`short-status`/`staged-diff-stat`
  need standalone top-level capability names — no, CAPABILITIES.md's own
  Provider column already documents them as snapshot-composite fields, not
  separate subcommands, and this implementation matches that as written
  rather than inventing new standalone entries pre-emptively.

## Done

- Built `agents-tools/capability-runner.sh` (Python stdlib, executable
  shebang — same bare-executable convention as `.ai/tools/claim.py`; kept
  the `.sh` name since 16+ `CAPABILITIES.md` rows already reference this
  exact path, renaming is TASK-0026.003's job).
- Three capability subcommands: `repo.vcs.staged-files`,
  `repo.vcs.changed-files --scope all|staged`, and the
  `repo.packaging.snapshot [--include-working-tree]` composite (fields:
  `branch`, `staged.files`, `staged.diffStat`, and `workingTree.shortStatus`
  when `--include-working-tree` is passed). All three support `--json`;
  plain output is human-readable, not a raw JSON dump.
- `changed-files --scope all` is the union of staged + unstaged-modified +
  untracked paths (`git diff --staged --name-only` ∪ `git diff
  --name-only` ∪ `git ls-files --others --exclude-standard`), sorted and
  deduplicated via `set()`.
- Exit-code contract: 0 success, 2 for unknown capability/malformed flags
  (argparse's own usage-error exit, unchanged from every other tool in
  this repo) or an underlying git command failure.
- Planned Validation, all cases exercised against this repo's real working
  tree (not scratch files — genuinely live, since another thread was
  actively staging/committing through part of this validation, which
  incidentally became a real-world exercise of `GIT-COMMIT`/`commit-guard`
  working correctly under real concurrent load): `staged-files` matched
  `git diff --staged --name-only` exactly, both on a non-empty index and
  after it emptied out from the other thread's commit; `changed-files
  --scope all` matched the live union of staged/unstaged/untracked at a
  verified instant; `repo.packaging.snapshot --include-working-tree`'s
  `workingTree.shortStatus` matched `git status --short` exactly, and
  `branch` matched `git branch --show-current`. Determinism: two
  back-to-back `changed-files --scope all --json` calls on a settled index
  produced byte-identical output.
- Updated TASK-0026's subtask table (this task's row only) and fixed its
  now-stale relative link (`TASK-0026.001-vcs-core.md` → the file's new
  `../DONE/` location).
