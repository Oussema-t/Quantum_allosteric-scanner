# 2026-08-23 — collaborator Hamiltonians (CTQW contribution debate)

`hamiltonians.py` here is a Colab-notebook export **provided by our collaborator**,
part of a debate involving the Reviewer thread, an external agent, and the
collaborator over whether a different CTQW Hamiltonian formulation invalidates
this register's own finding that CTQW does not deliver meaningful results across
different proteins.

It is **gitignored, not committed** — it is discussion material from an external
party, not this project's own code or a decided-on artifact.

Outcome so far: [[TASK-0237]] found the file's `H14_anm_pinv` cell fidelity-matches
this repo's own `hamiltonians.py`/`potentials.py` implementation, and (narrowed
scope, per direct instruction) that [[TASK-0138]] already answers the H14-specific
question — mixed, non-significant ceiling-vs-floor margins on all three mandatory
targets. See `.ai/tasks/DONE/TASK-0237-collaborator-hamiltonians-cell-validation.md`
for the full record.
