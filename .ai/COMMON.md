# .ai Common Hub

Last Updated: 2026-07-04
Status: Active

## Purpose

Central coordination hub for the repo-local agent scaffold.

## Quick Navigation

- scaffold overview: `.ai/README.md`
- architecture overview: `.ai/ARCHITECTURE_OVERVIEW.md`
- active bootstrap task: `.ai/tasks/IN_PROGRESS/TASK-0001-agent-scaffold-bootstrap.md`
- first agent bootstrap guide: `.ai/reference/FIRST_AGENT_BOOTSTRAP.md`
- second expert handoff walkthrough: `.ai/reference/SECOND_EXPERT_THREAD_WALKTHROUGH.md`
- local automation guidance: `.ai/reference/LOCAL_AUTOMATION_MANUAL_ENVIRONMENTS.md`
- capability catalog: `.ai/reference/CAPABILITIES.md`
- traceability model: `.ai/reference/TRACEABILITY_MODEL.md`
- resolution vocabulary: `.ai/reference/RESOLUTION_VOCABULARY.md`
- adoption guide: `.ai/reference/ADOPTION_GUIDE.md`
- minimum copy bundle: `.ai/reference/MINIMUM_COPY_BUNDLE.md`
- issue backend contract: `.ai/reference/ISSUE_BACKEND_PLACEHOLDER_CONTRACT.md`
- memory policy: `.ai/memory/README.md`
- addressed questions (per-role, lifecycle-foldered): `.ai/memory/questions/`
- backend selection: `.ai/reference/BACKEND_SELECTION.md`
- claim/lock tool (TASK-0024, TASK-0027, TASK-0028, TASK-0029, TASK-0107): `.ai/tools/claim.py` — `claim`/`release`/`status`/`sync`/`move`/`resolve`/`commit-guard`/`stage`, see "Current Rules" below
- command hygiene (TASK-0025): `.github/instructions/tooling/command-hygiene.instructions.md` — one command per call, no chains/pipes; `.claude/skills/command-hygiene/` is the applied procedure
- roadmap / phase-gated plan: `.ai/tasks/PLANS/PLAN.md`
- weekly timeline overlay: `.ai/tasks/PLANS/PLAN-01.07.26.md`
- seam protocol (TASK-0050): `.ai/reference/SEAM_PROTOCOL.md`, registry at `.ai/seams/`
- invariance protocol (TASK-0051): `.ai/reference/INVARIANCE_PROTOCOL.md`, registry at `.ai/invariants/`
- merge conflict protocol (TASK-0202): `.ai/reference/MERGE_CONFLICT_PROTOCOL.md`
- paper citation protocol: `.ai/reference/PAPER_CITATION_PROTOCOL.md` — check before citing any external source, or using a claim from one in scored work
- scientific references index: `__WORK_IN_PROGRESS__/documentation/REFERENCES.md` — challenge bibliography [1]-[25] + method/tool papers, status-tagged; local copies (if any) go gitignored under `__WORK_IN_PROGRESS__/documentation/references/`

## Source Of Truth

| Scope | Authoritative Location | Notes |
|------|------------------------|-------|
| Stable repo-local runtime policy | `.github/` | Keep durable and short |
| Mutable scaffold coordination | `.ai/` | Active status, backlog, open questions |
| Reviewed scaffold learnings | `.ai/memory/shared/*` | Team truth for the scaffold |
| Capability contracts | `.ai/reference/CAPABILITIES.md` | Behavior-first catalog |
| Workflow backend selection | `.ai/reference/BACKEND_SELECTION.md` | Current task, test, and delegation backend defaults |
| New work touching `__WORK_IN_PROGRESS__` module code | `.ai/tasks/TASK-XXXX` | See "Task ledger boundary" below — `.claude/TASKS.md` is closed to new entries |
| Cross-unit invariants (boundaries between tasks/modules) | `.ai/seams/` | TASK-0050. Every seam needs a real task owner — see `.ai/seams/README.md` |
| GAUGE/KNOB/SIGNAL classification per reported quantity | `.ai/invariants/` | TASK-0051. No transformation table → not reportable — see `.ai/invariants/README.md` |

## Task Ledger Boundary

`.claude/TASKS.md` and `.ai/tasks/TODO\|IN_PROGRESS\|DONE/` are two systems
covering overlapping ground (both can describe work on
`__WORK_IN_PROGRESS__/src/allostery/`). Decision (TASK-0002, 2026-07-04):
they do **not** get merged.

- `.claude/TASKS.md` stays as the closed historical ledger for its own
  BLUE/RED/ORCH review-remediation cycle (T-001…T-021, sourced from
  `.claude/criticism/*`). Do not delete or renumber it.
- **No new `T-NNN` entries get added.** Any new work item — including work
  on the same `__WORK_IN_PROGRESS__` codebase — is filed as a
  `.ai/tasks/TASK-XXXX` file going forward (TASK-0003 already establishes
  this pattern for module-content work).
- If `.claude/TASKS.md`'s remaining `TODO`/`Blocked` rows (T-017, T-018,
  T-021) are picked up again, wrap them in a `TASK-XXXX` file that links
  back to the `T-NNN` row rather than resuming the old ledger in place.

## Active Work Registry

**Note (2026-07-04, this thread):** this table was found reverted twice in
one session — first to a pre-TASK-0017 snapshot (missing the claim
columns and every row from TASK-0003 through TASK-0019), then, mid-repair,
to a partial state missing most of TASK-0003–0019 again — even though all
of those task files exist on disk the whole time (`ls .ai/tasks/TODO
IN_PROGRESS DONE` re-checked immediately before this write). Reconstructed
directly from that file listing rather than from another prose read, to
stop chasing a moving target. TASK-0024 (new, below) builds an atomic
claim/free tool so this stops happening. **If you were mid-edit on this
table when it was overwritten, your intended change is not lost** — the
task file itself is the source of truth; re-apply your registry row from
it once you read this note, and treat concurrent whole-file writes to
this table as unsafe until TASK-0024 lands.

Before starting or resuming a row, read `Claimed By` / `Claimed At` first —
see the claim-before-start rule under "Current Rules" below.

| Task ID | Description | Assigned To | Status | Priority | Last Active | Claimed By | Claimed At | Path |
|---------|-------------|-------------|--------|----------|-------------|------------|------------|------|
| TASK-0001 | Bootstrap repo-local agent scaffold under `.github` + `.ai` | Architect/Planner | In Progress | P0 | 2026-06-25 | — | — | `.ai/tasks/IN_PROGRESS/TASK-0001-agent-scaffold-bootstrap.md` |
| TASK-0002 | AI-scaffold hygiene & engineering review (this reconciliation pass) | General Critic | Done | P0 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0002-ai-scaffold-hygiene-review.md` |
| TASK-0003 | Reconcile and author `__WORK_IN_PROGRESS__/config/targets.yaml` | Implementer | Done | P1 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0003-targets-yaml-reconciliation.md` |
| TASK-0004 | Implement `__WORK_IN_PROGRESS__/src/allostery/labels.py` | Implementer | Done | P1 | 2026-07-06 | — | — | `.ai/tasks/DONE/TASK-0004-labels-py.md` |
| TASK-0005 | Implement `__WORK_IN_PROGRESS__/src/allostery/superpose.py` | Implementer | Done | P1 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0005-superpose-py.md` |
| TASK-0006 | Implement `__WORK_IN_PROGRESS__/src/allostery/protocol.py` | Implementer | Done | P1 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0006-protocol-py.md` |
| TASK-0007 | Implement `__WORK_IN_PROGRESS__/src/allostery/select.py` | Implementer | Done | P1 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0007-select-py.md` |
| TASK-0008 | Implement `__WORK_IN_PROGRESS__/src/allostery/analysis.py` | Implementer | Done | P1 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0008-analysis-py.md` |
| TASK-0009 | Implement `__WORK_IN_PROGRESS__/src/allostery/diagnostics.py` | Implementer | Done | P1 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0009-diagnostics-py.md` |
| TASK-0010 | Implement `__WORK_IN_PROGRESS__/src/allostery/report.py` | Implementer | Done | P1 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0010-report-py.md` |
| TASK-0011 | Implement `__WORK_IN_PROGRESS__/src/allostery/baselines.py` | Implementer | Done | P1 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0011-baselines-py.md` |
| TASK-0012 | Implement `__WORK_IN_PROGRESS__/src/allostery/pathways.py` | Implementer | Done | P1 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0012-pathways-py.md` |
| TASK-0013 | Implement `__WORK_IN_PROGRESS__/src/allostery/coarse.py` | Implementer | Done | P2 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0013-coarse-py.md` |
| TASK-0014 | Implement `__WORK_IN_PROGRESS__/src/allostery/viz.py` | Implementer | Done | P3 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0014-viz-py.md` |
| TASK-0015 | Build the holo-direction module (`HOLO_DIRECTION_MODULE.md`) Steps 0-2 (frozen protocol, deformation family, go/no-go gate) -- ran Step 2's own mandatory "First action" on all 7 pocket-scoreable verified targets. **7/7 `NO_GO`**: no admissible apo-only perturbation (60/target, 10 lowest ANM modes x 2 signs x 3 amplitude scales) ever creates the missing active-site<->pocket edge; no target's CO(10) clears 0.5. Steps 3-5 not built for any target, per the spec's own "build the rest only for targets that clear it." 2 real bugs found+fixed (chain_map, CASPASE7 CO-undefined case) | Implementer | Done | P2 (blocked on 0005/0006/0011 -- all now Done, unblocked) | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0015-holo-direction-module.md` |
| TASK-0016 | Add test for `heat()` behavior on indefinite `H_new` output (orphaned gap, see IMP-H4) | Implementer | Done | P2 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0016-heat-indefinite-hnew-test.md` |
| TASK-0017 | Soft-lock claim column on this registry (fixes the concurrent-edit near-miss between this session and a parallel thread on TASK-0002/COMMON.md/TASK-0007) | Implementer | Done | P0 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0017-registry-claim-lock.md` |
| TASK-0018 | Reconcile `backend/` (QAS) vs `__WORK_IN_PROGRESS__/src/allostery` (CCC) architecture — granularity/design-pattern gap confirmed, wraps closed-ledger T-018/T-021 | Architect/Planner | Done | P1 | 2026-07-05 | — | — | `.ai/tasks/DONE/TASK-0018-backend-vs-allostery-architecture-reconciliation.md` |
| TASK-0019 | Package accumulated `.ai`/`.claude` scaffold changes on `bartosz` into ordered, reviewable commits (Commit Packager overlay) | Commit Packager | Done | P1 | 2026-07-05 | — | — | `.ai/tasks/DONE/TASK-0019-scaffold-commit-packaging.md` |
| TASK-0020 | Product (backend/frontend) intent & feature inventory audit — endpoint/UI-control census mapped to rubric/roadmap | Architect/Planner | Done | P0 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0020-product-intent-inventory.md` |
| TASK-0021 | Backend API test-coverage baseline — codify `SOFTWARE.md` §9 examples as Playwright API tests | Implementer | TODO | P0 | 2026-07-04 | — | — | `.ai/tasks/TODO/TASK-0021-backend-api-test-baseline.md` |
| TASK-0022 | Frontend/UI tiered test coverage — presence / isolated functionality / intent chains / E2E process | Implementer | TODO | P0 | 2026-07-04 | — | — | `.ai/tasks/TODO/TASK-0022-frontend-ui-tiered-test-coverage.md` |
| TASK-0023 | YAGNI / scope-creep review of Product feature backlog vs. challenge rubric | General Critic | In Progress | P1 | 2026-07-04 | — | — | `.ai/tasks/IN_PROGRESS/TASK-0023-product-yagni-scope-review.md` |
| TASK-0024 | Whitelisted claim/free tool for scaffold coordination files — hardens TASK-0017 after two real collisions this session | Toolsmith | Done | P0 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0024-claim-lock-tool.md` |
| TASK-0024.001 | Extend `claim.py` to lock whole-file resources (e.g. `COMMON.md` itself), not just `TASK-XXXX` rows — filed after a risky git-checkout/restore maneuver on this file this session. **Done.** Core `RESOURCE-*` mechanism had already landed via TASK-0195; this pass finished the checklist (README section, `sync`-ignores-it test, Planned Validation) and fixed a real footgun in the landed code: the id-vs-resource split was a digit-*anywhere* search that silently mapped `PHASE1_SUBMISSION_V3.md` → `TASK-0001` — now an anchored whole-string task-id shape, every documented form unchanged. 4 new tests (160 total). Third `COMMON.md` incident (a TASK-0069 `sync`-recovery `git checkout` this session) folded in | Toolsmith | Done | P1 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0024.001-whole-file-resource-locks.md` |
| TASK-0024.002 | Fix `commit-guard`/`stage`'s shared `_staged_paths()`: git's content-similarity rename detection makes a moved+edited file's `--expect` shape unpredictable (needs old+new path sometimes, only new path other times) — add `--no-renames` for deterministic always-split behavior | Toolsmith | Done | P1 | 2026-07-09 | — | — | `.ai/tasks/DONE/TASK-0024.002-rename-detection-false-mismatch.md` |
| TASK-0025 | Single-command preference + reusable-script convention for all threads (widened from `.ai/`-only), delivered as a `.github/instructions/` policy doc + Claude Skill | Skills Crafter | Done | P2 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0025-command-hygiene-skill.md` |
| TASK-0026 | Recover `agents-tools/capability-runner.sh` (parent/coordinator — see subtasks below) | Toolsmith | TODO | P1 | 2026-07-04 | — | — | `.ai/tasks/TODO/TASK-0026-capability-runner-recovery.md` |
| TASK-0026.001 | Dispatcher entry point + `repo.vcs.*` primitives + `repo.packaging.snapshot` | Toolsmith | Done | P1 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0026.001-vcs-core.md` |
| TASK-0026.002 | Remaining `repo.packaging.*` + `repo.maintenance.behavior-contract-capture` + `repo.test.playwright-local` (blocked on .001) | Toolsmith | TODO | P1 | 2026-07-04 | — | — | `.ai/tasks/TODO/TASK-0026.002-packaging-remainder.md` |
| TASK-0026.003 | `CAPABILITIES.md` honesty pass + draft allowlist entry (blocked on .001, .002) | Toolsmith | TODO | P2 | 2026-07-04 | — | — | `.ai/tasks/TODO/TASK-0026.003-capabilities-doc-correction.md` |
| TASK-0026.004 | Named `repo.test.playwright-local` presets tied to TASK-0021/0022 + sibling `repo.test.pytest-local` — whitelisted self-verification for any thread | Toolsmith | In Progress | P1 | 2026-07-06 | — | — | `.ai/tasks/IN_PROGRESS/TASK-0026.004-test-execution-self-verification.md` |
| TASK-0026.005 | `pytest_local.py` uses `sys.executable` unconditionally — fails in any environment where the wrapper's own interpreter lacks the test deps (hit directly: fresh checkout, no ambient `.venv`); resolve `.venv/bin/python3` first, fall back unchanged | Toolsmith | Done | P2 | 2026-07-09 | — | — | `.ai/tasks/DONE/TASK-0026.005-pytest-local-venv-resolution.md` |
| TASK-0027 | Whitelisted `claim.py move` subcommand for TODO/IN_PROGRESS/DONE task-file transitions — folder move + Status field + registry row as one claim-checked command | Toolsmith | Done | P1 | 2026-07-04 | — | — | `.ai/tasks/DONE/TASK-0027-task-move-tool.md` |
| TASK-0028 | Commit lock (`claim.py`-backed) serializing stage-and-ship across threads, plus an index-hygiene guard refusing unexpected staged paths before commit — filed after a real misattributed-commit incident this session | Toolsmith | Done | P0 | 2026-07-05 | — | — | `.ai/tasks/DONE/TASK-0028-commit-lock.md` |
| TASK-0029 | Scoped `claim.py stage --expect` subcommand — stages exactly the declared `.ai/`/`.claude/` paths and self-verifies, closing the loop with `commit-guard` without making the tool's blanket whitelist imply unconstrained `git add` | Toolsmith | Done | P1 | 2026-07-05 | — | — | `.ai/tasks/DONE/TASK-0029-scoped-stage-tool.md` |
| TASK-0030 | Extract a shared, unit-tested Kabsch helper in `backend/` — dedupes `discovery.py::_kabsch` / `analysis.py::_kabsch_rotate`; reuse target for TASK-0005; concrete instance of TASK-0018 evidence #6 | Implementer | Done | P2 | 2026-07-05 | — | — | `.ai/tasks/DONE/TASK-0030-backend-kabsch-dedup.md` |
| TASK-0031 | Broaden `backend/data_layer.py::fetch()`'s exception handling (only catches `HTTPError`, not `URLError`/timeout) — High finding from the `Oussema-t` commit review | Implementer | Done | P1 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0031-data-layer-fetch-error-handling.md` |
| TASK-0032 | Escape RCSB-sourced strings before `innerHTML` injection in `frontend/app.js` — High finding from the `Oussema-t` commit review | Implementer | Done | P1 | 2026-07-05 | — | — | `.ai/tasks/DONE/TASK-0032-frontend-html-escaping.md` |
| TASK-0033 | Reconcile `quantum_seed_readiness`'s hardcoded verdict thresholds vs. commit `89215bc`'s "no hardcoding" claim — Medium finding, the one clear intent/implementation mismatch found | Implementer | Done | P2 | 2026-07-05 | — | — | `.ai/tasks/DONE/TASK-0033-seed-readiness-verdict-thresholds.md` |
| TASK-0034 | Reconcile RCSB-fetch error-handling philosophy + `data_layer.py`/`rcsb_extract.py` overlap — Medium finding, backend-internal instance of the TASK-0018 duplication pattern | Architect/Planner | TODO | P2 | 2026-07-05 | — | — | `.ai/tasks/TODO/TASK-0034-rcsb-fetch-error-philosophy-reconciliation.md` |
| TASK-0035 | Performance-audit the seed-readiness/permutation bootstrap cost -- measured first, per its own instruction. `site_potentials` confirmed a non-issue (0.27s). **`seed_readiness_shift` was a real, worse-than-suspected problem: 9.4-13.4s on the largest target (CARDIAC_MYOSIN, N=950).** Root cause one level deeper than the filing's own guess (GNM-context reuse was already in place, likely via TASK-0066): `_bootstrap_floor`'s 200-iteration loop recomputed `_abs_coupling`'s O(N^3) matrix product every iteration despite it not depending on the sampled residues. Fixed (precompute once, same rng draw sequence) -- **13.7x speedup on CARDIAC_MYOSIN (13.4s->0.978s), 10.6x on KRAS_G12C, both verified bit-identical output.** New characterization test coverage for `seed_readiness_shift` (previously untested). 31/31 backend tests pass | Implementer C | Done | P2 | 2026-08-14 | — | — | `.ai/tasks/DONE/TASK-0035-seed-readiness-bootstrap-performance.md` |
| TASK-0036 | Surface an explicit warning when `complete_apo` skips Kabsch alignment (currently only inferable from a null `align_rmsd`) — Medium finding | Implementer | TODO | P2 | 2026-07-05 | — | — | `.ai/tasks/TODO/TASK-0036-complete-apo-alignment-warning.md` |
| TASK-0037 | `H11_anisotropic_mechanical`/`H12_anm_scalarised` don't implement the anisotropic physics their docstrings claim (both silently duplicate H6/H2) — High finding from the `Bartosz`/`bchmura` commit review | Implementer | Done | P1 | 2026-07-05 | — | — | `.ai/tasks/DONE/TASK-0037-h11-h12-anisotropic-not-implemented.md` |
| TASK-0038 | `clean.py`'s module docstring says disconnected graphs are an error; `_assert_connected` only warns — contract mismatch | Implementer | Done | P1 | 2026-07-05 | — | — | `.ai/tasks/DONE/TASK-0038-clean-py-connectivity-gate-contract.md` |
| TASK-0039 | `clean.py`'s alt-loc handling always kept `'A'`, never implemented the docstring's "or highest occupancy" fallback. **Row was stale — marked Done at filing (2026-07-05) though the task file itself sat unimplemented in TODO/ until now; actually implemented 2026-08-24.** Pre-implementation review found a deeper root cause than the filing anticipated: ProDy's own `parsePDB()` default (`altloc="A"`) silently drops every non-`'A'` conformer at parse time, before any occupancy comparison is possible — fixed by switching to `altloc="all"`. Surveyed all 29 `targets.yaml` PDB IDs live: 5/29 have real alt-locs, and **one is a live, currently-active case** — CARDIAC_MYOSIN's own `holo_pdb` 8QYR, chain B residue 616, where the old code kept a minority conformer (A, occupancy 0.25) over the true majority one (C, occupancy 0.50); not one of the published pocket-label residues, so no headline number changes, but a real silent Cα-coordinate error until now. New `TestAltlocHighestOccupancy` (4 tests, real 8QYR/5MO4/4OBE data, not mocks) all pass; full suite re-run clean (1256 passed, 0 failed) | Implementer C | Done | P2 | 2026-08-24 | — | — | `.ai/tasks/DONE/TASK-0039-clean-py-altloc-occupancy-fallback.md` |
| TASK-0040 | `potentials.py` recomputed the GNM eigendecomposition redundantly. **Confirmed not already fixed by TASK-0066 (which deduped the code, not the computation) before assuming so.** Measured: 3 redundant O(N^3) `eigh` calls = ~78% of `build_H_new`'s own wall-clock on the largest target (CARDIAC_MYOSIN N=704, 0.308s). New `potentials.gnm_context()` (ported analogue of `backend/analysis.py::gnm_context`), optional `context=` param on `_gnm_msf`/`V_R`/`V_C`/`V_M`, `build_H_new` computes once and threads through. **Verified bit-identical via git-stash pre/post comparison on real data (max abs diff 0.0), ~2x speedup (0.308s->0.15-0.18s).** 7 new tests including a monkeypatched `eigh`-call-count check (3->1) and a pinned golden-value regression. Full suite: 1196 passed, 0 failed | Implementer C | Done | P2 | 2026-08-14 | — | — | `.ai/tasks/DONE/TASK-0040-potentials-shared-gnm-context.md` |
| TASK-0041 | `propagators.haken_strobl` never checks `solve_ivp`'s `sol.success` flag — a failed integration would silently return as if it succeeded. **Before implementing, checked reachability**: RK45 succeeds (fast, <1s) throughout this project's real gamma range (up to `gamma=100`, `dephasing_sweep`'s own ceiling); becomes impractically slow (not `success=False`) only around `gamma=1000-10000`, ~10-100x beyond any real call site -- a genuinely defensive gap, not a live bug. Added the `sol.success` check (raises `RuntimeError` naming gamma/t/status/message) regardless, plus 2 new regression tests (realistic-range pass, mocked-failure raise, since a genuine failure proved impractical to trigger with real physical parameters) | Implementer | Done | P2 | 2026-07-05 | — | — | `.ai/tasks/DONE/TASK-0041-haken-strobl-solve-ivp-success-check.md` |
| TASK-0042 | Hook-enforce the `GIT-COMMIT` gate via a Claude Code `PreToolUse` hook (upgrades TASK-0028's advisory lock to a blocking one) — filed as a handoff for Toolsmith, justified by this session's repeated real coordination incidents | Toolsmith | Done | P1 | 2026-07-05 | — | — | `.ai/tasks/DONE/TASK-0042-hook-enforced-commit-gate.md` |
| TASK-0043 | Split `bartosz` into a `scaffold` branch (40 commits, PR-ready toward `main`) holding back the one product-code commit (TASK-0030) as its own chunk — push/PR step blocked on user credentials, not available in this sandbox | Architect/Planner | Done | P1 | 2026-07-05 | — | — | `.ai/tasks/DONE/TASK-0043-scaffold-branch-split.md` |
| TASK-0044 | Reconcile the "Python 3.9" convention (CLAUDE.md/SOFTWARE.md/AGENTS.md) against the actual 3.11.9 Render runtime and a `biotite==0.41.0` pin requiring 3.10+ — no documented rationale found for 3.9 | Architect/Planner | TODO | P2 | 2026-07-06 | — | — | `.ai/tasks/TODO/TASK-0044-python-version-convention-reconciliation.md` |
| TASK-0045 | Extend `claim.py` with an atomic `reserve-next` task-id allocator — filed after this exact task number collided with a concurrent thread's TASK-0045 (see TASK-0046), which is the live incident motivating it | Toolsmith | Done | P1 | 2026-07-07 | — | — | `.ai/tasks/DONE/TASK-0045-highest-task-lookup-tool.md` |
| TASK-0046 | Give notebook §8's coordinate-descent ceiling search a home (`ceiling.py` or `analysis.ceiling_search`) — filed after TASK-0006/0007 landed, the trigger condition TASK-0008's own Open Questions named for this (renumbered from a colliding TASK-0045 claimed concurrently by another thread) | Implementer | Done | P1 (blocked on TASK-0008 if the analysis.py-function option is chosen) | 2026-07-07 | — | — | `.ai/tasks/DONE/TASK-0046-ceiling-coordinate-descent-search.md` |
| TASK-0047 | Bridge gaps from Reviewer A's Foundation Review (TASK-0003/0004/0005): add the missing network-gated KRAS_G12C integration test `labels.py`'s own TASK-0004 Intent Contract promised, and strengthen the numbering-offset test with a real alignment indel case | Implementer | Done | P2 | 2026-07-07 | — | — | `.ai/tasks/DONE/TASK-0047-foundation-review-gap-bridging.md` |
| TASK-0048 | Phase 3 review (Code Reviewer overlay): `protocol.py` (TASK-0006) + `select.py` (TASK-0007), both now Done — same evidence-first method as the Foundation review; deferred to next session per user direction | Code Reviewer | Done | P2 | 2026-07-07 | — | — | `.ai/tasks/DONE/TASK-0048-phase3-review-protocol-select.md` |
| TASK-0049 | Target decomposition proposal for `backend/` + `frontend/` into smaller single-concern modules (plan only, no code moved yet) — prepared ahead of a repository-standards meeting to diff against an external expert review | Architect/Planner | TODO | P1 | 2026-07-09 | — | — | `.ai/tasks/TODO/TASK-0049-backend-frontend-decomposition-proposal.md` |
| TASK-0050 | Adopt the Seam Protocol: `.ai/seams/` registry (5 seed records) + definition-of-done/green-bar gates + cross-link from OPERATION_PROTOCOL.md | Architect/Planner | Done | P1 | 2026-07-11 | — | — | `.ai/tasks/DONE/TASK-0050-adopt-seam-protocol.md` |
| TASK-0051 | Adopt the Invariance Protocol: `.ai/invariants/` registry seeded with `INV-0001` (real finding on `cumulative_overlap`), `SUGGESTION.md` absorbed into `pitfalls.md` | Architect/Planner | Done | P1 | 2026-07-11 | — | — | `.ai/tasks/DONE/TASK-0051-adopt-invariance-protocol.md` |
| TASK-0052 | Reconcile `test_leakage_gate.py`'s assumed `build_labels`/`FrozenConfig`/`lopo` contract against the real `labels.py`/`protocol.py` API — owns SEAM-0003 | Implementer | Done | P1 | 2026-07-11 | — | — | `.ai/tasks/DONE/TASK-0052-reconcile-leakage-gate-contract.md` |
| TASK-0053 | First seam sweep (General Critic overlay) — enumerate cross-unit invariants across TASK-0003–0012, register new `.ai/seams/` records | General Critic | Done | P2 | 2026-07-11 | — | — | `.ai/tasks/DONE/TASK-0053-first-seam-sweep.md` |
| TASK-0054 | Add the missing SE(3)-joint-rotation regression test for `cumulative_overlap`/`anm_modes` — owns INV-0001's one OPEN GAUGE row. Arbitrary off-axis rotation + translation applied jointly to coords/delta_r, full CO(m) output matches pre-transform to atol=1e-9 -- **passes**, confirms the historical select-by-eigenvalue fix generalizes correctly. Absorbed a translation-alone isolation test and a residue-permutation sibling test (closes INV-0001's 2nd OPEN row too, all 3 GAUGE rows now GAUGE-VERIFIED). Found+fixed a stale `==6`-mode-count claim in INV-0001, superseded by TASK-0128's `>=6` widening but never reflected there | Implementer | Done | P2 | 2026-07-11 | — | — | `.ai/tasks/DONE/TASK-0054-se3-invariance-regression-test.md` |
| TASK-0055 | Verify `AUC_*_optimised` provenance is structurally tied to `protocol.py`'s frozen state, not just a matching label — owns SEAM-0004 | Implementer | Done | P2 | 2026-07-11 | — | — | `.ai/tasks/DONE/TASK-0055-optimised-auc-freeze-provenance-check.md` |
| TASK-0056 | Phase 4 review (Code Reviewer overlay): `diagnostics.py`/`report.py`/`baselines.py`/`pathways.py` (TASK-0009-0012), none reviewed yet — cross-checks SEAM-0004/SEAM-0005 while reading `report.py`/`baselines.py` | Code Reviewer | Done | P2 | 2026-07-11 | — | — | `.ai/tasks/DONE/TASK-0056-phase4-review-diagnostics-report-baselines-pathways.md` |
| TASK-0057 | Retroactively apply Seam/Invariance protocol to TASK-0012 (`pathways.py`): registers SEAM-0006 (owner TASK-0014) + INV-0002, writes real SE(3)/permutation invariance tests | Implementer | Done | P2 | 2026-07-11 | — | — | `.ai/tasks/DONE/TASK-0057-pathways-seam-invariance-registration.md` |
| TASK-0058 | Wire a `floor_scores` beats-floor check into `diagnostics.classify_failure` — closes SEAM-0005 (a seam-test already exists, `xfail(strict=True)` pending this task) | Implementer | Done | P2 | 2026-07-11 | — | — | `.ai/tasks/DONE/TASK-0058-wire-baseline-floor-into-classify-failure.md` |
| TASK-0059 | Wire the cumulative-overlap go/no-go gate into which targets `analysis.py` actually scores — closes SEAM-0007, found by TASK-0053's sweep | Implementer | Done | P2 | 2026-07-11 | — | — | `.ai/tasks/DONE/TASK-0059-wire-openness-gate-into-scoring.md` |
| TASK-0060 | `claim.py question` subcommand family for the `.ai/memory/questions/` lifecycle (move between open/answered/need-action) + a `derive-task` helper reusing `reserve-next` — filed after answering Q-0001 by hand | Toolsmith | TODO | P2 | 2026-07-11 | — | — | `.ai/tasks/TODO/TASK-0060-question-lifecycle-tooling.md` |
| TASK-0061 | New `claim.py add <TASK-ID> <path> --purpose "..."` (+ manifest-file batch mode) — stages a file and appends an attributed record to the claiming task's own file, any path (not `.ai/`-scoped like `stage`) since attribution, not directory, is the safety property; commit gate unchanged. **Done.** Built as specified: single-file + manifest (array or shared/override-purpose object form) both live-tested; section created immediately before `## Done`; all-or-nothing on a malformed/missing-file manifest; `stage`/`commit-guard` confirmed byte-identical. Dogfooded on its own 4 changed files. 14 new tests (148 total) | Toolsmith | Done | P2 | 2026-07-11 | — | — | `.ai/tasks/DONE/TASK-0061-staged-files-task-audit-trail.md` |
| TASK-0062 | `.ai/memory/incidents/` registry + `.ai/tools/incident.py` — structured facts (detection/blast-radius/data-loss/recovery), not self-declared severity, so severity can be inferred consistently across incidents later | Toolsmith | TODO | P2 | 2026-07-11 | — | — | `.ai/tasks/TODO/TASK-0062-incident-reporting-registry.md` |
| TASK-0063 | `protocol.get_functional_indices` doesn't forward `labels.functional_indices`'s `heavy_atom_coords`/`heavy_atom_seq_index` — FROZEN-path callers silently forced onto the coarser Cα-only approximation, no way around the gate — TASK-0048's P2 finding | Implementer | Done | P2 | 2026-07-11 | — | — | `.ai/tasks/DONE/TASK-0063-functional-indices-gate-parameter-gap.md` |
| TASK-0064 | Wire `select.unsupervised_score` into a real FROZEN-loop consumer — closes SEAM-0009 (`analysis.py` never imports `select.py` despite both Done), found by TASK-0048's Phase 3 review, missed by TASK-0053's same-day sweep | Implementer | Done | P2 | 2026-07-11 | — | — | `.ai/tasks/DONE/TASK-0064-wire-unsupervised-score-into-frozen-loop.md` |
| TASK-0065 | Stage-Commit-Queue (SCQ) — gitignored, per-entry `.ai/tasks/.locks/SCQ-*.lock` files (reuses existing gitignore, no new rule); `scq-enter`/`scq-leave` let a thread proactively publish ticket + file list + prepared message, `status GIT-COMMIT` shows the ordered queue; visibility only, no ordering enforcement. Revised 2026-07-17 from refusal-only `--enqueue` to a standalone, proactive subcommand | Toolsmith | Done | P3 | 2026-07-17 | — | — | `.ai/tasks/DONE/TASK-0065-git-commit-queue.md` |
| TASK-0066 | Shared Kirchhoff-context + DCC numpy helper — dedupes `backend/analysis.py::gnm_context`/`_dcc` vs `allostery/potentials.py::_gnm_msf`/`V_R`/`V_C`/`V_M`'s independently re-derived pseudo-inverse math (TASK-0030-style port, not import); filed by TASK-0018's decision doc | Implementer | Done | P2 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0066-shared-kirchhoff-dcc-helper.md` |
| TASK-0067 | Run the T-018 (GNM cutoff 7.5/8.0/10.0 Å) and T-021 (5 contact-weight schemes) benchmark against `config/targets.yaml` — resolves the three-way cutoff divergence TASK-0018's decision doc found, wraps closed-ledger T-018/T-021 | Implementer | Done | P1 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0067-gnm-cutoff-weight-scheme-benchmark.md` |
| TASK-0068 | NISQ noise-model simulation (depolarizing + amplitude-damping Trotterized sim, top-5 degradation vs depth/error) consuming `coarse.py`'s output — owns SEAM-0010, filed per TASK-0013's own Open Question once `coarse.py` landed | Implementer | Done | P2 | 2026-07-14 | — | — | `.ai/tasks/DONE/TASK-0068-nisq-noise-model-simulation.md` |
| TASK-0069 | `pytest_local.py`'s `wip-*` presets resolve the wrong venv (top-level `.venv`, not `__WORK_IN_PROGRESS__/.venv`) now that a root venv exists — found live while TASK-0014 needed `matplotlib`, installed in the WIP venv only. **Done.** `_resolve_interpreter` now per-preset (`prefer_wip` derived from each preset's existing `extra_pythonpath`, no PRESETS-table change): every preset except `backend` prefers `__WORK_IN_PROGRESS__/.venv` → root `.venv` → `sys.executable`. Currently latent — repo has consolidated to one root `.venv` built to the *research* lock (numpy 2.5.1), so all presets still resolve to it, zero behavior change today. 8 new tests (resolved-path assertions, not "tests pass"). numpy/scipy backend-vs-research divergence surfaced with data, routed to TASK-0044 | Toolsmith | Done | P1 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0069-pytest-local-wip-venv-resolution-regression.md` |
| TASK-0070 | Pocket label exclusion assembly — assemble `pocket & ~functional & ~terminal` (`build_labels`), assert `pocket ∩ active_site == ∅`, record the KRAS Cys12 in/out decision — filed from `EXECUTION_PLAN.md` Phase 0.1, the oldest live defect (no seam-owner) | Implementer | Done | P0 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0070-pocket-label-exclusion-assembly.md` |
| TASK-0071 | Permutation-null leak detector in `diagnostics.py` — shuffle labels, re-run, flag anything still scoring above chance, backstopping the DEV/FROZEN firewall's prevention with real-time detection — `EXECUTION_PLAN.md` Phase 1.4 | Implementer | Done | P1 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0071-permutation-null-leak-detector.md` |
| TASK-0072 | Golden-value cross-tree drift test — new `test_golden_value_cross_tree_drift.py` (repo root) fixes a synthetic, network-free 40-residue reference structure and asserts `backend.analysis`/`allostery.potentials`'s shared `_kirchhoff_eigh`/`_normalized_dcc` (TASK-0066) are **bit-for-bit identical** (not just float-close, confirmed directly) at cutoff=8.0 (TASK-0067's resolved constant, backend's own unchanged live default). Real (harmless) 6-tuple-vs-5-tuple signature difference (`deg` present/absent) found and handled explicitly, not papered over. Drift detector constructed and confirmed to fire (TASK-0103 discipline): a mismatched cutoff and an unnormalized-covariance DCC both correctly detected as divergence. Wired into `.ai/tools/pytest_local.py` as a new `cross-tree` preset + extended into `all` (TASK-0077's CI workflow still TODO, so this is "runnable now," not a new CI pipeline). `all` preset (wip-all+backend+cross-tree): 1096 passed, 2 xfailed, 0 failed | Implementer | Done | P2 | 2026-08-02 | — | — | `.ai/tasks/DONE/TASK-0072-golden-value-cross-tree-drift-test.md` |
| TASK-0073 | Register cross-tree seams — `.ai/seams/` records (owner + seam-test) for the shared Kirchhoff/DCC primitive and the GNM cutoff constant — `EXECUTION_PLAN.md` Phase 2.5 | Implementer | Done | P2 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0073-register-cross-tree-seams.md` |
| TASK-0074 | Characterization tests for `backend/analysis.py` (`gnm_context`/`site_potentials`/`quantum_seed_readiness`/`connectivity_change`) pinning current live output before any convergence touches it — hard-rule prerequisite for TASK-0066/0072 per `EXECUTION_PLAN.md` Phase 4.2 | Implementer | Done | P1 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0074-backend-analysis-characterization-tests.md` |
| TASK-0075 | Knob-spread reporting for the cumulative-overlap gate — sweep cutoff × variant × k × reference, return `UNSTABLE` when knobs (not physics) decide go/no-go (18-combo toy case swung 0.067–0.860, flipped 15/18) — `EXECUTION_PLAN.md` Phase 5.5, ties to INV-0001 | Implementer | Done | P1 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0075-knob-spread-overlap-gate-reporting.md` |
| TASK-0076 | WIP graduation part 1: `git mv __WORK_IN_PROGRESS__/src/allostery → allostery/` at repo top level, update imports/test paths, behaviour-neutral (252-green before/after) — `EXECUTION_PLAN.md` Phase 3.1 | Implementer | TODO | P2 | 2026-07-12 | — | — | `.ai/tasks/TODO/TASK-0076-graduation-part1-promote-package.md` |
| TASK-0077 | WIP graduation part 2: single CI workflow running `backend/` + `allostery/` suites together, ending the two-test-worlds split — `EXECUTION_PLAN.md` Phase 3.2, depends on TASK-0076 | Implementer | TODO | P2 | 2026-07-12 | — | — | `.ai/tasks/TODO/TASK-0077-graduation-part2-one-ci.md` |
| TASK-0078 | WIP graduation part 3: move remaining `__WORK_IN_PROGRESS__` contents to permanent homes (`config/`, `notebooks/`, `documentation/`), delete the folder — `EXECUTION_PLAN.md` Phase 3.3, depends on TASK-0076/0077 | Implementer | TODO | P2 | 2026-07-12 | — | — | `.ai/tasks/TODO/TASK-0078-graduation-part3-dissolve-folder.md` |
| TASK-0079 | End-to-end challenge run (parent, thin coordinator — split into .001-.005 subtasks 2026-07-12) — one command producing the N×N connectivity matrix, top-5 hit list, and methodological report per mandatory target (KRAS_G12C, BCR_ABL1, CARDIAC_MYOSIN; MYC_MAX is TASK-0080) — `EXECUTION_PLAN.md` Phase 5.1, critical-path link | Implementer | Done | P0 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0079-end-to-end-challenge-run.md` |
| TASK-0079.001 | Schema assembly: `analysis.py` real nested output -> `report.py`'s flat results dict — closes SEAM-0008 | Implementer | Done | P0 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0079.001-analysis-report-schema-assembly.md` |
| TASK-0079.002 | Connectivity matrix (`pathways.edge_propensity` half-matrix -> dense N×N array) + hit-list assembly (`report.hit_list`) | Implementer | Done | P0 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0079.002-connectivity-matrix-hit-list-assembly.md` |
| TASK-0079.003 | FROZEN-gated per-target verdict pipeline — `select_frozen_config` -> `analysis.py` scoring -> `stamp_provenance` -> .001's assembly, all inside one `frozen_context` | Implementer | Done | P0 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0079.003-frozen-gated-verdict-pipeline.md` |
| TASK-0079.004 | One-command orchestrator (`run_challenge.py --target ...`) + output files — depends on .002, .003 | Implementer | Done | P0 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0079.004-one-command-orchestrator.md` |
| TASK-0079.005 | Run end-to-end for KRAS_G12C / BCR_ABL1 / CARDIAC_MYOSIN (live network) + manual inspection against Acceptance Scenarios — depends on .004 | Implementer | Done | P0 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0079.005-run-mandatory-targets.md` |
| TASK-0080 | c-Myc/1NKP application — no holo ground truth, route to consensus + theoretical docking viability reporting instead of AUC/ceiling — `EXECUTION_PLAN.md` Phase 5.7 | Implementer | Done | P2 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0080-cmyc-1nkp-application.md` |
| TASK-0081 | Generalization set — run the frozen pipeline on 2–4 extra Allosteric-Database targets to evidence robustness/scalability, encouraged (not required) by the brief — `EXECUTION_PLAN.md` Phase 5.8, depends on TASK-0079 | Implementer | Done | P2 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0081-generalization-set-asd-targets.md` |
| TASK-0082 | Competence map synthesis — per-target floor/ceiling/headroom table, the submission's central "honest NO is a publishable result" claim — `EXECUTION_PLAN.md` Phase 5.2, critical-path link | Implementer | Done | P0 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0082-competence-map-synthesis.md` |
| TASK-0083 | Result artifact contract ⚠️ decide EARLY — versioned NPZ/JSON artifact `allostery` emits and `backend` consumes (connectivity matrix, ranked hits, verdict+knob-spread, floor/ceiling/headroom, frozen-config hash) so `backend` never recomputes science in the request path — `EXECUTION_PLAN.md` Phase 6.1, critical-path link, keystone decision | Architect/Planner | Done | P0 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0083-result-artifact-contract.md` |
| TASK-0084 | Backend results API — ADD-only endpoints (`/api/results/{target}`, `/api/connectivity/{target}`, `/api/verdict/{target}`) serving precomputed artifacts, no science in the request path — `EXECUTION_PLAN.md` Phase 6.2, depends on TASK-0083 | Implementer | TODO | P2 | 2026-07-12 | — | — | `.ai/tasks/TODO/TASK-0084-backend-results-api.md` |
| TASK-0085 | Frontend research visualization — top-5 pockets on 3Dmol, N×N connectivity heatmap, floor/method/ceiling competence panel, explicit `UNSTABLE` state — the scored interpretability objective — `EXECUTION_PLAN.md` Phase 6.3, depends on TASK-0084 | Implementer | TODO | P2 | 2026-07-12 | — | — | `.ai/tasks/TODO/TASK-0085-frontend-research-visualization.md` |
| TASK-0086 | Execution/trigger path — decide + implement offline-batch (preferred) vs async-job-queue run triggering so artifacts are producible without heavy compute in the web request path — `EXECUTION_PLAN.md` Phase 6.4, depends on TASK-0083 | Architect/Planner | TODO | P1 | 2026-07-12 | — | — | `.ai/tasks/TODO/TASK-0086-execution-trigger-path.md` |
| TASK-0087 | Harden `protocol.py`'s firewall from a cooperative gate (bypassable by calling `labels.py`/`superpose.py` directly) to a hard data-seal matching `test_leakage_gate.py`'s `_SealedLabels` reference spec, or explicitly document the weaker guarantee as an accepted trust boundary — real gap found verifying equivalence for TASK-0052 | Implementer | Done | P1 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0087-harden-protocol-firewall-to-data-seal.md` |
| TASK-0088 | Structurally gate `provenance="frozen"` in `report.verdict_template` against `protocol.py`'s frozen-config state machine (e.g. a `stamp_provenance()` helper only callable from inside `frozen_context`) — closes SEAM-0004, real gap found by TASK-0055 | Implementer | Done | P1 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0088-structurally-gate-frozen-provenance.md` |
| TASK-0089 | Classify `select.py`'s reported quantities (GAUGE/KNOB/SIGNAL) — closes `INV-0004`, flagged by TASK-0064 | Implementer | Done | P2 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0089-select-invariance-classification.md` |
| TASK-0090 | `select.py::ballistic_exponent`/`source_specificity` crash (or silently mis-score) on a multi-index `source` — escalated P0 2026-07-16, blocked TASK-0118. Fixed `_hop_distances_from_source` (multi-source BFS, was silently wrong not crashing) and `source_specificity`'s `others` exclusion set (the actual crash site — its own Intent Contract wrongly marked this "already correct", checked by execution and fixed anyway). Real KRAS_G12C validation: full 18-residue active-site array through `unsupervised_score`, no crash. `run_challenge.py`'s workaround itself left for TASK-0118 to remove (owns the seed-convention decision) | Implementer | Done | P0 | 2026-07-16 | — | — | `.ai/tasks/DONE/TASK-0090-select-py-multi-index-source-crash.md` |
| TASK-0091 | **RE-FILED 2026-07-13** (original dephasing-sweep framing retracted, false premise — see REVIEW-2026-07-13 P1-B): does `H_new`'s ground-state localization detect BCR_ABL1's real *distal* (~25 Å) myristoyl pocket (0.731) once TASK-0094's proximity floor is applied — hard-blocked on TASK-0094 and TASK-0095 | Implementer | Done | P1 | 2026-07-13 | — | — | `.ai/tasks/DONE/TASK-0091-bcr-abl1-dephasing-sweep-investigation.md` |
| TASK-0092 | Wire `run_frozen_verdict`'s already-supported holo kwargs into the real end-to-end run (`AUC_holo_Hnew_optimised`/`mean_rho_apo_holo`/`mean_jacc20` are `N/A` in both real reports) — a diagnostic upper bound, not a prediction substitute; extends TASK-0067's own apo-vs-holo finding (tiny gap, bare operator) to the full `H_new` pipeline — real gap from TASK-0079.005 | Implementer | Done | P2 | 2026-07-12 | — | — | `.ai/tasks/DONE/TASK-0092-holo-diagnostic-comparison-real-run.md` |
| TASK-0093 | Reconcile KRAS_G12C's real end-to-end AUC (0.779) against this repo's own previously-asserted near-chance band (0.3-0.7, `test_analysis.py`) for the same target — factorial isolation of cutoff (8.0 vs 10.0 Å) / pocket-label definition (assembled vs raw) / source definition (apo- vs holo-derived) — real discrepancy from TASK-0079.005. **Updated 2026-07-13**: candidate mechanism (cutoff changes distance-decay) + every combination must clear TASK-0094's proximity floor, not just chance — hard-blocked on TASK-0094 | Implementer | Done | P1 | 2026-07-13 | — | — | `.ai/tasks/DONE/TASK-0093-kras-auc-discrepancy-reconciliation.md` |
| TASK-0094 | Proximity baselines into the floor — add `euclid_from_seed_centroid`/`hop_from_seed` to `baselines.py`, wire into `floor_scores`/`classify_failure`, re-run all mandatory targets. **Blocks every current headline number in RESULTS.md; do this first.** REVIEW-2026-07-13 P1-A: BCR_ABL1 (the one genuinely distal pocket) is the one target at chance — signature of a proximity detector, not an allostery detector | Implementer | Done | P0 | 2026-07-13 | — | — | `.ai/tasks/DONE/TASK-0094-proximity-baselines-into-floor.md` |
| TASK-0095 | Correct propagator semantics — renamed `heat`→`ground_state_relaxation`, added an indefinite-operator warning guard (`strict=True` to raise), corrected `RESULTS.md`/`report.py` framing. REVIEW-2026-07-13 P1-B: `heat(H_new)` was the ground-state density (Spearman 0.998 vs `\|ground state\|²`), not classical diffusion — BCR_ABL1's "0.731 classical heat" framing was a false physical claim, now fixed. Also closed TASK-0016 (superseded) | Implementer | Done | P1 | 2026-07-13 | — | — | `.ai/tasks/DONE/TASK-0095-correct-propagator-semantics.md` |
| TASK-0096 | Delete or implement the phantom `H11_anisotropic_mechanical`/`H12_anm_scalarised` operators — both verified (`np.allclose`) identical to `H6`/unweighted-contact Laplacian despite docstrings claiming anisotropic weighting; closes TASK-0037 for real (fix or removal, not just "known gap") — REVIEW-2026-07-13 P2-A | Implementer | Done | P2 | 2026-07-13 | — | — | `.ai/tasks/DONE/TASK-0096-delete-or-implement-phantom-operators.md` |
| TASK-0097 | Name the "quantum metric" honestly — state in `RESULTS.md`/methodological report that `time_averaged_ctqw` is the decoherent/infinite-time-average limit (a spectral overlap quantity), not a coherent quantum walk — REVIEW-2026-07-13 P2-B | Implementer | Done | P2 | 2026-07-13 | — | — | `.ai/tasks/DONE/TASK-0097-name-quantum-metric-honestly.md` |
| TASK-0098 | Amend `.ai/reference/INVARIANCE_PROTOCOL.md`'s SIGNAL class — null control must ask "does it beat the strongest domain heuristic (distance-from-seed)?", not just "does it beat random?" — third instance of an unexecuted assumed-gauge symmetry per REVIEW-2026-07-13's Meta section (after SE(3) rotation, SU(2)/Clifford-frame). **Classified precisely, not just rhetorically**: this is a SIGNAL-tier weak-null-control gap, not a GAUGE violation (seed-dependence is correct/designed CTQW behavior, not a bug) — amended `INVARIANCE_PROTOCOL.md` in 5 places (opening bug list, classification table, new general-rule paragraph, new Tier 3b, new Rule #5) plus a cross-link from `.ai/invariants/README.md`'s own separate criteria enumeration. Documentation only, no code changed | Architect/Planner | Done | P2 | 2026-07-13 | — | — | `.ai/tasks/DONE/TASK-0098-amend-invariance-protocol-signal-class.md` |
| TASK-0099 | Wire a genuine coherence-sensitivity metric into the scored verdict path — `analysis.coherence_sensitivity` wires `dephasing_sweep` into `assemble_verdict_results`/`verdict_template` (calibrated γ, floor-gated via `classify_failure`). KRAS_G12C real result: flat, `COHERENCE_NOT_SIGNIFICANT`, robust across t_max∈{8,25,100} and 2 seed conventions (post-completion audit vs `REVIEW-panel-2026-07-16-v2`). BCR_ABL1/CARDIAC_MYOSIN blocked on `calibrate_kappa`'s exactly-6-mode assertion — filed as TASK-0128. New: INV-0007 (renumbered 2026-07-16/17, collided with TASK-0109's INV-0005), SEAM-0008 extended, pitfalls P-0003 | Implementer | Done | P1 | 2026-07-16 | — | — | `.ai/tasks/DONE/TASK-0099-wire-coherence-metric-into-scored-verdict.md` |
| TASK-0100 | Decide the shape of a real cross-operator sweep against the proximity floor — we have 14 named Hamiltonian operators (`H1`-`H14`) but no runtime harness ever runs more than 2 (`H_new`, `H10`) against a real benchmark target and rates the result; where does the sweep live, what does "rate the results" mean, which operators are in scope, what happens to ones that don't clear the floor — filed per explicit user direction, 2026-07-13. **Decided**: library fn `analysis.operator_sweep` + thin `scripts/sweep_operators.py` (not `run_challenge.py`); two-tier rating (Tier-1 descriptive, no frozen-gate; Tier-2 selection, hard-gated through `select_frozen_config`/LOPO); 3-tier operator classification (A: `H_new`/`H10`/`H14` selection-eligible, B: `H1`-`H9`/`H11`-`H13` descriptive-only); floor-failing operators recorded not deleted. TASK-0093/TASK-0099 confirmed NOT blocked (diagnostics on already-chosen `H_new`, not selection acts). Follow-up: TASK-0101 | Architect/Planner | Done | P1 | 2026-07-13 | — | — | `.ai/tasks/DONE/TASK-0100-operator-sweep-architecture-decision.md` |
| TASK-0101 | Operator sweep Tier-1 harness — `analysis.operator_sweep` + `scripts/sweep_operators.py`, scores all 14+2 operators **× both propagators** (`time_averaged_ctqw`, `ground_state_relaxation`) against TASK-0094's proximity floor on all 3 mandatory targets (96 cells), reports a table. **Amended 2026-07-13**: must support chunked/resumable/parallel runs (`--target`/`--operator`/`--propagator` filters, per-cell result files, skip-if-exists + `--force` to recompute specific suspicious cells, `--aggregate` to merge partial runs from multiple machines). Tier-2 selection explicitly out of scope — follow-up from TASK-0100's decision | Implementer | Done | P1 | 2026-07-13 | — | — | `.ai/tasks/DONE/TASK-0101-operator-sweep-tier1-harness.md` |
| TASK-0102 | Is `ground_state_relaxation`'s BCR_ABL1 result (TASK-0091, AUC 0.7315) seed-dependent, or an artifact of the potential's global minimum being a fixed, seed-independent feature that just happens to sit near the pocket? Real-data seed-invariance check first (rerun with different seeds on the cached real `H_new`, compare occupancy correlation), spectral-gap convergence check second, synthetic discriminating network (engineered minimum away from vs. coupled to a "drug site") third if needed — filed per explicit user question, 2026-07-13 | Implementer | Done | P1 | 2026-07-13 | — | — | `.ai/tasks/DONE/TASK-0102-ground-state-relaxation-seed-invariance-test.md` |
| TASK-0103 | Dumbbell 2x2 negative-control test suite (well vs. coupling confound, synthetic, no PDB/network) — GSR follows the well, CTQW follows the coupling; from REVIEW-2026-07-13b T-A, "do immediately, unblocks everything" | Implementer | Done | P0 | 2026-07-13 | — | — | `.ai/tasks/DONE/TASK-0103-dumbbell-negative-control-suite.md` |
| TASK-0104 | Re-frame BCR_ABL1 GSR=0.731 claim in RESULTS.md from "allosteric signal propagation" to "apo-computable structural prior" — number unchanged, claim corrected; from REVIEW-2026-07-13b T-B | Implementer | Done | P0 | 2026-07-13 | — | — | `.ai/tasks/DONE/TASK-0104-reframe-gsr-bcr-abl1-claim.md` |
| TASK-0105 | ENAQT gamma-sweep on real mandatory targets — interior-optimum transport measurement (not AUC recovery), reframes the killed dephasing question; from REVIEW-2026-07-13b T-C, "highest scientific upside in the repo" | Implementer | Done | P0 | 2026-07-14 | — | — | `.ai/tasks/DONE/TASK-0105-enaqt-gamma-sweep-real-targets.md` |
| TASK-0106 | Reproduce the CTQW-trapping finding (REVIEW-2026-07-13c) on real BCR_ABL1 data — H10/H2 vs H_new(lambda=0.25) vs H_new(default) distal-pocket AUC + transport diagnostic, checked against the proximity floor | Implementer | Done | P0 | 2026-07-14 | — | — | `.ai/tasks/DONE/TASK-0106-ctqw-trapping-real-data-reproduction.md` |
| TASK-0108 | CTQW/propagator parameter validity + Optuna floor/ceiling scan (parent/coordinator for TASK-0109/0110) — filed per explicit user request after confirming zero validity checks exist for t_max/n_steps/gamma | Architect/Planner | Done | P1 | 2026-07-15 | — | — | `.ai/tasks/DONE/TASK-0108-ctqw-parameter-validity-and-optuna-scan.md` |
| TASK-0109 | Propagator convergence-validity check + synthetic power-law characterization (t_max/n_steps vs. system size/spectral gap) + literature grounding — no PDB/network, generalizes TASK-0102's one-off check | Implementer | Done | P1 | 2026-07-15 | — | — | `.ai/tasks/DONE/TASK-0109-propagator-convergence-check-and-power-laws.md` |
| TASK-0110 | Optuna parameter scan on real mandatory targets: apo-only scan (floor, convergence-objective) + holo-informed scan (ceiling, AUC-objective) for CTQW's numerical parameters — new `optuna` dependency, Tier-1 only per TASK-0100 | Implementer | Done | P1 | 2026-07-15 | — | — | `.ai/tasks/DONE/TASK-0110-optuna-apo-holo-parameter-scan.md` |
| TASK-0107 | `claim.py resolve` — on-disk backend adapter for the catalogued `workflow.task.resolve` capability: explicit canonical resolution value (`RESOLUTION_VOCABULARY.md`), moves to DONE, stages task file + `COMMON.md` row by default, `--no-stage` opt-out via `git reset` | Toolsmith | Done | P2 | 2026-07-14 | — | — | `.ai/tasks/DONE/TASK-0107-claim-resolve-subcommand.md` |
| TASK-0111 | `propagators.ctqw`/`time_averaged_ctqw` redundantly recompute `eigh(H)` on every call inside the `n_steps` time-average loop (up to 500x per call) — found investigating a `wip-all` hang root-caused to `test_ceiling.py`'s 60-trial real-target search | Implementer | Done | P2 | 2026-07-15 | — | — | `.ai/tasks/DONE/TASK-0111-ctqw-redundant-eigh-performance.md` |
| TASK-0112 | Wire `metrics.block_bootstrap_ci` into headline AUC reporting (`classify_failure` floor comparison + `verdict_template` rendering) — currently every floor-vs-score verdict in `RESULTS.md` is a bare point estimate — `REVIEW-2026-07-15-execution-plan-gap-audit.md` finding #1, should gate Phase 5.2. **Real re-run finding**: all 3 named comparisons (KRAS_G12C euclid floor; BCR_ABL1 ctqw + ground_state vs floor) have overlapping 95% CIs — none statistically decisive, including BCR_ABL1 GSR's "decisive +0.166" margin | Implementer | Done | P1 | 2026-07-17 | — | — | `.ai/tasks/DONE/TASK-0112-bootstrap-ci-headline-auc.md` |
| TASK-0113 | Re-run TASK-0067's GNM cutoff sweep (7.5/8.0/10.0 Å) against the actual headline operator (`H_new`, both propagators) instead of the bare Kirchhoff Laplacian TASK-0067 scored — `REVIEW-2026-07-15-execution-plan-gap-audit.md` finding #2. **Real, structural finding checked before running anything**: `H_new`'s own base Laplacian hardcodes `weight="exponential"` (`normalised_laplacian_alpha`), so unlike TASK-0067's 2-axis grid there is no weight-scheme knob to sweep for `H_new` — cutoff-only, reported not forced. Cross-validated exactly against [[TASK-0130]]'s own independently-computed numbers at the shipped 8.0 Å cutoff (BCR_ABL1 ctqw 0.5266, ground_state 0.6766). **Agrees with TASK-0067 in aggregate (if anything more stable: ctqw range 0.003 vs TASK-0067's 0.0124) but that stability is a coincidence of KRAS_G12C/BCR_ABL1 moving in opposite directions (~0.04-0.05 each) for ctqw, not real insensitivity.** **Real, headline-relevant disagreement: BCR_ABL1's `ground_state_relaxation` floor-clearing status (its own "apo-computable structural prior" finding, TASK-0091/0102/0104) flips at 10.0 Å** — clears at 7.5/8.0 Å, does not at 10.0 Å (both AUC and floor move, ordering flips). `time_averaged_ctqw`'s own floor-crossing story stays fully stable across the grid for both targets. Cross-linked into `RESULTS.md` rows 1/23, not silently corrected | Implementer | Done | P2 | 2026-07-22 | — | — | `.ai/tasks/DONE/TASK-0113-cutoff-sweep-headline-operator.md` |
| TASK-0114 | Pocket-label ligand-contact cutoff (4.5 Å, `holo_pocket_mask`, defines ground truth itself) sensitivity sweep — a second, distinct threshold from the GNM graph cutoff (TASK-0067/0113), never checked for the same knob-instability TASK-0075 already proved for the cumulative-overlap gate (0.067–0.860 swings, 15/18 flips). Swept {4.0,4.5,5.0,5.5} Å on all 3 mandatory targets, re-scoring the same once-computed `H_new`/closed-form occupation against each cutoff's regenerated label only (no operator recomputation). **Much better-behaved than TASK-0075's gate**: `floor_cleared` never flips for any target across the whole grid. `classify_failure`'s finer diagnosis category does flip for 2/3 targets at the grid's edges (KRAS_G12C `NO_FAILURE_DETECTED`→`NO_SIGNAL_IN_APO` at 5.5 Å; BCR_ABL1 `BEATS_CHANCE_NOT_FLOOR`→`NO_SIGNAL_IN_APO` between 4.0/4.5 Å) — real, narrow sensitivity in the reported label. TASK-0047's pinned 4.5 Å KRAS_G12C fixture (21/21) confirmed exactly correct but sits on a still-moving slope, not a plateau (raw recovered set changes at every 0.5 Å step, both directions). CARDIAC_MYOSIN run against [[TASK-0124]]'s new 8QYP structure (landed same day, discovered mid-task via an N mismatch, not assumed) — flagged as not comparable to this project's other (5TBY-era) CARDIAC_MYOSIN numbers. Shipped 4.5 Å default unchanged, per this task's own Out Of Scope | Implementer | Done | P2 | 2026-07-20 | — | — | `.ai/tasks/DONE/TASK-0114-pocket-label-cutoff-sensitivity.md` |
| TASK-0115 | Name and gate the repeated-exposure risk across ~15 human review cycles on the same 3-target answer key — code-level gates (`frozen_context`/TASK-0100) can't catch it; document + require TASK-0081's generalization set before any `RESULTS.md` claim is final — `REVIEW-2026-07-15-execution-plan-gap-audit.md` finding #4 | Architect/Planner | Done | P2 | 2026-07-15 | — | — | `.ai/tasks/DONE/TASK-0115-repeated-exposure-generalization-gate.md` |
| TASK-0116 | Ceiling search (TASK-0046) blind 60-trial random search over ~8D is weak evidence for its own "no headroom" negative claim — re-run with a space-filling design or real optimizer on KRAS_G12C — `REVIEW-2026-07-15b-ceiling-search-methodology.md` finding #1. **Prerequisite found first**: `ceiling.py::_PARAM_RANGES` sampled `lam_*` on `(0.0,2.0)` each, ~25x [[TASK-0121]]'s own `sigma(V)<=0.2*J<=0.4` budget — fixed via a constrained-simplex `sample_params` (budget in `[0,0.4]`, `Dirichlet` split), then added `ceiling.ceiling_search_optuna` (TPE) as the strategy upgrade. Real KRAS_G12C re-run, deltas kept separate: range-fix-only isolated (`coherent=True`, byte-identical to original) = 0.4864 vs original 0.5250; range-fix+TPE at the current convention = 0.4908 (reproduced exactly), marginally above the re-measured floor (0.4818, +0.009, not claimed as decisive — needs [[TASK-0131]]'s permutation null). **Confirms, does not overturn, TASK-0046's near-chance conclusion** — cross-validates [[TASK-0110]]'s independent 0.4750 on a different parameter axis | Implementer | Done | P1 | 2026-07-15 | — | — | `.ai/tasks/DONE/TASK-0116-ceiling-search-coverage-upgrade.md` |
| TASK-0117 | Ceiling search's `t_max=15`/`n_steps=500` share the same unvalidated CTQW parameters TASK-0108/0109/0110 exist to check — apply TASK-0109's convergence check to `ceiling.py` once it lands, hard-blocked — `REVIEW-2026-07-15b-ceiling-search-methodology.md` finding #2 | Implementer | Done | P1 | 2026-07-15 | — | — | `.ai/tasks/DONE/TASK-0117-ceiling-search-convergence-dependency.md` |
| TASK-0118 | Fix the seed gauge: one declared convention (full active-site array, incoherent statistical mixture — new `propagators.ctqw`/`time_averaged_ctqw` `coherent=False`), classified KNOB not GAUGE via a real 3-target sweep (KRAS_G12C AUC spread 0.326, [[INV-0006]]), threaded into `analysis`/`ceiling`/`protocol`/`run_challenge.py`/`ceiling_search_batched.py`. Re-ran floor/ceiling/actual for all 3 mandatory targets — **retracts "ceiling below floor" for KRAS_G12C** (ceiling now clears floor by +0.045); actual still below floor. CARDIAC_MYOSIN's actual now clears floor too, but confounded by an unrelated `LARGE_N_THRESHOLD` change and still gated on TASK-0124's 5TBY caveat. **Landed concurrently with TASK-0119 (not combined into one re-run — flagged as the next concrete gap in `COMPETENCE_MAP.md`)** | Implementer | Done | P0 | 2026-07-16 | — | — | `.ai/tasks/DONE/TASK-0118-seed-gauge-fix-and-headline-rerun.md` |
| TASK-0119 | Fix the clock: per-operator `t*` from spectral gap (`t*=-ln(tol)/gap`, TASK-0109's `min_adequate_t_max`) instead of shared `t_max=15` — re-ran TASK-0101's 96-cell sweep + TASK-0106's BCR_ABL1 reproduction. Found+fixed a real MemoryError (n_steps blew up to 3.1B on a near-degenerate gap). Two separate findings: most of CARDIAC_MYOSIN's ctqw floor-clears do not survive the clock fix (narrows TASK-0109's headline); `H_new`'s localization vs. `H10`/`H2` survives it. Also resolved TASK-0106's flagged PR-direction question — the review panel's own correction was mistaken (conflated `metrics.ipr` with `analysis._transport_participation_ratio`, opposite conventions) — `REVIEW-panel-2026-07-16-v2.md` §2.2, §5 P0-2 | Implementer | Done | P0 | 2026-07-16 | — | — | `.ai/tasks/DONE/TASK-0119-fix-clock-per-operator-timescale.md` |
| TASK-0120 | Learnability gate (HYP-P8): is the labeled pocket even in the apo topology? Kabsch-superpose + pocket-vs-background RMSD + Tama-Sanejouand cumulative overlap, all 3 mandatory targets. New `superpose.background_rmsd`/`learnability_verdict`. **Contradicts the panel's own expectation for KRAS_G12C**: pocket RMSD 2.27x background, but CO(20)=0.638 (well above the 0.5 low-overlap bar) → `LEARNABLE`, not cryptic-structural. BCR_ABL1's pocket moves *less* than background (ratio 0.49), consistent with its prior "structural prior" framing. CO blocked for BCR_ABL1/CARDIAC_MYOSIN by TASK-0128 — `REVIEW-panel-2026-07-16-v2.md` §6 | Implementer | Done | P0 | 2026-07-17 | — | — | `.ai/tasks/DONE/TASK-0120-learnability-gate-hyp-p8.md` |
| TASK-0121 | Renormalize `H_new`'s 5-term potential: z-score all 5 terms in `potentials.py` (fixes `V_R`'s 88.8%-of-variance normalization bug vs. `V_C`/`V_M`'s unreachable 0.1% each), re-derive `build_H_new`'s `lam_*` defaults so `sigma(V) <= 0.2*J` provably holds on any target (triangle inequality, `J<=2` universal). Real-data confirmation (KRAS_G12C, BCR_ABL1): variance share now 15.4%/61.5%/15.4%/3.8%/3.8% (38x gain for `V_C`/`V_M`); re-ran `analysis.ablation`, `most_impactful_term` flips `V_R`→`V_B` on both, `V_R` now least-impactful. Side-finding: also measurably weakens (not eliminates) the separate proximity confound (ρ_euclid 0.71-0.83→0.22-0.47) — `REVIEW-panel-2026-07-16-v2.md` §2.4, §5 P1-5 | Implementer | Done | P1 | 2026-07-16 | — | — | `.ai/tasks/DONE/TASK-0121-renormalize-potential-variance-budget.md` |
| TASK-0122 | Build `mode_coparticipation`/`CP_low` (REVIEW-2026-07-13b §6, sharpened by REVIEW-panel-2026-07-17.md P1-5 into a per-residue slow-mode-filtered form) — "the only route back to a defensible positive" per the panel. New `analysis.mode_coparticipation`; passed the mandatory dumbbell 2x2 gate cleanly (tracks coupling not the well, clean double dissociation vs GSR). **Real-target validation refutes the panel's own preliminary numbers, not in the hoped-for direction**: clean-Laplacian `|ρ(CP,-dist)|` is 0.24-0.62 on all 3 real targets (panel estimated 0.18); TASK-0121's renormalization made CP's distance-correlation *worse* not better on all 3 targets (0.20→0.31, 0.43→0.45, 0.24→0.36); k-sweep never reliably crosses a real, freshly-measured noise floor on any target. Real-label AUC clears the proximity floor on only 1/3 targets (BCR_ABL1, +0.176) — and that target's own CP score is the *most* distance-correlated of the three, weakening the coupling-detection reading. **Verdict: correctly implemented, correctly gated, but does not deliver on this project's real data — not a recommendation to adopt as a submission observable** | Implementer | Done | P1 | 2026-07-19 | Implementer B (this thread) | 2026-07-19 | `.ai/tasks/DONE/TASK-0122-mode-coparticipation-observable.md` |
| TASK-0123 | Distance-stratified evaluation (REVIEW-panel-2026-07-16-v2.md §2.3/§6): whole-graph AUC cannot distinguish "found the pocket" from "found distance." New `metrics.stratified_auc`/`stratified_auc_summary`, applied to all 16 register operators × 2 propagators × 3 mandatory targets (96 cells) + `mode_coparticipation`. **Naive reading overclaims**: 40 cells clear stratified AUC>0.65, but `stratified_auc_summary`'s own max-over-shells has the identical winner's-curse structure [[TASK-0131]] found for the ceiling search — checked directly via a real permutation null (median ~0.56-0.64, not 0.5). **Corrected picture (9 representative tests)**: 2/9 clear p<0.05 uncorrected (BCR_ABL1 `mode_coparticipation` shell-2 p=0.012; CARDIAC_MYOSIN `H_new`/ground_state shell-1 p=0.010), **neither survives Bonferroni correction**. Neither the panel's own "observable dead" prediction nor a clean reversal of this project's other floor findings — real, uncorrected suggestive signal in 2 cells, flagged for follow-up | Implementer | Done | P1 | 2026-07-19 | — | — | `.ai/tasks/DONE/TASK-0123-distance-stratified-evaluation.md` |
| TASK-0125 | c-Myc/1NKP hit-list resnum verification — does `keep_nucleic: true` leak DNA nucleotide indices into `results/tasks/0080/MYC_MAX/hit_list.json`'s top-5 consensus hits? `REVIEW-panel-2026-07-16-v2.md` §5 P2-11. **Verified — no bug.** Two independent checks (production `clean_from_config` pipeline trace + raw `prody.parsePDB('1NKP')` cross-check) on all 5 hits: every hit is a real amino acid (LEU/ALA/ARG) with correct native 1NKP chain/resnum, complete backbone atom set, zero DNA resnames. Secondary finding (not pursued, Out Of Scope): `keep_nucleic: true` is actually a no-op for MYC_MAX — `chains: ["A","B"]` excludes DNA chains F/G/H/J regardless, so the conflation risk this task checks for can't occur for this target's specific config. New reusable script `scripts/verify_cmyc_resnums.py` | Implementer | Done | P2 | 2026-07-18 | — | — | `.ai/tasks/DONE/TASK-0125-cmyc-1nkp-resnum-verification.md` |
| TASK-0126 | Run H13 (or its N×N projection) through the ceiling search, per `.claude/hypotheses/ceiling.md`'s "minimum set" (never run by TASK-0046). Literal Option A (scalar trace-of-3×3-blocks projection) proven degenerate -- collapses exactly to `H2_combinatorial_laplacian` (`np.allclose`, max diff 1.8e-15). Option B (3N×3N-native propagation) implemented as a pure propagator wrapper (`propagators.ctqw_3n_native`/`time_averaged_ctqw_3n_native`/`time_averaged_ctqw_converged_3n_native`, no core refactor needed), run to completion on all 3 mandatory targets under TASK-0130's closed-form convention (which landed mid-task and also removed an `O(n_steps)` cost that had made a full run infeasible under the older finite-`t_max` convention this task started with). **Mixed, target-dependent result, not a clean win or loss**: Option B does not beat `H_new`'s TASK-0130 ceiling on KRAS_G12C (0.5026 vs 0.6288) or BCR_ABL1 (0.6466 vs 0.6671), but does on CARDIAC_MYOSIN (0.8513 vs 0.8297, +0.0216). `H14_anm_pinv_trace` (existing Tier-A operator, not one of IMP-H7's two options) beats `H_new` on BCR_ABL1 only (0.7174 vs 0.6671). Real rigid-body-nullspace bug found in `propagators.min_adequate_t_max`'s gap formula for H13's 6 near-zero modes -- documented as a docstring caveat, not fixed in the shared function (out of scope). New `scripts/h13_ceiling_comparison.py` | Implementer | Done | P2 | 2026-07-19 | — | — | `.ai/tasks/DONE/TASK-0126-h13-ceiling-comparison.md` |
| TASK-0127 | Extend the ASD generalization set (2 -> 4 targets) and reframe it as the reported headline, not an appendix -- mitigates ~15 human review cycles over the same 3 mandatory-target answer keys. RCSB-verified 6 remaining draft candidates: CASPASE1 passes (same dimer-interface mechanism as CASPASE7); GLUCOKINASE passes after resolving `Q-0001` (apo/holo use different chain letters for the same chain -- added optional `apo_chains`/`holo_chains` override to `clean_from_config`, additive/backward-compatible); ATCase/HEMOGLOBIN/GLYCOGEN_PHOSPHORYLASE/PFK each fail for a different real, substantive reason (missing biological-assembly chains, no target ligand present, apo already ligand-occupied at the site of interest, apo/holo size mismatch) -- not backfilled. Re-ran PTP1B/CASPASE7 under the current fully gauge-fixed pipeline (their TASK-0081 numbers predated TASK-0118/0119/0121). All 4: `BEATS_CHANCE_NOT_FLOOR`, overlapping score/floor 95% CIs, `most_impactful_term` never `V_R` (independent cross-check of TASK-0121). Ceiling intentionally deferred for all 4 -- `ceiling.py::_PARAM_RANGES` is still pre-TASK-0121 scale ([[TASK-0116]]'s scope). `RESULTS.md` restructured: new reading-order note plus a full dated section make the generalization set the headline, mandatory-3 numbers explicit supporting detail | Implementer | Done | P2 | 2026-07-16 | — | — | `.ai/tasks/DONE/TASK-0127-extend-asd-generalization-set-as-headline.md` |
| TASK-0128 | `superpose.calibrate_kappa`/`anm_modes`'s exactly-6-near-zero-rigid-body-mode assertion (TASK-0005's own hardening) fails on BCR_ABL1 (n_zero=7) and CARDIAC_MYOSIN (n_zero=10), blocking any analysis needing calibrated `gamma_scale`/`kappa` on 2/3 mandatory targets — TASK-0005's own Done section already predicted this for a multi-chain/floppy-linker target; found running TASK-0099's real 3-target sweep. **Root cause diagnosed, not assumed**: both connected (`operator_diagnostics` n_components=1), extra eigenvalues at true machine-precision zero then a 5-9-order-of-magnitude gap to the real spectrum, eigenvectors localized to a small compact segment each — the known central-force-ANM under-constrained-substructure artifact, not disconnection or numerical noise. Widened `n_zero != 6` → `n_zero >= 6` + connectivity check (disconnected case still raises, re-verified). Re-ran TASK-0099's sweep: BCR_ABL1 now fully unblocked, matches KRAS's `COHERENCE_NOT_SIGNIFICANT` exactly (2/3 targets confirmed, not 1/3); CARDIAC_MYOSIN's calibration unblocked, full sweep still not-run for a separate pre-existing `haken_strobl` cost reason | Implementer | Done | P2 | 2026-07-18 | — | — | `.ai/tasks/DONE/TASK-0128-calibrate-kappa-exact-6-mode-assertion-blocks-multichain-targets.md` |
| TASK-0129 | Combined re-run: TASK-0118's seed convention (full-array incoherent mixture) + TASK-0119's per-operator clock (`t*`), together — neither prior task combined them. Extended `fix_clock_operator_sweep.py` in place; new `combined_competence_map_rerun.py`; added `coherent` to `analysis.operator_sweep`. **Headline: CARDIAC_MYOSIN's TASK-0118-only positive (+75.1% headroom) does not survive the clock fix** — under the combined convention actual AUC drops to 0.7912, landing 0.0009 *below* its own floor (cross-validated via 2 independent code paths). **No mandatory target's shipped actual result clears its own floor under the fully corrected convention**; real ceiling headroom exists for all three, reached by none. Flagged, not resolved: tension with TASK-0110's BCR_ABL1 short-`t_max` finding, and a `t*` reproducibility discrepancy vs TASK-0119's own recorded value. `COMPETENCE_MAP.md` fully recomputed (2nd SUPERSEDED layer) | Implementer | Done | P0 | 2026-07-18 | — | — | `.ai/tasks/DONE/TASK-0129-combined-seed-and-clock-rerun.md` |
| TASK-0130 | `time_averaged_ctqw`'s own AAKV convergence criterion is computationally infeasible (TASK-0110: 145,000x-3,950,000x the shipped `t_max=15`) — replaced with the exact infinite-time closed form, new `propagators.time_averaged_ctqw_converged` (promoted from a validation script's private helper), with a real degenerate-eigenspace correctness fix (grouped block-projector formula, proven basis-invariant where the naive index formula isn't — regression test constructs two different valid eigenbases of the same degenerate H by hand). New ADD-only `use_converged_limit` flag threaded through `analysis.{quantum_vs_classical,benchmark,operator_sweep}`/`ceiling.{consistency_score,ceiling_search}`/`protocol.run_frozen_verdict` (default False, byte-identical everywhere). Full recompute (new `closed_form_competence_map_rerun.py`/`closed_form_operator_sweep.py`), TASK-0112 CI wired in directly. **Headline: KRAS_G12C's point-estimate diagnosis flips NO_SIGNAL_IN_APO→NO_FAILURE_DETECTED (actual 0.5901 vs floor 0.4818, +73.7%) but the 95% CI still overlaps the floor's — not a decided win.** BCR_ABL1/CARDIAC_MYOSIN unchanged in kind. Secondary finding: BCR_ABL1's H_new-vs-H10/H2 trapping *contrast* (TASK-0106/0119) largely washes out at the true infinite-time limit (PR 0.0079 vs 0.0077-0.0079, all four operators). Fixed 2 stale KRAS Switch-II doc predictions; closed Q-0003; closes the clock-management need behind TASK-0108/0109/0110/0117/0119/0129 | Implementer | Done | P0 | 2026-07-18 | — | — | `.ai/tasks/DONE/TASK-0130-time-averaged-ctqw-convergence-infeasible.md` |
| TASK-0131 | Permutation null for the ceiling search — decides whether "ceiling clears floor" (COMPETENCE_MAP.md's only nonnegative claim) is real signal or the winner's-curse bias of taking a max over 60 noisy draws. Ran the actual current procedure (`ceiling.ceiling_search(use_converged_limit=True)`, TASK-0130's closed form — not the now-superseded finite-`t_max` script this task was filed against, an Implementer's-call decision stated in Done) against permuted pocket labels, 200 replicates/target, all 3 mandatory targets (36,000 ceiling-search trials total, parallelized 12-way). **Decisive, target-specific result**: CARDIAC_MYOSIN's headroom claim (carried since TASK-0082) is fully explained by the null (real margin at the 55th percentile of pure noise, p=0.45) — never real evidence. KRAS_G12C's margin is real, uncorrected-significant (p=0.045, 95.5th percentile) but does not survive Bonferroni correction for 3 targets (p=0.135). BCR_ABL1 inconclusive (p=0.10). New `scripts/ceiling_permutation_null.py`. Corrected `REVIEW-2026-07-15b`'s "lucky trial" text additively; found and fixed a real gap — TASK-0129's own "Cross-target reading" section had never been updated when TASK-0130 landed and still asserted "real headroom exists... for all three targets" | Implementer | Done | P0 | 2026-07-19 | — | — | `.ai/tasks/DONE/TASK-0131-ceiling-permutation-null.md` |
| TASK-0135 | Reproducibility audit (does a fixed seed on fixed inputs give the same output across repeated invocations in this environment) + shared run-logging convention -- raised directly by the orchestrating user, explicitly as an untested hypothesis, upstream of trusting any "improvement"/"new best trial" currently in flight. **Refuted, not confirmed**: 3 independent fresh repeats each of a spectral-gap eigendecomposition (BCR_ABL1), `ceiling.ceiling_search` (blind random, KRAS_G12C seed=7), and `optuna_scan.apo_floor_scan` (TPE, seed=7) all reproduced bit-for-bit. BLAS thread-count (`1/2/4/8`, tested directly) produces real but negligible drift (~1e-14 relative). **Directly refutes `REVIEW-panel-2026-07-17.md`'s "from BLAS reduction order" attribution** for the original BCR_ABL1 gap discrepancy (0.0374 vs 0.1933) -- that claim was never itself a controlled test; real cause traces to a stale cached `H_new` object from [[TASK-0102]] (2026-07-13), 5 days before [[TASK-0121]] changed `build_H_new`'s defaults. New `allostery.runlog` module (environment fingerprint + incremental wall/CPU-time JSONL logging, addressing P-0005/[[TASK-0134]]'s wall-clock-alone finding too), adopted in new `scripts/reproducibility_audit.py`. `INV-0008` closed (GAUGE holds); `COMPETENCE_MAP.md`'s "BLAS reduction order" citations corrected additively | Implementer | Done | P0 | 2026-07-18 | — | — | `.ai/tasks/DONE/TASK-0135-reproducibility-audit-and-run-logging.md` |
| TASK-0133 | Learnability gate control: does a random same-sized patch also score high on cumulative overlap (`REVIEW-panel-2026-07-17.md` §5.2)? **Found first: TASK-0120's reported `CO(20)` is a whole-structure quantity, not pocket-specific** (`len(delta_r) == 3*len(alignment.apo_idx)`, not `3*n_pocket`). Built a genuinely pocket-restricted CO (new `superpose.restricted_cumulative_overlap`), found+fixed a real bug in the obvious implementation first: naively slicing+renormalizing eigenvectors for a small subset breaks `CO(m)<=1` (up to 1.64 observed) -- fixed via zero-padding into the full 3N ANM space and projecting onto untouched orthonormal eigenvectors. Real run, 1000 random patches/target: **only matters for KRAS_G12C** (the one target whose verdict depends on the CO half). Restricted CO=0.458, below the 0.5 threshold (vs 0.638 whole-structure) -- would flip the verdict to `UNLEARNABLE_FROM_APO` if used, but sits at only the 93rd percentile of the random-patch null (p~=0.07, not decisive per [[TASK-0131]]'s own precedent). BCR_ABL1/CARDIAC_MYOSIN score below their own random-patch medians (37th/13th percentile) but their verdicts are RMSD-determined regardless. `learnability_verdict`'s threshold not changed, per scope -- flagged for whoever revisits KRAS_G12C's classification | Implementer | Done | P1 | 2026-07-17 | — | — | `.ai/tasks/DONE/TASK-0133-learnability-gate-random-patch-control.md` |
| TASK-0138 | Characterize `H14_anm_pinv_trace` at ceiling with the same rigor `H_new` has been through -- [[TASK-0126]] found H14 beating `H_new`'s ceiling on BCR_ABL1, needs the permutation-null/trial-density scrutiny `H_new`'s own numbers already had. **Premise correction found on claim**: this task's own Context cited an in-session draft of TASK-0126 (H14 beating `H_new` on all 3 targets) that was superseded before TASK-0126 committed -- actual committed finding is BCR_ABL1 only. New `scripts/h14_ceiling_permutation_null.py` (TASK-0131-compatible null, adapted to H14's 1-D cutoff-only search) + `scripts/h14_trial_density_check.py`. **Headline: H14's own ceiling-clears-floor claim does not survive scrutiny on any target.** KRAS_G12C margin (0.0914) at the 70th null percentile (p=0.300); BCR_ABL1's (0.1357) at the 79th (p=0.210) -- neither significant even uncorrected, 200 replicates each. CARDIAC_MYOSIN's margin is negative, confirmed (30-replicate reduced null) at the 0th percentile. Trial-density check: KRAS_G12C's 60-trial convention has not converged (running max 0.5732->0.5949 by 150 trials); BCR_ABL1's has (0.7174->0.7213) -- its margin over `H_new` is not a sampling artifact, but still doesn't survive the null. Real bug found+fixed: an initial 12-worker run hit severe memory-bandwidth contention on CARDIAC_MYOSIN (~5.8x slowdown, ~9.7hr projected) and the script only wrote output once at the end (would have discarded completed KRAS/BCR_ABL1 results too) -- fixed to checkpoint after every target; re-ran CARDIAC_MYOSIN at a throughput-tuned `workers=4` | Implementer | Done | P1 | 2026-07-19 | — | — | `.ai/tasks/DONE/TASK-0138-h14-ceiling-validity-characterization.md` |
| TASK-0139 | Resolve KRAS_G12C's contradictory learnability verdicts -- [[TASK-0120]]'s whole-structure CO said `LEARNABLE`, [[TASK-0133]]'s pocket-restricted CO disagreed but left it "reported, not decided" (p≈0.07). **Decided both open questions from first principles**: (1) pocket-restricted CO is correct (RMSD half is already region-specific; whole-structure CO answers a different, easier question than the panel's own kill criterion); (2) `co_threshold=0.5` was never validated against any null (confirmed by re-reading its only other consumer, `cumulative_overlap_gate`) -- replaced by a direct significance test against TASK-0133's own random-patch null where available. New `superpose.learnability_verdict(co_percentile=..., significance_alpha=0.05)` (ADD-only, real third verdict `AMBIGUOUS`). Applied programmatically to the real, already-computed TASK-0120/0133 numbers (new `scripts/resolve_kras_learnability.py`, no new expensive computation): **KRAS_G12C resolves to `AMBIGUOUS`** (RMSD ratio 2.27 clears; CO's 93rd-percentile elevation over the null doesn't clear α=0.05 either direction). BCR_ABL1/CARDIAC_MYOSIN confirmed unchanged (RMSD-determined). Propagated additively into `RESULTS.md` (2 sections + 2 open-questions rows), `EXECUTION_PLAN.md` (new row + TASK-0120's row updated), `.claude/hypotheses/physics.md`'s HYP-P8 entry | Implementer | Done | P0 | 2026-07-20 | — | — | `.ai/tasks/DONE/TASK-0139-kras-learnability-reclassification-decision.md` |
| TASK-0143 | Coordinated-closure graph-openness premise (HYP-P10) -- renumbered from the batch's own `TASK-0139`, which collided with the unrelated already-committed TASK-0139 above. Tests whether real holo-defined cryptic pockets are Euclidean-near but apo-graph-far (the premise gate for the whole loop/multi-site-closure family, HYP-P9/P12). New `allostery.closure` (`graph_dist_matrix`/`openness_signature`/`matched_spread_null`), synthetic positive-control gate confirmed ~100th percentile before touching real data (distinct from `cumulative_overlap`/[[TASK-0059]]/[[TASK-0120]] by construction -- `closure.py` never imports `superpose`). **Headline: 0/7 targets PASS the pre-registered gate** (>95th percentile AND Bonferroni across 7). KRAS_G12C (98.4th pctile, p=0.016 uncorrected/0.112 Bonferroni) and CASPASE1 (90.2nd pctile) are INSUFFICIENT; CASPASE7 is a clean FAIL (26.8th pctile, opposite direction from the claim). **BCR_ABL1/CARDIAC_MYOSIN/PTP1B/GLUCOKINASE could not be tested at all** -- the matched-spread null is infeasible via unbiased rejection sampling at +/-35% tolerance even at 20M attempts (0-19/500 replicates), since a compact real pocket's spread is intrinsically rare among uniform random same-size draws over a large protein -- a real finding, not fixed here (redesigning the null's sampling scheme is Out of Scope after seeing the outcome). Two real bugs found+fixed: non-reproducible seed (`hash()` is per-process-salted for strings, fixed to `zlib.crc32`) and output written only once at the end (fixed to checkpoint per-target, same lesson [[TASK-0138]] independently learned). Gates [[TASK-0140]]/[[TASK-0142]] as unsupported, not blocked outright -- `EXECUTION_PLAN.md`'s 1D.2/1D.4 rows updated, `physics.md`'s HYP-P9/P10/P12 cross-references updated | Implementer | Done | P0 | 2026-07-20 | — | — | `.ai/tasks/DONE/TASK-0143-openness-premise-real-targets.md` |
| TASK-0136 | Path-ensemble + percolation connectivity between the active site and known allosteric pocket -- raised directly by the orchestrating user, 2026-07-18. Purely topological (no propagator, no clock, no seed coherence), deliberately outside the P0 gauge-fixing batch's confound axes. New `allostery.percolation` (shortest-path ensemble via Dijkstra+Yen's algorithm, Menger's-theorem edge connectivity + literal min-cut, Union-Find percolation-threshold sweep) + `baselines.connectivity_robustness` (part c, new blind scoreable baseline). **Headline: all 3 mandatory targets show broad, redundant connectivity (68-76 edge-disjoint routes) between active site and pocket -- no narrow bottleneck anywhere.** Real bug found+fixed before trusting any number: the virtual super-source/sink construction used unit-capacity edges, silently capping every reported connectivity at `min(|active|,|pocket|)` (confirmed via a synthetic single-hub-3-fan-out check that true connectivity isn't bounded by set cardinality) -- fixed via large-capacity virtual edges + `nx.maximum_flow_value`/`minimum_cut` (`nx.edge_connectivity`/`minimum_edge_cut` don't accept a `capacity` argument at all, checked directly, not assumed). Pre-fix numbers (13-18, empty cuts) were wrong; post-fix (68-76, real multi-edge cuts) are correct. KRAS_G12C's percolation threshold merges after just 2 edges (3.75A) -- an independent topological confirmation of this project's own established active-site/pocket proximity finding. Part (c) scored: beats floor on 1/3 targets (KRAS_G12C only); TASK-0123's `stratified_auc` cross-read actually run (not cited by analogy) -- confirms most of the raw AUC is the same distance/degree confound found pervasive elsewhere in this project's register. Dumbbell gate: exactly 0.500 in every cell -- a clean, understood negative (unweighted route-counting can't see the dumbbell's edge-weight-only confound), not ambiguous | Implementer | Done | P1 | 2026-07-22 | Implementer A (this thread) | 2026-07-22 | `.ai/tasks/DONE/TASK-0136-percolation-connectivity-robustness.md` |
| TASK-0144 | `superpose.align_apo_holo`/`common_residues_by_resnum` matched apo/holo Ca atoms by raw `(chain, resnum)`, silently empty when apo/holo deposit the same biological chain under different author chain letters ([[TASK-0127]]'s `apo_chains`/`holo_chains` override, e.g. GLUCOKINASE apo chain A/holo chain X) -- found live while executing [[TASK-0124]]'s CARDIAC_MYOSIN apo replacement (new 8QYP apo chain A / 8QYR holo chain B pair: 0 common residues, `align_apo_holo` raised). New optional `chain_map` param (default `None`, byte-identical prior behavior) + `chain_map_from_config`, threaded through all 5 real call sites found by re-grepping the repo at pickup time (`run_superpose`, `learnability_gate.py`, `learnability_gate_patch_control.py`, `holo_diagnostic_comparison.py`, `kras_auc_reconciliation.py`) -- one more real caller than the 4 found at filing. Re-ran `scripts/learnability_gate.py`: CARDIAC_MYOSIN (8QYP/8QYR) now resolves 698/704 residues (vs. the retired 5TBY pair's 709/950), `LEARNABLE`; GLUCOKINASE run for the first time ever, resolves 446/448, the project's first target where `learnability_verdict`'s AND-conjunction genuinely fires both halves, `UNLEARNABLE_FROM_APO`. TASK-0120/0133's own CARDIAC_MYOSIN rows flagged superseded (by TASK-0124's apo swap, not retroactively by this bug -- 5TBY happened to share holo's chain letter) in `RESULTS.md`, not deleted | Implementer | Done | P2 | 2026-07-20 | — | — | `.ai/tasks/DONE/TASK-0144-align-apo-holo-chain-letter-mismatch.md` |
| TASK-0140 | Chiral (broken-time-reversal) circulation observable (HYP-P9) -- Peierls-substituted complex-hopping Hamiltonian -> infinite-time-averaged bond current (projector-dephasing generalization of [[TASK-0130]]'s closed form to this off-diagonal quantity) -> Helmholtz-Hodge split, scoring only the proximity-orthogonal circulating part. Reference script confirmed absent (grepped directly, per the Architect/Planner's own flag) -- reconstructed from HYP-P9's own description + cited literature (Zimborás 2013, Lu 2016). New `allostery.chiral`, 35 tests. **Real physics finding while building the test suite (not anticipated)**: a bare N-cycle ring gives exactly zero converged circulation from a diagonal seed, at any flux -- confirmed directly across several constructions including a uniform-weight triangle; real protein contact graphs aren't at risk (dense 3-D packing, degree >>2 everywhere) but it ruled out the obvious minimal synthetic test fixture. GATE 1 (coupling-vs-well dissociation, new loop-dumbbell fixture since TASK-0103's own dumbbell is a cycle-free tree and can't carry Peierls flux) and GATE 2 (beats floor on a synthetic distal loop pocket) both re-verified passing on this fresh reconstruction, own numbers (not the lost script's cited 0.33/0.6-0.97). **Real run, all 3 mandatory + 4 ASD targets: FAIL** against the pre-registered bar (floor-beating AUC AND non-overlapping 95% block-bootstrap CIs) -- CI overlap `True` on every target, consistent with [[TASK-0143]]'s own FAIL on the graph-openness premise this observable depends on. KRAS_G12C/PTP1B clear the Bonferroni-corrected (0.05/7) stratified-AUC permutation null alone (suggestive, not a PASS). Proximity-orthogonality itself confirmed cleanly on 7/7 targets (ρ(circ,-dist) consistently smaller in magnitude than ρ(occ,-dist)). [[TASK-0142]]'s own hard gate remains unmet -- `EXECUTION_PLAN.md`'s 1D.2/1D.4 rows, `physics.md`'s HYP-P9 entry updated | Implementer | Done | P1 | 2026-07-20 | — | — | `.ai/tasks/DONE/TASK-0140-chiral-circulation-observable.md` |
| TASK-0142 | Hodge-L1 + persistent H2 observable (HYP-P12) -- H2 half only, ungated 2026-07-22 by the Architect/Planner (a capped void's lining residues are graph-*adjacent*, the opposite of [[TASK-0143]]'s open-cleft graph-*far* premise, demonstrated directly by the delivered `test_void_detected_even_when_lining_is_graph_adjacent`). Ported the delivered `persistent_voids.py`/`test_persistent_voids.py`/`persistent_voids_synthetic_control.py` (`.ai/reviews/2026-07-22/`) into `src/allostery/`, `tests/`, `scripts/` unmodified; re-verified independently (4 tests pass, synthetic-control script reproduces its own cited numbers exactly). `ripser` added to `pyproject.toml`. Two filing-text imprecisions resolved by direct test before assuming: `thresh=16.0` Rips filtration cap (not "8 A", tested directly -- 8.0 truncates real H2 classes mid-birth on KRAS_G12C, diagram stabilizes at >=12.0); TASK-0133's own *plain* random-patch null (not TASK-0143's spread-matched, infeasible-on-4/7-targets convention) as the actual cited precedent. **Real run, all 3 mandatory targets: clean FAIL, one case sharply informative.** 2/3 targets (KRAS_G12C, BCR_ABL1) show no H2 void at all -- top persistence at/below the synthetic solid-ball noise floor (2.5); honest gated score is all-zeros/chance, nominal ungated floor-beating explained away by the matched-null (77.1st/46.3rd percentile, p=0.229/0.537). CARDIAC_MYOSIN detects a void (2.829) but the **wrong** one -- AUC 0.192, null percentile 0.0 (real pocket's mean score below all 1000 random patches); a large protein has multiple cavities, the most-persistent one isn't the ligand pocket. Residue-localization weak link confirmed on real data (proximity floor 0.583 beats void_score 0.192 on CARDIAC_MYOSIN, same pattern as the synthetic control's 0.824 vs 0.576). **L1 half not run -- its own gate (TASK-0143 OR TASK-0140 showing life) remains closed, both are FAIL** | Implementer | Done | P1 (H2)/P2 (L1, not run) | 2026-07-20 | — | — | `.ai/tasks/DONE/TASK-0142-hodge-l1-persistent-h2.md` |
| TASK-0149 | Low-mode PRS/DCC predictor (`prs_low`/`dcc_low`, proximity-orthogonal-by-construction ANM-pseudo-inverse/GNM-covariance observables restricted to the `k` lowest modes) -- ported unmodified from `.ai/reviews/2026-07-22/` (delivered pre-tested on synthetic data only, RCSB unreachable from the delivering thread's own sandbox, honestly disclosed rather than fabricated). Re-ran on real targets under [[TASK-0118]]'s current seed convention (full active-site array), gated through [[TASK-0123]]'s own stratified-AUC + permutation-null machinery, swept `k_modes` in {5,10,15,20}. **KRAS_G12C/BCR_ABL1: genuinely dead** (p=0.67-0.99 at every k). **CARDIAC_MYOSIN: real, Bonferroni-surviving signal** (stratified well-powered max AUC 0.92/`prs_low` and 0.96/`dcc_low` at k=20, p=0.006/p<0.001, survives the primary 6-comparison Bonferroni bar; only 2/8 cells survive the maximally conservative 24-comparison full-grid bar, both readings reported). **Real divergence from the synthetic control's own prediction, not smoothed over**: rho(score,-hop) stays substantial on every real target (`prs_low` -0.18 to -0.51 anti-proximity; `dcc_low` +0.44 to +0.63, reproducing the classic proximity-confound direction) -- far from the synthetic control's ~0.08-0.16 "cleanly decorrelated" result -- so CARDIAC_MYOSIN's whole-graph AUC alone is not trustworthy proof of signal; the stratified/permutation-null result (2 independent well-powered shells, both observables) is the actual finding. A real sign bug found+fixed in this task's own new evaluation script (not in the ported predictor code) before trusting any rho value. Cross-referenced, not conflated, with [[TASK-0122]]'s `CP_low` (mostly failed to decorrelate; `prs_low` decorrelates further, `dcc_low` not at all -- a genuinely different method) | Implementer | Done | P1 | 2026-07-22 | — | — | `.ai/tasks/DONE/TASK-0149-lowmode-prs-dcc-predictor.md` |
| TASK-0151 | Generalization-set check for [[TASK-0145]]'s BCR_ABL1 transport positive and [[TASK-0149]]'s CARDIAC_MYOSIN `dcc_low`/`prs_low` positives -- the project's two strongest results as of 2026-07-24 -- against PTP1B/CASPASE7, the first real application of [[TASK-0115]]'s Rule #6 (repeated-exposure risk). Pure re-application: new `scripts/generalization_check_transport_lowmode.py` imports and calls `transport_observable_real_run.run_one`/`lowmode_predictor_real_run.run_target` directly, unmodified. Each observable kept its own already-established evaluation lens (a tested divergence from this task's own filing text, which described both as using TASK-0123's stratified-AUC lens -- only `lowmode_predictor_real_run.py` actually does; transport uses whole-graph AUC + CI + permutation null). Two new Bonferroni families scoped to this task's own comparisons (transport 2x3=6, alpha=0.00833; lowmode 2x2x4=16, alpha=0.00313), not folded into either mandatory-3 task's own alpha. **Headline: mixed, real, informative.** Transport does not clearly generalize -- PTP1B's `T(E=0)` on L comes back below chance (AUC 0.382, p=0.926, opposite direction from BCR_ABL1's own p=0.003); CASPASE7 echoes the direction (AUC 0.748) but only at uncorrected p=0.011. **`dcc_low` replicates cleanly on PTP1B** -- 3/4 k-values clear this task's own stricter Bonferroni bar, including well-powered max AUC 1.000 at k=10 (p=0.001) -- now Bonferroni-significant on two independent targets, the strongest cross-target evidence any observable in this project's register has. `prs_low` never reaches significance on either generalization target. Neither mandatory-3 finding retracted (this task's own Constraint) -- changes framing, not standing | Implementer | Done | P1 | 2026-07-24 | — | — | `.ai/tasks/DONE/TASK-0151-generalization-check-transport-lowmode-findings.md` |
| TASK-0153 | Extends [[TASK-0067]]'s/[[TASK-0092]]'s holo-native diagnostic (same operator, holo topology instead of apo -- failure-attribution only, never a prediction) to [[TASK-0145]]'s transport and [[TASK-0149]]'s low-mode families, neither ever tested this way. New `scripts/holo_diagnostic_transport_lowmode.py` -- confirmed directly (not assumed) that neither observable goes through `run_frozen_verdict`'s blind-selection machinery, so this task's own simpler direct-application design (holo coords + `_holo_native_labels`, TASK-0067's own construction, + each observable's own already-established `_score`/`_score_cell` reused unmodified) is correct, not TASK-0092's own two-pass shape. **Headline: mixed, informative, includes a recurring third outcome.** BCR_ABL1's transport headline gets a clean operator-limited reading (holo=0.626 vs apo=0.698, gap -0.073, holo itself still nominally elevated p=0.042) -- corroborates TASK-0092's own BCR_ABL1 pattern on a third operator family. CARDIAC_MYOSIN's `prs_low` headline gets the cleanest near-information-ceiling reading in the project so far (holo stays strongly significant at every k, p=0.0000-0.0051, gap only -0.020). **`dcc_low`'s headline on the identical target does not** -- holo loses significance at every k (p=0.07-0.30, gap -0.303 at k=20), flagged as an unresolved complication, not smoothed over. KRAS_G12C's transport-on-H_new and BCR_ABL1's `prs_low` both reproduce TASK-0092's own "holo scores worse than apo" third outcome, confirming it recurs. Diagnostic only throughout, per this task's own Constraint | Implementer | Done | P1 | 2026-07-24 | — | — | `.ai/tasks/DONE/TASK-0153-holo-diagnostic-transport-lowmode.md` |
| TASK-0146 | Frequency-domain / spectral coherence observable: un-averaged coherent amplitude `c_j(t)=<j|exp(-iHt)|source>`, Fourier-analyzed instead of time-averaged away (every other propagator this project scores discards phase information; `time_averaged_ctqw_converged` is provably phase-free at the converged limit). New `allostery.spectral_coherence` -- score = total AC (non-DC) spectral power of `p_j(t)=|c_j(t)|^2` (the DC bin is exactly the already-scored converged quantity; equals `Var_t[p_j(t)]` exactly by Parseval, confirmed as a numerical identity). Real-data-calibrated `T=5000` (not arbitrary) after finding the task's own suggested "resolve the smallest Bohr gap" criterion is, literally, [[TASK-0110]]'s own already-documented infeasibility -- resolves 55.8-94.6% of real gaps across the 3 targets. Sensitivity check confirms the shipped `t_max=15` default is materially inadequate (flips KRAS_G12C's own floor-clearing verdict vs. the calibrated default, which itself agrees closely with a 10x-larger window). Dumbbell falsification gate: clean double dissociation against GSR (C2=1.000/C3=0.000 exact). **Real-target verdict: no decisive result, with a confirmed mechanism** -- all 3 targets' CIs overlap the floor, no well-powered stratified max clears the 3-target Bonferroni bar, and rho(score,-hop)=+0.68 to +0.72 on every target (matching a confounded observable's own reference magnitude) -- this task's own filed Priority note ("no structural argument for proximity-orthogonality") confirmed empirically, not merely repeated | Implementer | Done | P2 | 2026-07-20 | Implementer B (this thread) | 2026-07-24 | `.ai/tasks/DONE/TASK-0146-frequency-domain-coherence-observable.md` |
| TASK-0147 | Structured (mode-resolved) vibronic bath -- specific ANM normal modes resonantly coupled to specific site-energy gaps, vs. `haken_strobl`'s existing flat-rate dephasing. `PANEL_REVIEW_2026-07-25.md` explicitly deprioritizes this task: "describe in the forward-proposal section, do not spend implementation time before 2026-09-15" -- same treatment TASK-0157 received. **Scope corrected to match**: wrote and literature-grounded the required mode-resolved coupling model (this task's own first In-Scope bullet) without building the structured-bath evolution. 2 citations independently re-verified (Christensson/Kauffmann/Pullerits/Mancal 2012, J. Phys. Chem. B 116:7449, vibronic-exciton coherence longevity; Patra & Tiwari 2022, J. Chem. Phys. 156:184115, more directly on-point for *selective* site-specific enhancement). Standard Holstein-type site-diagonal coupling mapped onto this project's own quantities (site energies = `H_new` diagonal, bath modes = `superpose.anm_modes` reused, coupling strength proportional to mode eigenvector displacement per residue, resonance `omega_k~\|H_new_ii-H_new_jj\|`). Honest ceiling stated: a proposed, literature-grounded mapping, not a validated model -- the full Redfield/structured-bath evolution, synthetic falsifier, and real-target scoring are the deferred, highest-implementation-cost work. Filed as a forward-proposal note in `EXECUTION_PLAN.md`'s Phase 1E + `RESULTS.md` section + open-questions row 60 | Implementer | Done | P2 | 2026-07-20 | Implementer C (this thread) | 2026-08-03 | `.ai/tasks/DONE/TASK-0147-vibronic-resonance-structured-bath.md` |
| TASK-0154 | `stage` refuses outright unless `GIT-COMMIT` is currently claimed (existence check) -- real gap found verifying the commit-lock/staging tooling: nothing previously stopped `stage` from running lock-less. Scoped to `stage` only, not `move`/`resolve` (per explicit user choice) | Toolsmith | Done | P1 | 2026-07-24 | — | — | `.ai/tasks/DONE/TASK-0154-gate-stage-on-git-commit-held.md` |
| TASK-0159 | `PANEL_REVIEW_2026-07-25.md` W2 -- re-point `run_challenge.py` at the converged closed-form propagator (`time_averaged_ctqw_converged`, [[TASK-0130]]), not the hardcoded `T_MAX=15.0`/`N_STEPS=500` finite-time `time_averaged_ctqw` call the shipped pipeline still used. Wired `run_frozen_verdict(use_converged_limit=True)` (existing TASK-0130 machinery, never previously called with it) + swapped the winner's own occupation directly; old constants deleted, a renamed/scoped-down pair kept only for 3 genuinely different, still-finite-by-design uses (candidate selection, GSR snapshot, `ablation()`'s per-term diagnostic). Real AAKV `t_max*` checked on all 5 targets touched: 83,834x-420,682x the shipped default. **Per explicit user request, directly validated the closed form itself**: brute-force `time_averaged_ctqw` integration run to true AAKV convergence (877K-4.6M steps, up to 12.3 wall-hours for CARDIAC_MYOSIN) agrees with the closed form to 1e-6/1e-7 on every target -- floating-point noise, not an approximation gap. First attempt lost all progress when a harness-tracked background job was killed by session teardown (0/5, confirming `LONG_JOB_CONVENTION.md`'s own warning); re-run OS-detached and sequentially (uncoordinated 5-way parallelism caused a real 5-26x slowdown on the first attempt). **Real finding**: PTP1B's converged AUC (0.4859) disagrees with the ASD generalization set's own row (0.2050) -- confirmed to be a finite-time-vs-converged discrepancy (old table pre-dates/is same-day as TASK-0130), verdict flips `BEATS_CHANCE_NOT_FLOOR`->`NO_SIGNAL_IN_APO`, flagged additively in that table's own section, not silently absorbed. Full suite: 950 passed, 0 failed | Implementer | Done | P0 | 2026-07-25 | — | — | `.ai/tasks/DONE/TASK-0159-run-challenge-converged-propagator.md` |
| TASK-0160 | `PANEL_REVIEW_2026-07-25.md` W3 -- replace `run_challenge.py`'s `connectivity_matrix.npz` deliverable (currently a classical, active-site-seeded, sparse current-flow solve) with a genuine dense N×N quantum-defined `P_∞(i,j)` connectivity matrix -- the challenge's own §5 spec requires "quantum connectivity strength between residue i and residue j," all-pairs and dense, which the current deliverable fails on all three counts. New `propagators.quantum_connectivity_matrix` (`P_inf(i,j)=sum_k|v_k(i)|^2|v_k(j)|^2`, one `V2 @ V2.T` product, reuses the same `eigh()` call `time_averaged_ctqw_converged` already needs -- no second diagonalization). Exact symmetry + row-sum-to-1 derived and confirmed numerically; real-data check on all 3 mandatory targets: new matrix density 1.000 vs. old matrix's 0.014-0.057 (falling as N grows) -- decisive confirmation of the dense-vs-sparse fix. **Design choice checked directly**: the plain per-eigenvector formula (not TASK-0130's degenerate-block-corrected form) matches `time_averaged_ctqw_converged`'s own single-source column to 1e-16/1e-17 (machine precision) on every mandatory target -- real `H_new` spectra are near- not exactly-degenerate, confirmed not assumed. Wired additively into both `run_target` and `run_target_no_ground_truth` as `quantum_connectivity_matrix.npz`; pre-existing classical file untouched. 8 new unit tests + 1 real-pipeline test. Full suite: 959 passed, 0 failed | Implementer | Done | P0 | 2026-07-25 | Implementer B (this thread) | 2026-07-25 10:21 | `.ai/tasks/DONE/TASK-0160-genuine-quantum-connectivity-matrix.md` |
| TASK-0162 | Reverse-direction coupling test -- seed at the pocket, score coupling into the active site (`PANEL_REVIEW_2026-07-25.md` §2.4/V5). Every observable in the register so far seeds at the active site; the forward/reverse directions are not symmetric under a non-normal operator or a multi-residue incoherent-mixture seed. Re-applied 5 existing observables (`time_averaged_ctqw_converged`, `prs_low`/`dcc_low` k=20, `R_eff`, `T(E=0)` on `H_new`) unmodified, only seed/label roles swapped, across 5 targets (3 mandatory + PTP1B/CASPASE7, Open Question resolved in favor of running both -- compute cost negligible). **Real, decisive asymmetry**: forward clears its own TASK-0094 proximity floor in 10/25 cells (40%), reverse in only 4/25 (16%), both in 2/25 (8%). Starkest single cell: CARDIAC_MYOSIN `prs_low` forward AUC 0.836 collapses to reverse 0.321 (anti-correlated) -- this project's own strongest whole-graph result is strongly one-directional. Background Spearman rho (residues outside both label sets) mostly strongly positive (mean 0.61) -- asymmetry concentrates in the labeled sets, not the whole graph. Forward numbers reproduce every already-published value exactly (implicit consistency check); no prior verdict changed. Full suite: 971 passed, 0 failed | Implementer | Done | P1 | 2026-07-25 | — | — | `.ai/tasks/DONE/TASK-0162-reverse-direction-coupling-test.md` |
| TASK-0163 | Run external classical baselines (PocketMiner, ProteinLens, fpocket) -- `PANEL_REVIEW_2026-07-25.md` W7/V8. Access confirmed first: fpocket built from source (new permanent repo-local dependency, `tools/fpocket/`); PocketMiner's web server DNS-dead, run instead in a separate isolated repo (`/home/bchmura/PROJECTS/PocketMiner/`, own from-source Python 3.9 build -- this repo's Python 3.12 is incompatible with the pinned `tensorflow==2.6.2`); ProteinLens live/no-login but browser-only, no API -- explicit blocker, not run. fpocket+PocketMiner scored against all 3 mandatory targets' holo pocket labels, same AUC/floor convention as every other observable. **Headline: fpocket decisively beats both this project's own proximity floor and its own `H_new`/CTQW headline observable on 2/3 targets** (KRAS_G12C 0.8348, BCR_ABL1 0.8596 vs. floor 0.4818/0.5817, actual 0.5901/0.5266); just below its own floor on CARDIAC_MYOSIN (0.5345 vs 0.5679). PocketMiner mixed (beats actual on KRAS_G12C/CARDIAC_MYOSIN, narrowly clears floor on CARDIAC_MYOSIN, falls short of floor on BCR_ABL1). Real alignment bug found+fixed: CARDIAC_MYOSIN's 8QYP carries 2 `HETATM`-record trimethyllysine (M3L) residues an `ATOM`-only resSeq parser missed, undercounting PocketMiner's residue axis by 2 -- fixed by parsing `HETATM` CA records too | Implementer | Done | P1 | 2026-07-25 | — | — | `.ai/tasks/DONE/TASK-0163-external-classical-baselines.md` |
| TASK-0161 | Program-level multiple-comparison budget -- enumerate every real-target scored cell across the whole register (not the review's own one-line estimate) and check whether reported positives exceed what that testing volume alone would produce at α=0.05. 226 real-target scored cells confirmed directly, ~11.3 expected false positives. Using [[TASK-0158]]'s corrected-null re-run: zero confirmed, corrected-null-surviving positives program-wide -- fewer positives than pure chance predicts. One cell ([[TASK-0145]]'s BCR_ABL1 transport) flagged genuinely unresolved (scattered null, not re-run), not counted as a survivor | Implementer | Done | P0 | 2026-07-25 | — | — | `.ai/tasks/DONE/TASK-0161-program-level-multiplicity-budget.md` |
| TASK-0164 | Frozen held-out generalization check -- re-verify the 7-unresolved-target premise directly against `targets.yaml` (found to be 6, not 7 -- LDH lives under a separate already-closed `omitted_targets:` key) and RCSB-verify each of the 6 `status: draft` ASD candidates independently. 5/6 already permanently blocked per [[TASK-0127]]; the 6th (GROEL_SUBUNIT), never independently checked before, confirmed to have two of its own permanent blockers (apo deposits only 7/14 biological chains; ligand GroES is a 7-chain protein complex, not a small molecule). Zero of 6 qualify -- escalates sourcing brand-new ASD candidates to the Architect/Planner role rather than starting it unilaterally | Implementer | Done | P1 | 2026-07-25 | — | — | `.ai/tasks/DONE/TASK-0164-frozen-held-out-generalization-check.md` |
| TASK-0166 | Ensemble/entropic observable, challenge ref [4] (Motlagh & Hilser 2014) -- per-residue GNM low-mode conformational entropy (Bahar/Atilgan/Erman 1997 MSF formula + Cover & Thomas differential-entropy identity), the first test in this program of ensemble *redistribution* rather than signal *transmission*, no active-site seed or propagation step at all. Scored against all 7 pocket-scoreable `status: verified` targets (MYC_MAX excluded, no pocket label -- IDP with `allosteric_pocket_exists: false`): zero of 7 survive Bonferroni correction (GLUCOKINASE p=0.035 closest near-miss). **Second finding**: despite having no seed, ρ(entropy,-hop) is strongly positive on every target (0.39-0.76) -- not proximity-orthogonal, unlike [[TASK-0140]]/[[TASK-0149]]'s observables built specifically to weaken that confound. Entropy is a strictly monotonic transform of the underlying low-mode variance, so this is, in ranking terms, a per-residue-flexibility-magnitude test -- it fails on this data | Implementer | Done | P1 | 2026-07-25 | — | — | `.ai/tasks/DONE/TASK-0166-ensemble-entropy-observable.md` |
| TASK-0155 | Apo-structure sensitivity sweep -- every result in the register is conditioned on a single, arbitrary apo structural draw ([[TASK-0124]]'s 5TBY->8QYP swap moved AUC by 0.27 and erased CARDIAC_MYOSIN's only positive); sweep KRAS_G12C (the one target with a marginal surviving signal) across >=5 independently RCSB-verified apo structures, same holo/pipeline/cutoffs throughout, report the AUC/P@5/floor-clear distribution -- is the result structure-robust or a lucky draw? Picked up per `REVIEW-panel-2026-07-28-external.md` §2 ("a single apo structure is the ensemble," Motlagh-Hilser's own sub-population framing) and §5C, which names this task specifically as filed-but-unrun and a cheap, high-value gap before the 2026-08-08 writing freeze | Implementer | Done | P1 | 2026-07-30 | — | — | `.ai/tasks/DONE/TASK-0155-apo-structure-sensitivity-sweep.md` |
| TASK-0158 | `PANEL_REVIEW_2026-07-25.md` §2.3/W1 -- every permutation null in this project drew a uniformly *scattered* same-size subset, anti-conservative for real (spatially *compact*) pockets by 4.8x at alpha=0.05, rising to 42x at alpha=0.001 (confirmed directly). New `allostery.nulls.compact_patch`, validated (compact-vs-compact restores ~nominal calibration). Re-ran every named dependent task side by side (additive, originals preserved): [[TASK-0149]]/[[TASK-0151]]'s `dcc_low` loses Bonferroni-significance on BOTH CARDIAC_MYOSIN (p 0.000->0.060) AND PTP1B (p 0.001->0.019) -- **the review's own pre-registered falsification statement fires.** [[TASK-0142]]/[[TASK-0133]]/[[TASK-0139]]/[[TASK-0152]]'s nulls were already non-significant, weaken further, no verdict change. [[TASK-0145]]'s transport null evaluated (not re-run, Out of Scope) -- found MORE exposed (7.0x/242x), flagged as an urgent open follow-up | Implementer | Done | P0 | 2026-07-25 | — | — | `.ai/tasks/DONE/TASK-0158-compact-null-fix-and-rerun.md` |
| TASK-0165 | `PANEL_REVIEW_2026-07-25.md` W6/V4 -- `metrics.block_bootstrap_ci` blocks on sequence index, but pockets are spatially compact while sequence-scattered (same root mechanism as [[TASK-0158]]'s null defect). New `metrics.spatial_block_bootstrap_ci` (k-NN block, drop-in signature), validated: exact convergence to sequence-block behavior on a 1D-line control, 1.4x-2.4x widening on a globule control once an accidental sequence/3D correlation in the naive fixture was found and removed. **Real transport data (TASK-0145's BCR_ABL1 family + KRAS_G12C/PTP1B's `H_new` cells, TASK-0158's own flagged near-survivors): 4/5 cells got narrower, not wider** (0.83-0.98x; only KRAS_G12C widened, 1.27x) -- the opposite of the naive expectation, reported honestly. No cell anywhere in this project has ever had a non-overlapping CI (confirmed by direct grep); every re-checked cell still overlaps its floor's CI under the corrected method -- no verdict flips | Implementer | Done | P1 | 2026-07-27 | — | — | `.ai/tasks/DONE/TASK-0165-spatial-block-bootstrap.md` |
| TASK-0167.001 | [[TASK-0167]]'s protein-scale positive control -- build `allostery.plant` (channel reweighting: multiply edge weights along random-seed-to-random-target weighted shortest paths by `(1+strength)`, existing edges only) + `select_distal_patch` (two hard admission criteria) + `assert_confound_orthogonal` gate (exact-equality, no tolerance). Gate seen to pass across a strength sweep AND fire on a deliberately contaminated (edge-added) plant. **Decisive finding: a weight-only plant is structurally invisible to `build_H_new` and `dcc_low`** (confirmed exactly, `np.array_equal`, on real KRAS_G12C data) -- every GNM-derived `H_new` term and `dcc_low` itself route through `potentials._kirchhoff_eigh`, which always rebuilds a **binary** contact matrix from raw coords, never consulting `W`. Not a stale-cache bug (TASK-0135's precedent this task expected to find) -- there is no code path at all. [[TASK-0167.002]] must score against an operator built directly from `W_planted` (e.g. a weighted `hamiltonians.laplacian`), not `H_new`. Induced-coupling dose axis resolved by measurement: `R_eff` (`transport.effective_resistance_from_source`'s own `1/R_eff` conductance return, confirmed via its docstring) rises monotonically with strength (Rayleigh's law, real data); `dcc_low` proven exactly inert, not merely non-monotone as the prototype suggested | Implementer | Done | P0 | 2026-07-28 | — | — | `.ai/tasks/DONE/TASK-0167.001-plant-machinery-and-orthogonality-gate.md` |
| TASK-0169 | `PANEL_REVIEW_2026-07-28-external.md` §6/§7 (filed as TASK-0172, renumbered) -- consolidate this project's own already-scattered benchmark-integrity findings into one explicit per-target discriminability verdict: is the mandatory 3-target set capable of certifying a working method vs. a broken one? Every row independently re-verified against RCSB live (not relayed): **6C1H (challenge's own named CARDIAC_MYOSIN validation structure) reconfirmed to NOT contain mavacamten** (`nonpolymer_bound_components=['ADP','MG']` only; polymer entities are actin/myosin-Ib/calmodulin, no motor domain) -- this project's own substitute (8QYR) does. 5TBY reconfirmed a 20.0 A docked homology model verbatim from its own RCSB title. KRAS_G12C's active-site/pocket adjacency freshly measured (3.75 A min distance, not cited from memory). BCR_ABL1's pre-formed-pocket claim (RMSD ratio 0.49) and fpocket's 2/3-target win re-cited from already-executed runs ([[TASK-0120]]/[[TASK-0139]], [[TASK-0163]]). **A stale number caught**: the filing's own "0.798" KRAS_G12C floor is TASK-0094's pre-seed/clock/closed-form-fix number; current is 0.4818, corrected not propagated. **Verdict: every mandatory target fails at least one of 3 axes** (ground truth valid / task the stated task / task non-trivial) -- KRAS_G12C and BCR_ABL1 fail "distal/cryptic" and "non-trivial" (fpocket beats both by a wide margin); CARDIAC_MYOSIN fails "ground truth valid" for the challenge's own named structure. No axis 4 ([[TASK-0167.002]] still TODO at pickup). 5-point certifying-benchmark specification (constructive half). Promoted to `RESULTS.md` + `COMPETENCE_MAP.md`; no code touched, pure re-verification/synthesis | Architect/Planner | Done | P1 | 2026-07-28 | — | — | `.ai/tasks/DONE/TASK-0169-benchmark-discriminability-audit.md` |
| TASK-0171 | Re-run TASK-0162's forward/reverse coupling test on HOLO topology end-to-end (holo-native `H_new`/`H2_combinatorial_laplacian` + holo-native labels via `_holo_native_labels`, per TASK-0067's precedent) -- TASK-0162 confirmed apo-only by direct code read; this is the holo-side diagnostic comparison, not leakage-safe, per direct user request. **Result: a real, materially different apo-vs-holo gap from TASK-0067's own "tiny gap" precedent.** Forward floor-clear drops 10/25 (apo, matches TASK-0162 exactly) -> 6/25 (holo), 10/25 cells flip verdict; reverse stays 4/25 in aggregate but only 2/25 cells overlap. Mean \|gap\| (0.133 fwd/0.119 rev) an order of magnitude larger than TASK-0067's own 0.081/0.061, concentrated in `T_E0_Hnew` (up to -0.40) and `dcc_low` (up to -0.41, extending TASK-0153's own already-flagged complication). Diagnostic only, never a submission-path result | Implementer | Done | P2 | 2026-07-28 | Implementer D (this thread) | 2026-08-02 22:04 | `.ai/tasks/DONE/TASK-0171-reverse-direction-coupling-test-holo.md` |
| TASK-0172 | Spectrum-preserving Hamiltonian reduction (Krylov + Loewdin/Feshbach-Schur), citations verified first (Loewdin 1962/Feshbach 1962/Novo 2015, all confirmed). **Result: Schur beats the existing Louvain baseline on 2/3 targets (decisively on KRAS_G12C, loses on BCR_ABL1); block-Krylov breaks sharply, not gradually, at m<=n_active on all 3 targets.** Real structural finding: a block-Krylov seed of n_seed residues consumes n_seed Ritz dimensions per power step, so at this project's real active-site sizes (18-26) the seed alone meets/exceeds the ~12-16 node NISQ budget before reaching any distal structure -- confirmed a block-seed artifact not a Krylov limitation via a single-seed check. Louvain (existing baseline) is the least reliable of the three -- sometimes anti-correlated with the true ranking. Two real bugs caught+fixed en route (unsorted retain_idx silently permuting H_eff; un-normalized Louvain broadcast producing Bhattacharyya coefficients >1). No submission-operator change (Tier-2-gated) | Implementer C | Done | P1 | 2026-08-14 | — | — | `.ai/tasks/DONE/TASK-0172-spectrum-preserving-reduction.md` |
| TASK-0167.002 | [[TASK-0167]]'s protein-scale positive control -- detection curve + LOD through the unmodified verdict pipeline, built on [[TASK-0167.001]]'s finding that only `T(E=0)` on a weighted Laplacian is plant-sensitive (H_new/dcc_low/prs_low reconfirmed exactly invariant). 3 targets x 8 strengths x 20 seeds x 3 nulls x 2 CI methods, all gates in series. **Headline: under the corrected (compact, [[TASK-0158]]) null, no target reaches 80% detection power at any strength tested, up to ~4x background conductance (R_eff); under the superseded scattered null, LOD ~2x background on 2/3 targets** -- a direct, considerably more severe confirmation of the external review's own "2-2.5x power cost" estimate. Per-gate profile: CI non-overlap AND the permutation-null gate are both independently binding, neither alone explains the failure. Real non-monotonicity found on 2/3 targets (channel-leakage mechanism, matching TASK-0167.001's own prediction). Compact and Rg-matched nulls proved empirically indistinguishable at this patch size. BCR_ABL1 never reaches 80% power under any null. Two real bugs found+fixed: the external review's spatial-CI 1D-line claim verified overstated (55% block overlap, not exact coincidence -- corrected in TASK-0165), and the collection script's own Bonferroni family wrongly used the synthetic replicate grid (160) instead of targets (3) -- fixed in analysis, no re-run needed. RESULTS.md top-level section + COMPETENCE_MAP.md status-update callout (scope call: one callout, not every individual mention) | Implementer | Done | P0 | 2026-07-28 | — | — | `.ai/tasks/DONE/TASK-0167.002-detection-curve-and-limit-of-detection.md` |
| TASK-0168 | Mechanism-discriminating plant -- protein-scale generalization of TASK-0103's 44-node dumbbell double dissociation: does each observable family (directed-channel `ctqw`/`T(E)` vs. ensemble/mode `dcc_low`/`prs_low`) detect only the mechanism it's justified on? New `plant.plant_mode` (Plant B, boundary-softening, mirrors `plant_channel`) + `dcc_low_from_L` adapter (real `dcc_low`/`prs_low` structurally can't see any weight-only plant at all, re-confirmed). **Result: partially discriminating, not simply redundant, weaker/less general than hoped.** Mandatory scale-check passed both targets (GSR tracks a real well not a channel, CTQW the reverse). Channel family (`T(E=0)`/`ctqw_converged`) shows a clean, mechanism-specific, replicated (2/2 targets) point-estimate response; `dcc_low_from_L` shows the hoped-for opposite signature on KRAS_G12C only, does not replicate on BCR_ABL1. Under the corrected-null certification pipeline almost nothing reaches 80% power at the strengths tested -- extends TASK-0167.002's own finding to a second mechanism. See also TASK-0199 (more thorough register-wide redundancy measurement, split out of this task's own redundancy checkbox) | Implementer | Done | P2 | 2026-08-04 | — | — | `.ai/tasks/DONE/TASK-0168-mechanism-discriminating-plant.md` |
| TASK-0176 | Phase-1 end-to-end loop parent coordinator (2026-07-31 rev-8 batch): close the submission's required loop -- apo-only prediction scored against an undisputable holo ground truth. Subtask table: 0177/0178/0180/0181/0182/0183/0184/0185. **TASK-0179 retired** (folded into 0178) -- do not re-file | Architect/Planner | TODO | P0 | 2026-07-31 | — | — | `.ai/tasks/TODO/TASK-0176-phase1-end-to-end-apo-to-holo-verified.md` |
| TASK-0177 | Consensus holo pocket ground truth -- replace the single-cutoff `holo_pocket_mask` with a >=4-independent-criteria consensus label (C1 contact / C2 dSASA / C3 fpocket cavity / C4 depositor annotation / C5 crypticity / C6 distality), reporting a stable core, disputed shell, and per-target resolution statistic. **Hard-blocks every scoring claim in TASK-0176** | Implementer | Done | P0 | 2026-07-31 | — | — | `.ai/tasks/DONE/TASK-0177-consensus-holo-pocket-ground-truth.md` |
| TASK-0178 | Binding-response coupling -- exact closed-form GNM allosteric coupling free energy `ddG(A,B)` + distance-normalised specificity statistic (`coupling_specificity`), reciprocity/zero-coupling/dumbbell gates pre-registered. Absorbs retired TASK-0179 (module + evaluation merged, same owner) | Implementer | Done | P1 | 2026-07-31 | — | — | `.ai/tasks/DONE/TASK-0178-binding-response-coupling.md` |
| TASK-0180 | Site-level prediction + the single end-to-end apo-to-pocket runner -- cluster top-scoring residues into spatially deduplicated ranked sites (fixes `assemble_hit_list`'s no-dedup defect), site-level hit metric with its own chance level, wired additively into `run_challenge.py`; reconciles `main`'s `min_sep`/`distal_ang` vs the research branch | Implementer | Done | P0 | 2026-07-31 | — | — | `.ai/tasks/DONE/TASK-0180-site-level-prediction-and-end-to-end-runner.md` |
| TASK-0181 | Selection formulation -- Phase A: constrained-subset QUBO (`allostery.selection`, hand-rolled exact/SA solvers, no new dependency) solved classically at the pre-registered canonical weight point. **Gate CLOSED: 0/3 mandatory targets beaten (KRAS_G12C greedy hits, QUBO misses despite a higher self-scored objective; BCR_ABL1/CARDIAC_MYOSIN both miss), robust to solver noise (8-restart check) -- no quantum formulation built, full 14-target grid not run per the gate's own Constraint.** 3 pre-registered controls all pass (degenerate-case, anti-confound, solver-quality). Phase B (write-up only, not built): `PHASE_B_ROTAMER_QUBO.md`, side-chain rotamer-packing QUBO, cross-referenced by [[TASK-0185]] row 51 | Implementer (Phase A) / Architect/Planner (gate + Phase B write-up) | Done | P1 | 2026-08-02 | — | — | `.ai/tasks/DONE/TASK-0181-selection-formulation.md` |
| TASK-0182 | Hardware realization -- per-target qubit/2-qubit-gate/depth/Trotter-step resource table at full + coarse-grained resolutions; re-run the TASK-0068 NISQ noise study under the converged propagator (phase-free-robustness hypothesis pre-registered); one real small-instance Braket execution; explicit EXECUTABLE_NOW/EXECUTABLE_COARSE/FAULT_TOLERANT_ONLY verdict per target | Implementer | Done | P1 | 2026-07-31 | — | — | `.ai/tasks/DONE/TASK-0182-hardware-realization-and-resource-accounting.md` |
| TASK-0183 | **Thread: Implementer A. Done, 2026-08-17.** Delivered `documentation/POC_SPRINT_PLAN.md`. **Found the task's own "Scope decision" (2026-07-29) stale on pickup** — its primary track (conformational search + side-chain QUBO) is now closed by measurement ([[TASK-0204]] complexity closure, [[TASK-0213]] coupled-search closure, [[TASK-0217]]'s "no open quantum-advantage route remains"); writing to the literal old text would have proposed a QUBO for a problem this register already solved in milliseconds, exactly what this task's own Constraint forbids. Corrected via a dated, superseding SCOPE UPDATE (kept, not deleted) to match [[TASK-0184]]'s already-decided (A′) narrative — not a new decision, its mechanical consequence. **Delivered**: 4 monthly milestones (scale the certifying benchmark; apo-vs-stripped-holo delta; apo-only screening criterion; packaging), each with a falsifiable exit criterion; resource numbers all traced to specific tasks, none invented; a 6-item risk register including this session's own construct-validity sweep as a new entry; team split drawn from the register's own proven role structure; an explicit exclusion list (no MD, no observable proliferation, no deep-ML on ENM data, no quantum hardware promised, LOD not accuracy target). **[[Q-0002]] folded in** (Reviewer thread, 4 compliance facts): rubric weights stated as a holding assumption (unverified against the only challenge doc in-repo); Braket/Classiq named concretely as target platforms per §Constraint 4; CARDIAC_MYOSIN's quoted qubit count annotated with its actual (non-Table-1) structure pair. Propagated into [[TASK-0184]]'s own §3/§6 `[TBD]` markers. **2026-08-19 update**: the holding assumption is now confirmed — `documentation/2026-04-06-Assessment-Criteria-VF.md` landed (gitignored, `documentation/*.md`, via [[TASK-0221]]) and matches the 25/25/20/15/5/10 weighting and "Phase 1 = Ideation" premise exactly. Phase 2's own previously-unavailable criteria (PoC Quality & Results 30%, Enterprise Relevance 25%, Technical Rigour 20%, Scalability & Path Forward 15%, Presentation & Communication 10%) folded into a new "Phase 2, now known" section in the sprint plan — 3 of 5 already map onto the plan's existing shape, stated explicitly; 2 (Enterprise Relevance, Presentation) named as not owned by this plan rather than silently assumed covered | Architect/Planner (with Bartosz) | Done | P0 | 2026-07-31 | — | — | `.ai/tasks/DONE/TASK-0183-poc-sprint-plan.md` |
| TASK-0184 | The Phase-1 submission -- freeze the analysis register 2026-08-07, record the narrative framing decision (A/B/C, recommend B: "wrong problem, here's the right one"), write the proposal + §5.3 methodological report, full numbers audit against `RESULTS.md`, submit by deadline 2026-09-15. **§4.3 item 2 (workflow description) is ready to paste from [[TASK-0224]]'s own closing section** — not yet pulled in, this document's own narrative-freeze content is untouched by that task on purpose | Bartosz (writing) / Architect/Planner (section map, evidence pull) | TODO | P0 | 2026-07-31 | — | — | `.ai/tasks/TODO/TASK-0184-phase1-submission-document.md` |
| TASK-0228 | Re-indexed 2026-08-21 external drop (`TASK-0212_conformer_graph_search`) — conformer-graph search reframing (branching factor vs. path depth). **§2 PDB-retest, pocket-restricted half done**: built `adaptive_anm_modes` (per-step-recomputed ANM subspace union) on the project's own validated `H13_3N_anm_hessian`; measured static vs. adaptive `restricted_cumulative_overlap` against each mandatory target's real labeled pocket displacement, equal dimension (110). **Mixed, real, target-dependent — not the drop's own reported 9.4× win**: KRAS_G12C 1.31×, CARDIAC_MYOSIN 1.42×, BCR_ABL1 **0.73× (adaptive worse)**. Complements [[TASK-0227]]'s sibling whole-structure PDB-retest (same batch, same day) — that task explicitly left the pocket-restricted question for whoever picked this task up. **§6.2 progress-probability `p`, collective-move-layer partial version, also done**: real, well-powered (47-59 admissible moves/target, after finding the module's own frozen amplitude protocol left only 1-7 admissible), all 3 targets land inside the document's own cited 0.24-0.69 "not rare" band (KRAS_G12C 0.492, BCR_ABL1 0.237, CARDIAC_MYOSIN 0.426) — same conclusion as every other progress-probability finding in this project. [[Q-0004]] (full joint blocked on missing rotamer packer) **retracted same day**: EvoEF2/fpocket turned out to be vendored, not absent (TASK-0227's own correction, commit `fa94316`). **Full joint (backbone+rotamer) `p` built and run**: extends each admissible move to a real full-atom structure (`prody.extendVector`), EvoEF2-repacks the pocket window, scores fpocket druggability. Sharply target-dependent, tracking each target's own apo baseline druggability: KRAS_G12C 0.25→**1.00**, BCR_ABL1 0.20→**0.05** (repacking hurts an already-druggable apo pocket — consistent with this project's "apo-computable structural prior" finding), CARDIAC_MYOSIN 0.50→0.65. Cross-checked against [[TASK-0230]] (same day, independent build, same tooling correction): baseline druggability numbers match exactly. RESULTS.md addendum landed (commit `a316268`) — caught a live claim (Implementer B, TASK-0230) via the SCQ queue before almost force-overriding it as stale. §3/§4/§5/§6.1/§6.3/§6.4 remain unbuilt — real, substantial remaining scope | Toolsmith/Implementer C | Done | P2 | 2026-08-21 | — | — | `.ai/tasks/DONE/TASK-0228-conformer-graph-search-branching-vs-depth.md` |
| TASK-0224 | **Thread: Architect/Planner. Done, 2026-08-19.** `documentation/WORKFLOW.md` (new) — the canonical end-to-end pipeline, transcribed from `run_challenge.py::run_target` itself, 10 steps each with assertion/enforcement-status/failure-found/task-citation. 3-way enforcement classification (Enforced / Enforced downstream / Documented-audited-not-gated), not binary — surfaces real gaps a binary would flatten: Step 1's chain-correspondence override is opt-in (no check without it beyond a coarse `<3-pairs` raise); Step 6's connectivity check only warns in `clean.py`, the real hard gate is a different module (`superpose.py`'s ANM nullspace check, TASK-0038); Steps 2/3 and half of Step 9 are **not gated by the pipeline at all** — separate, human-invoked audits. Caught and fixed its own error before publishing: a first-draft citation (Step 7's truncation numbers) was fabricated/misattributed, re-sourced directly against TASK-0159's own record before landing. "Cannot do" section flags KRAS_G12C's own apo structure as wild-type (not G12C) and a lucky-draw outlier (median AUC 0.482, P@5=0.000 across 10 verified G12C structures, TASK-0155/TASK-0192) — not in the original pre-registered table, added because a submission-facing pipeline doc omitting it would itself be the gap this task exists to close. Picked up after overriding a 5h20m-stale Reviewer-thread claim (no file existed yet, no work lost, override authorized in-session) | Architect/Planner | Done | P0 | 2026-08-19 | — | — | `.ai/tasks/DONE/TASK-0224-canonical-workflow-with-assertions.md` |
| TASK-0185 | Conformational-search reformulation -- reframes cryptic-pocket prediction as ENM-ensemble search, fpocket as oracle ([[TASK-0163]]). **Real measurement on all 3 mandatory targets (100-sample ANM ensembles, MSF gate passed on all 3): positive control BCR_ABL1 passes convincingly (80% hit rate); specificity vs. matched decoy is mixed** -- BCR_ABL1 gap 0.29, KRAS_G12C gap 0.13, **CARDIAC_MYOSIN no specificity at all** (0.200 vs 0.190). n_modes sweep on BCR_ABL1 corroborates the synthetic toy model's own not-rare finding on real data (recovery 0.78-0.98, not rare) -- backbone-layer Grover-style search claim fails on real data too; quantum claim moves to the side-chain layer, cross-referenced (not duplicated) with [[TASK-0181]] Phase B, in progress elsewhere | Architect/Planner (design) / Implementer C (measurement) | Done | P1 | 2026-08-02 | — | — | `.ai/tasks/DONE/TASK-0185-conformational-search-reformulation.md` |
| TASK-0186 | Active-site to pocket distance generalization audit, 3 axes (Euclidean / spatial contact-graph hop / primary-sequence chain hop) -- ran on all 7 pocket-scoreable targets (env fixed 2026-08-01). **Result: partial generalization, uneven.** Min spatial-hop=1 on 6/7 targets (only PTP1B is genuinely non-trivial, min hop 2, 0% of pocket at hop<=1); fraction of pocket at hop<=1 ranges CASPASE1 83% (worse than KRAS_G12C's 39%) down to PTP1B 0%; fold-compression ratio (chain-hop/spatial-hop) large everywhere, 8.9-37.7x. KRAS_G12C sanity check exact match to TASK-0169 (3.7530 A). Caveat: scored against the incumbent 4.5 A label only, pending re-score against TASK-0177's consensus label | Architect/Planner | Done | P0 | 2026-08-01 | — | — | `.ai/tasks/DONE/TASK-0186-hop-distance-generalization-audit.md` |
| TASK-0187 | ENM-induced connectivity-graph shortcut hypothesis on PTP1B (TASK-0186's positive control). **Result: clean negative on specificity.** New `allostery.shortcuts` module: 2000-sample closed-form ANM equipartition ensemble (blocking MSF cross-check passed, r=0.9998); real-pocket shortcut rate 1.7% vs. 30 matched-decoy rates 0-52.5% (median 12.9%) -- real pocket sits *below* the decoy median, effect size -0.112 (wrong sign), fails the pre-registered gate. Holo-native hop unchanged at 2. Not extended past the positive control, per the task's own pre-registered TODO ordering | Implementer (build) / Architect/Planner (design) | Done | P1 | 2026-08-01 | — | — | `.ai/tasks/DONE/TASK-0187-enm-connectivity-shortcut-hypothesis.md` |
| TASK-0188 | Spatial contact-graph hop-cutoff robustness sweep -- the 8.0 A cutoff TASK-0186/TASK-0177-C6 use for hop-count was only ever benchmarked (TASK-0067) for a different metric (GNM AUC) over a narrower range (7.5/8.0/10.0); this sweeps {6.0..9.0} A on the hop-count metric specifically, checks connectivity doesn't fragment at low cutoffs (confirmed OK down to 5.0 A on KRAS_G12C before filing), and states which of TASK-0186's headline claims (esp. CASPASE1's 83%) survive. No source-code changes needed -- pure sweep script reusing existing `cutoff` parameters | Implementer | Done | P1 | 2026-08-01 | — | — | `.ai/tasks/DONE/TASK-0188-hop-distance-cutoff-robustness-sweep.md` |
| TASK-0157 | Two-boson Hong-Ou-Mandel path interference -- the one genuinely non-reducible multi-particle residual (fermions reduce to determinants/P, Terhal & DiVincenzo 2002; naive co-occupation is already-dead Falsifier C; anyons have no formalism on a 3D contact graph). `PANEL_REVIEW_2026-07-25.md` explicitly deprioritizes this task: "file, do not build before the deadline... describe in the forward-proposal section." **Scope corrected to match, not built**: wrote the required non-reducibility precondition paragraph (the HOM bunching cross-term depends on the *relative phase* between two single-particle amplitude paths, information every converged/time-averaged observable in this register -- TASK-0130 -- provably discards, TASK-0146's own finding) and independently re-verified all 3 governing citations (Valiant 1979 #P-completeness of the permanent; Terhal & DiVincenzo 2002; Aaronson & Arkhipov 2011, linear-optics hardness needs many photons/`m=O(n^2)` modes) via live search against each paper's own abstract. Honest ceiling restated from the primary sources: a 2x2 permanent equals a 2x2 determinant in cost, so even a clean positive at k=2 would be a better observable, not a demonstrated quantum advantage. No k=2 Hilbert space, synthetic falsifier, or real-target scoring built, per the review's own explicit instruction. Filed as a forward-proposal note in `EXECUTION_PLAN.md`'s Phase 1E + `RESULTS.md` section + open-questions row 50 | Implementer | Done | P3 | 2026-07-25 | — | — | `.ai/tasks/DONE/TASK-0157-two-boson-hom-interference.md` |
| TASK-0189 | `zero_plant_specificity.py:154` reintroduced the exact Bonferroni-family bug TASK-0167.002 found and fixed (`ALPHA/(len(STRENGTHS)*N_SEEDS)`=3.125e-4 instead of `alpha/3`=0.0167), with a comment falsely asserting it matches .002. Compounding: at that alpha the gate is unreachable — min non-zero p is `1/N_PERM_REPS`=1e-3 (5e-3 matched) > 3.125e-4, so gate4 fires only at p==0 exactly. TASK-0167.003's Part A alpha-hat table therefore does not measure a false-positive rate "against a 5% nominal bar"; the compact/matched `alpha_hat<=0.2%` figures are near-guaranteed by construction and cannot distinguish a calibrated null from an over-conservative one. Recompute from stored raw p-values at alpha/3; add an `(alpha, family, n_reps)` reachability guard | Implementer | Done | P0 | 2026-08-03 | — | — | `.ai/tasks/DONE/TASK-0189-zero-plant-bonferroni-family-and-unreachable-gate.md` |
| TASK-0190 | The external review's own §2.1 P0, never executed: `compact_patch_matched`'s only 2 non-test call sites both set `target_rg` from a `select_distal_patch` draw, which is itself a `compact_patch` draw (`plant.py:244`) — so "matched ~= compact" is true *by construction*, not a finding, though `RESULTS.md` reports it as an answer to the over-correction question and commit `92aa669` calls the dispute settled. TASK-0167.003 Part B meanwhile shows every real pocket sits at the 97th-100th percentile of the compact null's own Rg distribution (real Rg 6.42-10.84 A) — real pockets are systematically more dispersed than the null they are tested against. Re-run `dcc_low` (CARDIAC_MYOSIN p=0.060, PTP1B p=0.019) and `T(E=0)` (BCR_ABL1) at `target_rg`=measured real pocket Rg; report scattered/ball/matched side by side. Decides whether §5.3's falsification statement fired against a mis-specified null | Implementer | Done | P0 | 2026-08-03 | — | — | `.ai/tasks/DONE/TASK-0190-real-pocket-rg-matched-null.md` |
| TASK-0191 | `RESULTS.md` synthesis repair, 3 defects: (a) TASK-0167.003's "Zero-plant specificity and null-calibration" section was written, deliberately withheld from commit `92aa669` to avoid clobbering a concurrent thread, never re-filed — 0 grep hits, content lost, while both the task file and the commit cite it as "Full detail"; (b) TASK-0161's 226-cell multiplicity budget frozen 2026-07-25 while TASK-0162/0163/0170/0171/0177/0178/0185/0187/0155 all landed after, and TASK-0178's own section says it adds ~21 cells; (c) TASK-0178's section calls TASK-0167.002 "still-TODO" 4 days after it closed | Implementer | Done | P1 | 2026-08-03 | — | — | `.ai/tasks/DONE/TASK-0191-results-synthesis-repair.md` |
| TASK-0192 | Propagate TASK-0155's KRAS_G12C wrong-genotype finding out of `RESULTS.md` — independently re-verified (4obe.pdb chain A residue 12 = GLY, not CYS). Still unflagged in `config/targets.yaml:58`, `backend/systems.py:19-22` (live app, ships `covalent_anchor=12` on a Gly12 structure), `SOFTWARE.md:222`, and `COMPETENCE_MAP.md` (zero mentions — while its current table still carries KRAS as the project's ONLY `NO_FAILURE_DETECTED` mandatory row at +73.7% headroom, the exact number TASK-0155 showed is both wrong-mutant and a lucky draw: median 0.482 across 10 verified G12C structures, P@5=0.000 on all ten). Flag, do not swap — every historical number is conditioned on 4OBE | Implementer | Done | P0 | 2026-08-03 | — | — | `.ai/tasks/DONE/TASK-0192-kras-genotype-propagation.md` |
| TASK-0193 | `COMPETENCE_MAP.md`'s per-target narrative sections (`:297` KRAS, `:331` BCR_ABL1, `:404` CARDIAC_MYOSIN) end at TASK-0129 and carry headings stating verdicts the document's own current TASK-0130 table (`:147`) reverses — KRAS's heading reads "the actual result is still below floor" while the current row is 0.5901 clearing 0.4818, `NO_FAILURE_DETECTED`. The superseded *tables* above them are correctly bannered; the narratives are not. Apply the same convention | Implementer | Done | P2 | 2026-08-03 | — | — | `.ai/tasks/DONE/TASK-0193-competence-map-narrative-staleness.md` |
| TASK-0194 | `ARCHITECTURE.md`'s change log's newest entry is 2026-06-28 while 5 commits changed `backend/`/`frontend/` since (`c5cd13f` new module `backend/geometry.py`, `b3e7ef4` XSS fix, `e6e442e` fetch error handling, `1232340` Kirchhoff context, `2ea0224` characterization tests) — a direct, repeated violation of CLAUDE.md convention #7. §5's directory layout also lists neither `geometry.py` nor `rcsb_extract.py`; §2's dependency diagram is missing `geometry`. Retroactive dated/signed lines (visibly retroactive), layout + diagram catch-up, `SOFTWARE.md` check | Implementer | Done | P2 | 2026-08-03 | — | — | `.ai/tasks/DONE/TASK-0194-architecture-changelog-catchup.md` |
| TASK-0195 | `RESULTS.md` has no claim/lock/numbering protection despite being edited by every closing thread across machines — two silent losses in one week: open-questions rows 47-53 overwritten by a stale-snapshot merge (recovered `61b8096`, renumbered 52-59, invalidating TASK-0182's "row 51" citation) and TASK-0167.003's section lost entirely (TASK-0191). Same failure class as Q-0001/TASK-0107 on `COMMON.md`, which got protection; this file did not. Two distinct weaknesses: stale-snapshot merges that merge clean, and positional (row-number) cross-references that a renumber silently invalidates. Convention half (cite task IDs, not row numbers) lands free; tooling half likely subsumes TASK-0024.001. **Open design question first**: a lock would NOT have caught loss #1 — a staleness check might be the correct mechanism | Toolsmith | Done | P1 | 2026-08-03 | — | — | `.ai/tasks/DONE/TASK-0195-results-md-concurrent-write-protection.md` |
| TASK-0196 | `pytest_local.py`'s 4 hand-named single-file presets are stale by 49 files (53 test files now exist, incl. `test_response.py`/`test_selection.py`/`test_shortcuts.py`/`test_sites.py` from this session's own work) — adds a validated `--file <basename>` selector (format + existence checked, no path traversal, no free-form pytest args) so any test file gets a fast whitelisted run without a full ~126s `wip-all`. Filed after an ad hoc chained/piped `pytest` invocation (can't match any whitelist pattern) prompted unnecessarily | Implementer | Done | P2 | 2026-08-03 | — | — | `.ai/tasks/DONE/TASK-0196-pytest-local-single-file-selector.md` |
| TASK-0197 | `claim.py stage --expect <path>` refuses outright when `<path>` is absent from disk, even when that absence is a legitimate deletion of a previously-tracked file — cannot distinguish a typo from a real cleanup, forcing a bare un-whitelisted `git add`/`git rm`. Adds a `git ls-files`-based tracked-check so a tracked-but-missing path stages its deletion, while a genuinely never-tracked path still refuses. Live test case: a duplicate `TASK-0189` file tracked in both `TODO/` and `DONE/` after another thread's commit | Toolsmith | Done | P2 | 2026-08-03 | — | — | `.ai/tasks/DONE/TASK-0197-stage-tool-deletion-support.md` |
| TASK-0198 | `claim.py move` leaves the *original* starting path duplicate-tracked when a task transits 2+ states (`TODO -> IN_PROGRESS -> DONE`) with no `git commit` in between — reproduced twice same session (`TASK-0189`, `TASK-0073`), both fixed by hand via TASK-0197's new deletion-staging support. Plausible mechanism named but unconfirmed: editing the task file's own content between the two `move` calls may desync working-tree vs. index before the second `git mv`. Fix at the identified step, or add a self-check that catches it at introduction time | Toolsmith | Done | P2 | 2026-08-03 | — | — | `.ai/tasks/DONE/TASK-0198-move-duplicate-file-on-chained-transitions.md` |
| TASK-0199 | Effective rank of the observable register — the program treats ~40 observables / 226 cells as breadth (many independent falsification attempts), but every observable is a scalar function of the same static Cα contact graph seeded at the active site, and independence has never been measured. Cross-observable Spearman matrix + effective rank (participation ratio, pre-registered), per target, with `−hop`/`−euclid` projected in to measure how much of the leading component is the proximity confound. Low rank (≈2–3) → the 226 cells are ~15 independent tests, TASK-0161's arithmetic changes *against* the program, and every negative is explained at once as one quantity measured thoroughly; high rank → the negative is stronger than currently claimed. **Consumes no multiplicity budget — observable-vs-observable only, never against a label.** Split out of TASK-0168's own redundancy checkbox, which needs no plant and is blocked behind a P2 it does not depend on | Implementer | Done | P0 | 2026-08-03 | — | — | `.ai/tasks/DONE/TASK-0199-observable-register-effective-rank.md` |
| TASK-0200 | Conditional-on-fpocket residual analysis — **resolved: strict subset, asymmetric.** Forward (dynamics \| fpocket): no cell on any of 3 targets x 3 representative observables x 4 swept band widths beats its within-band floor and clears the within-band permutation null — clean, consistent negative. Reverse (fpocket \| dynamics): one cell (BCR_ABL1 `dcc_low`, one window) crosses the pre-registered bar but fails this task's own robustness rule (does not survive the other 3 swept windows) — disqualified, reported as a suggestive sub-threshold pattern only. Positive control (planted, geometry-blind) confirms the pipeline can detect a real signal; negative control confirms the null-clearance criterion rejects chance floor-beats. fpocket's recomputed AUC did not match TASK-0163's published numbers on any target (diagnosed — label/chains/determinism all ruled out; likely vendored-binary drift, not chased further) | Implementer | Done | P1 | 2026-08-03 | — | — | `.ai/tasks/DONE/TASK-0200-conditional-on-fpocket-residual-analysis.md` |
| TASK-0201 | A null construction that can actually reach real-pocket Rg — TASK-0190 found `compact_patch_matched` never raises on CARDIAC_MYOSIN/PTP1B but silently fails to centre on `target_rg`: the k-NN-ball construction has a hard Rg ceiling (20,000-draw max 7.784/7.134) below the real pocket's own Rg (9.628/7.969) — structurally unreachable, not rare. Build + verify an alternative whose Rg support genuinely covers real pocket geometry; re-run the two affected `dcc_low` cells against it. Does not change TASK-0190's own negative verdict (a genuinely-reaching null would only be more lenient) | Implementer | Done | P2 | 2026-08-03 | — | — | `.ai/tasks/DONE/TASK-0201-rg-reaching-compact-null-construction.md` |
| TASK-0156 | Rank residues by minimum GRAPE control-energy `E_i` to steer the CTQW from the active-site seed to each residue (Khaneja et al. 2005), the classical analogue being Atilgan & Atilgan 2009's PRS — **resolved: no, chance-level on all 3 mandatory targets** (stratified AUC 0.501/0.404/0.474, permutation p=0.48/0.99/0.71). Synthetic dumbbell gate passed (tracks planted coupling at equal hop-distance) and kill-switch passed (rho=0.56, not a distance proxy) before scoring. A size-blind fixed horizon T=15 first produced a degenerate all-0.5-AUC artifact on the 2 larger targets (near-zero feasible fraction); this project's own gap-based `min_adequate_t_max` rescaling was checked and also failed to fix it (KRAS/BCR_ABL1 have near-identical spectral gaps); fixed via a label-blind per-target feasibility-rate search instead. T/F sensitivity check confirms rankings are stable (rho 0.73-0.89) while AUC stays at chance — a real negative, not a hyperparameter artifact | Implementer | Done | P2 | 2026-08-04 | — | — | `.ai/tasks/DONE/TASK-0156-control-effort-scanning.md` |
| TASK-0202 | **Closed 2026-08-24 (Architect) — a stale claim, not stale work**, per a reviewer's stranded-locks audit. Adopted a merge/rebase conflict resolution protocol — `.ai/reference/MERGE_CONFLICT_PROTOCOL.md`, grounded in 3 real incidents (COMMON.md whole-file collision, `RESULTS.md` silent row-loss, `claim.py move` duplicate-file defect — [[TASK-0195]]/[[TASK-0198]], both now Done). All 5 TODO items found already satisfied by this session's own ordinary subsequent use (Current Rules bullet, Quick Navigation line, both-ways cross-links all landed silently along the way) — only the claim/Status bookkeeping never caught up. The one literal remaining item ("rebase `bartosz` onto `origin/bartosz`") is moot: no divergence currently exists (46 ahead, 0 behind), and the protocol has been exercised for real repeatedly since (TASK-0205, TASK-0224), a stronger validation than the single staged rebase originally planned | Architect/Planner | Done | P1 | 2026-08-05 | — | — | `.ai/tasks/DONE/TASK-0202-adopt-merge-conflict-protocol.md` |
| TASK-0203 | **Thread: Implementer A (science).** The PTP1B triangle — three results landed in 48h from three threads, none citing the others: TASK-0201's PTP1B `dcc_low` positive (the program's only one), TASK-0200's strict-subset verdict (3 mandatory targets, **PTP1B not among them** — `fpocket_conditional_analysis.py:58`), TASK-0168's finding that `dcc_low`'s mechanism signature holds on KRAS_G12C but fails on BCR_ABL1 (**PTP1B not tested**). "Dynamics ⊂ static geometry" and "PTP1B's `dcc_low` is a real positive" cannot both stand unqualified unless the positive lives exactly where TASK-0200 never looked — it does. Two legs, existing tested code + one target each: conditional-on-fpocket on PTP1B (update `FAMILY_SIZE`, currently hardcoded 18) and the 2-mechanism plant grid on PTP1B. Four-outcome reading rule pre-registered before running | Implementer | Done | P0 | 2026-08-05 | — | — | `.ai/tasks/DONE/TASK-0203-ptp1b-mechanism-triangle.md` |
| TASK-0204 | **Thread: Implementer B (science).** TASK-0181 Phase B's own falsification criterion #1, never run: solve the side-chain rotamer packing objective **classically** on real windows and test whether it opens an fpocket-druggable cavity more often than naive greedy per-residue rotamer choice. The last route where the quantum-advantage argument does not collapse — rotamer packing is discrete/NP-hard (Pierce & Winfree 2002) and pocket opening there is *plausibly* a rare event, the precondition TASK-0185 measured and found **absent** at the backbone layer (1-8 draws/hit). Purely classical, no QAOA/hardware (Phase A's precedent binds: no quantum formulation for an objective without classical value). Needs sourcing a rotamer library + pairwise energy or an existing DEE/A* packer — `src/allostery/` has none. Closing the route is as valuable as opening it **[relocated by TASK-0355, 2026-09-09]:** Actual last claimant/timestamp before this row's `Claimed By`/`Claimed At` cells were hand-reused (now reset by `sync`): Reviewer-thread (Opus), 2026-08-06 20:38. | Implementer | Done | P1 | 2026-08-05 | — | — | `.ai/tasks/DONE/TASK-0204-phase-b-rotamer-qubo-classical-baseline.md` |
| TASK-0205 | **Thread: Architect/Planner (synthesis).** The multiplicity-budget paragraph got one of two corrections: TASK-0201's "no longer zero" caveat landed, TASK-0199's effective-rank rescaling did not — so it still argues *"zero against expected ~18.3 is a stronger position than zero against ~11.3"* when the measured ~9x redundancy factor puts the real expectation at **~2**, and more non-independent cells do not strengthen the claim. Separately `COMPETENCE_MAP.md` is stale for the **third time in three weeks** (zero mentions of 0190/0199/0200/0201; its PTP1B row does not know that target carries the program's only positive) — after TASK-0192 and TASK-0193 both fixed it. Includes the process fix: a standing rule that any result changing a per-target verdict updates it in the same commit, mirroring CLAUDE.md convention #7 | Architect/Planner | Done | P1 | 2026-08-05 | — | — | `.ai/tasks/DONE/TASK-0205-budget-rescaling-and-competence-map-refresh.md` |
| TASK-0206 | The vendored `fpocket` binary drift, root-caused not just pinned. **`tools/fpocket/bin/` is gitignored — never version-controlled, only the build recipe is — and TASK-0163/TASK-0200's commits carry two different machines' git identities, confirming two independently-built, previously-unpinned binaries is the cause** (raised directly by the orchestrating user mid-task, confirmed correct). The TASK-0177 arm64-rebuild hypothesis doesn't hold up (binary never tracked, no commit to diff). Binary's own version banner ("fpocket 4.0") proven untrustworthy as a pin (exposes a 4.1-era flag). Pinned via SHA256 (`tools/fpocket/PROVENANCE.json` + new `allostery.baselines.fpocket_provenance()`), authoritative set = the reproducible recomputed one (0.7910/0.8618/0.5303, re-verified fresh), golden-value test (`tests/test_fpocket_pin.py`, ran for real, passed) + citations swept (COMPETENCE_MAP.md/RESULTS.md/ALGORITHM_REGISTER.md/EXECUTION_PLAN.md + 2 more). No verdict changes. Cross-machine recomputation still open — needs a thread on the other machine | Implementer C | Done | P1 | 2026-08-06 | — | — | `.ai/tasks/DONE/TASK-0206-fpocket-binary-drift.md` |
| TASK-0207 | Named TASK-0199's ~3 axes — refined, not confirmed. **PC1 = the 15-member Hamiltonian-occupancy family on all 5 targets (46.5-60.6% of variance), substantially the seed-proximity confound** (`-hop`/`-euclid` land there in 7/10 pairs) — not a separable "proximity" label. **PC2 = `ensemble_mode`-labelled on 4/5 targets but driven specifically by `prs_low`**, not the family as a whole (`prs_low`/`dcc_low` rho=-0.11) — independently confirms TASK-0168's own "genuinely non-redundant observable" finding via an unrelated method. **PC3 does not survive across targets** (3 different labels in 5 targets). The pre-hypothesized "directed channel" axis never dominates a component anywhere — real under active perturbation (TASK-0168) but not a source of passive-data variance. No verdict changes; refines TASK-0199's headline characterization | Architect/Planner | Done | P1 | 2026-08-05 | — | — | `.ai/tasks/DONE/TASK-0207-label-the-principal-components.md` |

| TASK-0208 | **Thread: Implementer A (original run) → Reviewer thread (recompute) → Architect (write-up reconciliation, 2026-08-12). Done — INCONCLUSIVE, not CLOSED.** Decomposed real apo→holo change into backbone (φ/ψ) vs side-chain (χ), 4 targets. **The original run's "CLOSED on all 4" verdict does not survive its own reviewer's audit**, run 2026-08-07 same day: (V1) the frustration statistic's `joint`/`sum_singles` were ~97% clash energy from rigid rotamer transplant, not coupling, until relaxed (EvoEF2 `RepairStructure`); (V2) the `not_evaluable` guard should have fired on 3/4 targets (`side_chain_explained` 0.036-0.078) and didn't, including PTP1B's headline +16.2%; (V3) `backbone_explained`'s "substantially_backbone" call on all 4 inverts in dihedral space (bb-share 0.20-0.37, below 0.5 everywhere). Corrected reading: coupling statistic `not_evaluable` on 3/4 targets, not-frustrated-at-9.2%-gap on GLUCOKINASE; attribution supports neither a pure-side-chain nor pure-backbone framing. **TASK-0210 is neither opened nor closed by this evidence** — gate stays genuinely open, pending a coupling statistic that works when `side_chain_explained` is small (does not yet exist). Invalidated TASK-0212 as filed (its PTP1B ranking premise no longer exists) | Implementer | Done | P0 | 2026-08-07 | — | — | `.ai/tasks/DONE/TASK-0208-apo-holo-change-decomposition-gate.md` |
| TASK-0210 | **Thread: Implementer A (science). Done, 2026-08-12.** Ran the pre-registered classical solver first (ANM-mode backbone perturbation, apo-only, + EvoEF2 `SideChainRepack` + fpocket-druggability objective; 2 restarts x 10 iterations/target — revised down from an initial 3x40 draft after timing `SideChainRepack` mid-registration, see task file). **Known-answer/firewall check passes on only 2/4 targets** (KRAS_G12C, PTP1B: objective correctly prefers holo; CASPASE1, GLUCOKINASE: inverted — apo scores *more* druggable than holo at the window, CASPASE1 plausibly because its "apo" 1ICE is covalently occupied at the orthosteric site by an unrelated inhibitor, GLUCOKINASE unexplained) — those two targets' search results are **not interpretable**. On the 2 evaluable targets, **0 of 2 restarts on either reached the known holo basin** within budget (best scores 0.29-0.47 vs. true holo 0.76-0.89). **Verdict: tentatively OPEN, weakly supported — not handed to TASK-0183.** The budget turned out far cheaper in practice than the estimate that set it (~2-10s/call vs. an initial 16s/call misread from a stray calibration file), so this negative result comes from an under-tuned first attempt on n=2 valid targets, not a rigorous characterization; a properly-budgeted rerun is the recommended next step, filed as this task's own "Recommended next step," not a new TASK-XXXX (small enough to be a rerun, not new scope) | Implementer | Done | P1 | 2026-08-06 | — | — | `.ai/tasks/DONE/TASK-0210-coupled-backbone-rotamer-search-hardness.md` |
| TASK-0211 | **Thread: Architect/Planner (synthesis). Done, 2026-08-12.** Built `ensemble_contact_covariance` (per-residue \|Pearson\| correlation between the seed's and residue *j*'s per-sample contact-graph degree, across a legal closed-form ANM-equipartition ensemble — genuinely rebuilt discrete graphs per sample, unlike `dcc_low`'s single-eigendecomposition construction) and tested it for independence against TASK-0199's 28-observable matrix. MSF gate and split-half reproducibility (rho 0.72-0.79) passed on all 5 targets; baseline rank reproduced TASK-0199's own numbers exactly. **Verdict: inside the existing span, 5/5 targets — not a new axis.** Pre-registered rule (real delta must exceed a matched-variance noise column's own delta) failed on every target (delta_real 0.115-0.221 < delta_noise 0.180-0.273); rho vs. `dcc_low` alone is 0.257-0.339 on every target, flat across targets — real, modest overlap explaining the non-independence rather than a bare null. TASK-0199's "~3 axes" headline (refined by TASK-0207) is unweakened. Zero multiplicity budget consumed. Closes contact co-variance as a candidate; edge persistence and ensemble-averaged propagation remain untested | Architect/Planner | Done | P1 | 2026-08-06 | Architect | 2026-08-12 | `.ai/tasks/DONE/TASK-0211-ensemble-graph-observable-independence.md` |
| TASK-0212 | **Invalidated as filed, not executed (Architect, 2026-08-12).** Filed to rank targets by TASK-0208's frustration statistic (PTP1B nominally highest, +16.2%, matching HYP-S6). TASK-0208's own recompute re-keyed the guard this premise depended on: PTP1B's +16.2% was computed on `side_chain_explained=0.078`, exactly the regime the corrected guard marks `not_evaluable` — there is no PTP1B frustration number left to rank. Re-scoping needs a coupling statistic that works when `side_chain_explained` is small, which TASK-0208's recompute flags as missing entirely (new method development, not a reproducibility check) — not attempted here | unclaimed | Done | P2 | 2026-08-07 | — | — | `.ai/tasks/DONE/TASK-0212-frustration-rank-reproducibility-and-ptp1b-correlation.md` |
| TASK-0216 | **Filed retroactively, 2026-08-16, Implementer A** (work done 2026-08-13, commit `a75c802`; never given its own task file). Found `labels.functional_indices`' silent top-degree-fallback seed while scoring [[TASK-0215]]'s new pairs — **9 of 13 register targets affected**, including PTP1B (the register's one surviving positive). Fixed 6 via real `func_ligand` codes in `targets.yaml`; flagged 3 (`PTP1B`/`CASPASE1`/`CASPASE7`) as genuinely having none, active site must come from UniProt (later wired in by [[TASK-0217.003]]); made the fallback raise a `RuntimeWarning`. Scored [[TASK-0215]]'s 6 new candidate pairs: leg A (static) — `dcc_low` 0.295/0.323 on the 2 pairs with a real seed, below floor and chance; leg B (coupled search) — 0/20 on both scoreable TEM-1 pairs, weakening [[TASK-0213]]'s own 2-target CLOSED verdict (KRAS_G12C now looks like the outlier, not PTP1B: 1 of 4 easy, 3 of 4 not). **Recomputed TASK-0201's own actual statistic under the corrected seed** (not just a non-comparable whole-graph AUC) — no k survives; this was the "outstanding measurement" `RESULTS.md`'s own text named as not-yet-done, but it had already been run and simply never committed (fixed by this filing, which commits the 2 orphaned artifacts for the first time). [[TASK-0217.003]] later independently re-derived the identical result before this gap was noticed | Implementer | Done | P0 | 2026-08-13 | — | — | `.ai/tasks/DONE/TASK-0216-seed-provenance-audit-and-new-pair-scoring.md` |
| TASK-0217 | **Thread: Implementer A (closing synthesis). Done, 2026-08-14.** Parent of the construct-validity sweep family (.001–.004, all Done). Diagnosis that motivated it: 6 corrections in the 2026-08-06→13 window all belonged to one class — sound statistics, wrong construct. **Combined: 47 items audited across the family (15 fallback branches, 13 pre-registered criteria, 13 targets' seed provenance, 6 claim-bearing metrics); 3 genuinely new findings** (the .001 array-correspondence bug; .002's latent `fpocket_conditional_analysis.py` reachability margin, fixed before it could fire; .004's `degree_centrality`-never-binds suspicion) **plus 4 pre-existing defects consolidated under this family's own naming for the first time.** Predictions scored honestly, as pre-registered: "nothing new in the statistical layer" held (confirmed clean throughout); "little change to conclusions" held for 9/10 — **except the one case its own text explicitly named as the risk**: the register's one positive (PTP1B `dcc_low`) did not survive its own seed correction (.003). **The register's honest positive count is now zero — the single material conclusion this entire sweep changed.** Both Open Questions resolved: the effective-rank/shared-seed interaction is confirmed (not just plausible), and the convention amendment landed in `.002` per the parent's own recommendation. Advisories already propagated to `RESULTS.md` and the Phase-1 submission draft, not left as a forward pointer | Architect/Planner | Done | P0 | 2026-08-13 | — | — | `.ai/tasks/DONE/TASK-0217-construct-validity-sweep.md` |
| TASK-0217.001 | **Thread: Implementer A. Done, 2026-08-14.** Subtask of [[TASK-0217]] (construct-validity sweep). Confirmed and fixed a live array-correspondence bug in `labels.functional_indices`: holo-space heavy-atom contact indices were used directly as apo-space indices in `build_labels` (and 3 other call sites) whenever apo/holo numbering or length differ — **10 of 13 real targets exposed**, including 2 of 3 mandatory (KRAS_G12C, BCR_ABL1). Confirmed wrong with real numbers (KRAS_G12C's "active site" residue 12 should have been 11) before fixing. **Consequence beyond the CTQW seed**: `active_site` also gates the SEAM-0003 pocket-label exclusion, so the register's ground-truth `pocket` label itself shifted on the same 10 targets — KRAS_G12C's pocket went 18→17 residues, and `test_fpocket_pin.py`'s own golden AUC moved 0.7910→0.8210 (real, not TASK-0206's binary-drift class; marked `xfail` with a dated reason, not silently re-pinned — re-scoring is an explicitly out-of-scope follow-up). Fix is additive (`heavy_atom_resnames`/`coords_resnames`, opt-in translation via the same Needleman-Wunsch path `holo_pocket_mask` already used); backward-compatible same-structure usage untouched. 15 fallback/degradation branches swept register-wide; 2 unsafe (both now fixed/loud), 13 safe, denominator reported. Suite green (1158 passed + this task's own 2 new tests, 4 pre-existing failures in another thread's untracked `test_reduce.py` unrelated). Flagged, not chased: TASK-0217's own "9 of 13" fallback count doesn't match this sweep's direct "3 of 13" measurement | Implementer | Done | P0 | 2026-08-14 | — | — | `.ai/tasks/DONE/TASK-0217.001-silent-degradation-paths.md` |
| TASK-0217.002 | **Thread: Implementer B. Done.** Subtask of [[TASK-0217]] — pre-registered criteria that cannot return one of their own outcomes. Retrospective test passed (re-derived all 3 known reachability defects — [[TASK-0204]] D1, [[TASK-0208]] V1, [[TASK-0189]] — plus [[TASK-0204]] D2's missing control, from their pre-registrations alone). **13 pre-registered criteria audited (every "Pre-Registered" section header, 2026-08-02 through 2026-08-13): 3 known reachability defects + 1 known missing control, all already fixed; 1 NEW finding; 8 clean.** New finding: `scripts/fpocket_conditional_analysis.py`'s `ALPHA` gate is currently reachable (confirmed by calling the guard directly) but sits at only ~60% of the permutation floor and shrinks as `TARGETS` grows — a latent instance of [[TASK-0189]]'s own defect, fixed pre-emptively with `assert_gate_reachable` before any verdict was affected. Convention amendment landed in `.ai/reference/INTENT_CONTRACT_TEMPLATE.md`'s own Planned Validation section (state the positive control's expected result; demonstrate each verdict is reachable), checked against all 4 known instances | Implementer | Done | P0 | 2026-08-14 | — | — | `.ai/tasks/DONE/TASK-0217.002-criterion-reachability-and-controls.md` |
| TASK-0217.003 | **Thread: Implementer A. Done, 2026-08-14.** Subtask of [[TASK-0217]], continuing [[TASK-0216]]'s seed-provenance audit. **Post-fix fallback count: 3 of 13** (`PTP1B`/`CASPASE1`/`CASPASE7`, no functional ligand in holo at all) — reconciles [[TASK-0217.001]]'s own independently-measured 3/13 against TASK-0216's original pre-fix 9/13. **PTP1B's corrected `dcc_low` result independently re-derived twice** (own fresh code, not just re-running TASK-0216's script): AUC 1.000→0.598, p 0.00275→0.567 against the 0.05/16 bar — **no k survives. The register's one surviving positive does not survive its own seed correction.** Found and resolved a real discrepancy between TASK-0216's own two scripts en route (one silently used a weaker Cα-only pocket when apo/holo lengths differ — PTP1B's do — the other correctly used full heavy atoms; the 14-residue/0.598 number is the comparable one). **UniProt-before-top-degree: decided yes and implemented, not just recorded** — new `active_site_uniprot` curated field in `targets.yaml` (ported as data, not imported from `backend/`, per TASK-0018) + a new tier in `functional_indices`; all 3 targets now resolve the real catalytic site with zero warnings, closing the fallback class permanently. Additive advisories written in `RESULTS.md` and `documentation/PHASE1_SUBMISSION_DRAFT.md` (§2.4 rewritten: one weak positive → zero surviving positives, framed as a second self-auditing-strengthens-the-narrative instance alongside Finding 4). Suite green throughout | Implementer | Done | P1 | 2026-08-14 | — | — | `.ai/tasks/DONE/TASK-0217.003-seed-anchor-provenance.md` |
| TASK-0217.004 | **Thread: Implementer A. Done, 2026-08-14.** Subtask of [[TASK-0217]] — the analytical class: metrics that are correctly computed and statistically sound but measure something other than their name asserts. Retrospective test passed (rediscovered TASK-0208 V3's carriage confound from `backbone_explained`'s definition alone). **6 claim-bearing metrics audited, 4 flagged, 2 clean.** New finding: across the 3 targets with a published 3-leg proximity-floor breakdown, `degree_centrality` never binds the `max()` — `euclid`/`hop` alternate — labelled a **suspicion** (no verdict changes), not a confirmed defect, per this task's own labelling discipline. Consolidated 3 pre-existing confirmed instances under this class's name for the first time (TASK-0204 D3's overlap-dominated hit criterion, TASK-0208 V3's `backbone_explained`, TASK-0208 V1's clash-dominated "frustration" gap). Verified clean, not assumed: the stratified well-powered-max AUC's null replicates the identical max-across-shells selection on every permutation draw (no look-elsewhere leak), and `effective_rank`'s Spearman-based construction is scale-free with the causal-vs-correlational distinction already stated in its own prior write-ups. One-paragraph advisory added to `RESULTS.md`; no verdicts changed | Architect/Planner | Done | P2 | 2026-08-14 | — | — | `.ai/tasks/DONE/TASK-0217.004-metric-construct-audit.md` |
| TASK-0219 | `backend/systems.py`'s GLUCOKINASE `chain="X"` is wrong for the apo structure (1V4S only has chain A) — confirmed live, `data_layer.load_structure("1V4S","X")` returns `None` on the currently-shipped config. Same apo/holo chain mismatch `__WORK_IN_PROGRESS__/src/allostery/clean.py` already fixed via TASK-0127 (`apo_chains`/`holo_chains` override), apparently never ported to this live backend. Found incidentally during [[TASK-0033]]'s validation | Implementer | Done | P1 | 2026-08-16 | — | — | `.ai/tasks/DONE/TASK-0219-glucokinase-apo-chain-mismatch.md` |
| TASK-0220 | `quantum_seed_readiness`'s `SAFE` verdict never fires across all 5 currently-loadable `verified=True` `systems.py` targets (4x PARTIAL, 1x RISKY) — `distal_enrich` sits in a tight 0.775-0.948 band on every target, never once above the 1.2 `SAFE` bar. Is the threshold miscalibrated against real protein data, or is `PARTIAL`-as-ceiling the honest real answer? Distinct from [[TASK-0033]] (which confirmed the fixed-vs-relative *contract* is correct, not whether the fixed *values* are calibrated) — found incidentally during that task's validation | Implementer | Done | P2 | 2026-08-16 | — | — | `.ai/tasks/DONE/TASK-0220-seed-readiness-safe-verdict-unreachable.md` |
| TASK-0221 | Three items only Bartosz can unblock: (1) the two-phase description + organisational document — **RESOLVED 2026-08-19**, both load-bearing [[TASK-0184]] premises (weights, "Phase 1 = ideation") confirmed; (2) AWS Braket/Classiq accounts — **RESOLVED 2026-08-25**, organisers confirmed access is Phase-2-only, "No access is required for Phase 1 submissions," moot rather than a gap ([[TASK-0182]], `POC_SPRINT_PLAN.md` addenda); (3) organiser questions (a)-(e) sent, their "points 2 through 5" forwarded to the Cleveland Clinic team, **answer pending**, mapping to (a)-(e) not yet recorded. Filed by Reviewer-thread (Opus), recovered uncommitted from the working tree by Implementer D alongside [[TASK-0222]] — content unchanged, not authored by this row's committer | Bartosz | TODO | P0 (item 3) | 2026-08-16 | — | — | `.ai/tasks/TODO/TASK-0221-bartosz-actions-docs-accounts-organizer-questions.md` |
| TASK-0222 | Table 1 mandates Cardiac Myosin's 5TBY→6C1H; this register uses 8QYP→8QYR (TASK-0124's substitution). Run and disclose **both** — resolved: mandated pair cannot be scored at all under either methodology tried (standard AUC pipeline; TASK-0209's VALID-rule ladder), both failing independently at pocket-label derivation. Live RCSB re-verification found a stronger defect than previously on record: 6C1H isn't just missing mavacamten, its myosin entity is "Unconventional myosin-Ib" (rat) — not beta-cardiac myosin (MYH7) at all. Incumbent pair wiring-check reproduces cleanly (`NO_SIGNAL_IN_APO`). New additive `CARDIAC_MYOSIN_TABLE1` config entry; `RESULTS.md` row 75 | Implementer | Done | P1 | 2026-08-19 | — | — | `.ai/tasks/DONE/TASK-0222-cardiac-myosin-both-pairs.md` |
| TASK-0229 | **Thread: Architect (closing synthesis, 2026-08-24).** Parent (subtasks .001-.007, all Done 2026-08-21/23) — a systematic extraction of testable hypotheses from the challenge's own bibliography. All 7 solid; the parent itself sat unclosed until an audit found half its own Dependency unfulfilled: `.006` (Ensemble Allosteric Model/COREX — real mixed result, ρ=0.542/0.493 vs. propagation, plus a type-correct Gibbs-sampling quantum target) never reached `PHASE1_SUBMISSION_DRAFT.md`, and none of `.004`/`.005`/`.006` reached `POC_SPRINT_PLAN.md`, which had already shipped and closed ([[TASK-0183]]) before `.006` even ran. **Fixed today, honestly post-dated, not backfilled**: new Finding 7 + §3(d) cross-reference in the submission draft, a dated addendum section in the sprint plan (named as a candidate, not folded into any committed milestone), `RESULTS.md` row 81 | Architect/Planner (parent), Implementer (subtasks) | Done | mixed | 2026-08-24 | — | — | `.ai/tasks/DONE/TASK-0229-reference-hypothesis-family.md` |
| TASK-0229.001 | Ref [9] (Gunasekaran, Ma & Nussinov 2004, *Proteins* 57:433 — verified directly) argues no clean class of "non-allosteric" surface sites exists, attacking the negative class every AUC in this register assumes — resolved: alternative metrics (new `metrics.rank_of_known_site` + existing `enrichment_at_k`) don't rescue the result, they sharpen it (`enrichment@5=0.00` on all 3 mandatory targets' headline cells); the real consequence is on interpretation, not arithmetic — TASK-0161/TASK-0199's zero-confirmed-positives count stands, but must now be phrased as "no robust signal found," not "no signal exists." Limitations text written directly into `documentation/PHASE1_SUBMISSION_DRAFT.md` §2.5; `RESULTS.md` row 76 | Implementer | Done | P0 | 2026-08-21 | — | — | `.ai/tasks/DONE/TASK-0229.001-negative-class-validity-and-metrics.md` |
| TASK-0229.002 | Refs [10] (Mitarai & Fujii 2021, circuit cutting) / [11] (Oh et al. 2024, SVD/dilation, directly on-topic — FMO exciton transport) — both verified directly; do either change [[TASK-0182]]'s `FAULT_TOLERANT_ONLY` verdict? Resolved: [10] reinforces it — real, measured per-target cut counts (not assumed) give sampling overhead 10²²⁴-10⁴⁸⁷, 15-400+ orders of magnitude past the observable universe's atom count. [11] is the one route that clears the qubit-count bar, honestly caveated: needs an amplitude-encoded register (~9-10 system qubits +1 ancilla, ~10⁵-10⁶ gates) this project has never built or costed — a real unbuilt option, not a contradiction of TASK-0182. Also fixed: TASK-0182's own resource verdict had never been cited in `documentation/PHASE1_SUBMISSION_DRAFT.md` before this task — corrected in the same pass (new §3(d)); `RESULTS.md` row 77 | Implementer | Done | P0 | 2026-08-22 | — | — | `.ai/tasks/DONE/TASK-0229.002-quantum-hardware-story-cutting-and-dilation.md` |
| TASK-0229.003 | Ref [6] (Tsai & Nussinov 2014, effector-specificity) — does ASD list additional allosteric sites beyond our single incumbent site per target, undercounting our own false positives? Live ASD lookup (worked around the site's own expired TLS cert with `curl -k`, reverse-engineered its legacy Ext.js/JBoss AJAX API — documented in the new script for reuse). **3/4 targets (KRAS_G12C, CARDIAC_MYOSIN, PTP1B) confirmed single-site, matching our incumbent key.** BCR_ABL1 has a real second site: PDB 5DC4, a monobody at the SH2-kinase interface — computed 24 real contact residues (143-239) directly from the structure, confirmed disjoint from our active site, our incumbent myristoyl pocket, and our own currently-reported top-5 hit list (no correction needed to anything already published). Full whole-protein re-score against the union key not done (needs a pipeline re-run, outside "cheap" scope) — flagged, not silently closed. **HYP-P13 cross-reference added** (this task's own Context section) on direct user instruction — this result is consistent with but does not confirm HYP-P13's own ensemble mechanism | Implementer C | Done | P1 | 2026-08-22 | — | — | `.ai/tasks/DONE/TASK-0229.003-asd-multi-site-answer-key-audit.md` |
| TASK-0229.004 | Ref [1] (Zheng 2023, the challenge's own reference #1 — NMA-guided conformational sampling) implemented faithfully as a scored classical baseline for the first time, on [[TASK-0209]]'s VALID targets. Validated on BCR_ABL1 first (92.5% vs 36.7% real/decoy specificity, passed). KRAS_G12C: AUC 0.728 vs. floor 0.530 — beats this register's own quantum-observable point estimate (0.557-0.590) by a wide margin; null p=0.024 clears a naive local bar but is ~3 orders of magnitude short of the register's own multiplicity bar (not counted as significant). PTP1B: beats floor (0.610 vs 0.451) but Zheng's own preferred pocket-level statistic disagrees with the residue-level AUC — disclosed, not resolved either way. Written into `documentation/PHASE1_SUBMISSION_DRAFT.md` as new Finding 5 (matches Finding 3's "classical method beats quantum observable" pattern); `RESULTS.md` row 78. **Correction 2026-08-23**: the Finding-5 write-up above never actually landed in the submission draft (shared-file-collision bug in this task's own commit); recovered in [[TASK-0229.005]]'s own commit, numbers unaffected | Implementer | Done | P1 | 2026-08-23 | — | — | `.ai/tasks/DONE/TASK-0229.004-zheng-nma-sampling-baseline.md` |
| TASK-0229.005 | Refs [1]+[2] stitched (NMA sampling → persistent homology, the register's own "highest-value construction") — revives the H2 persistent-void arm ([[TASK-0142]]) with an ensemble version and a positive control it never had. **Resolved: fails its own cheapest gate on both VALID targets, decisively.** `void_score` on the already-open HOLO structure (no sampling needed): KRAS_G12C AUC 0.557 (top H2 persistence 0.812, floor 2.5), PTP1B AUC 0.116 — anti-correlated (top H2 1.535) — not a filtration-cap artifact (stable thresh=16-30). Apo-ensemble scoring gated off per-target on this failure; run anyway as an ungated diagnostic (AUC 0.521/0.487, both near chance, null p=0.63). New `allostery.persistent_voids.ensemble_void_score`; real bug found+fixed (infinite-lifetime persistence classes were corrupting the shell-kernel score with NaN — fixed via right-censoring, checked for and found no retroactive impact on [[TASK-0142]]'s own published numbers). Decisive on PTP1B where [[TASK-0143]]'s graph-openness proxy was INFEASIBLE (no null constructible) — different observable, first decisive answer either way. Written into `documentation/PHASE1_SUBMISSION_DRAFT.md` as new Finding 6, alongside the recovered Finding 5; `RESULTS.md` row 79 | Implementer | Done | P1 | 2026-08-23 | — | — | `.ai/tasks/DONE/TASK-0229.005-nma-sampling-plus-persistent-homology.md` |
| TASK-0229.006 | Ref [4] (Motlagh, Wrabl, Li & Hilser 2014, Ensemble Allosteric Model) — the largest genuinely untested item in the bibliography: does the genuine (binary folded/unfolded, nonlinear) EAM diverge from a propagation-based picture, where the already-tested harmonic proxy did not (|partial ρ|=0.773, confounded)? New `allostery/corex.py`, a real COREX-style implementation (sliding-window folding units, ASA via BioPython's validated ShrakeRupley, a stated, simplified two-term ΔG model — Eisenberg & McLachlan 1986 apolar-burial coefficient + Tien et al. 2013 max-ASA reference, both bibliographically verified, digits not re-OCR'd against paywalled tables). **Sanity check passes on both TASK-0209-VALID targets** (buried residues significantly more stable than exposed, ρ=-0.365/-0.298, p<2e-6 both). **Decisive negative control: neither clean closure.** EAM-vs-propagation-ranking Spearman ρ=0.542 (KRAS_G12C) / 0.493 (PTP1B) — real, significant, substantial agreement, but far short of the ~0.85-0.95 this register's other proximity-confounded observables show, meaning ~70-75% of EAM's own ranking variance is unexplained by hop-distance alone. A genuinely mixed result, reported as such. HYP-P13 cross-referenced (this task is its own named decisive test) — result is inconclusive for HYP-P13, not decisive either way. c-Myc/1NKP correctly excluded (unvalidatable, no holo structure) | Implementer C | Done | P1 | 2026-08-23 | — | — | `.ai/tasks/DONE/TASK-0229.006-ensemble-allosteric-model-corex.md` |
| TASK-0229.007 | Refs [15]+[7] (Das et al. 2014 two-state ANM; Stock & Hamm 2018 non-equilibrium transport). (a) Whole-structure CO(50) two-state ANM capture score, all 7 of [[TASK-0209]]'s real-drug-ligand targets — **Planned Validation FAILS** (KRAS_G12C 0.766 < BCR_ABL1 0.858; whole-structure CO confounds with size/flexibility, not TASK-0209's rule). Pocket-restricted variant built in response: **PASSES the pre-registered comparison** (0.547 > 0.316) but not a clean 7-target separation (GLUCOKINASE, INVALID, scores 0.676, above both VALID targets); CASPASE7 unscoreable (zero pocket/alignment correspondence, a real data gap). (b) New `allostery/impulse_response.py` — closed-form finite-time GNM dynamic cross-correlation, `C(t=0)` verified exact vs. the register's own existing static covariance. `peak_time` found degenerate (67% zero on KRAS_G12C); `integrated_response` used as primary observable. **Independence test (reusing [[TASK-0199]]'s pipeline + [[TASK-0211]]'s own decision rule): `new_axis=False` on all 5 of TASK-0199's targets** — delta_noise exceeds delta_real every time — a clean negative, not scored against labels, matching TASK-0211's own precedent. Confirmed (not assumed) 4 unrelated full-suite test failures trace to concurrent `IN_PROGRESS` [[TASK-0231]] work on `labels.py`, untouched by this task | Implementer C | Done | P2 | 2026-08-23 | — | — | `.ai/tasks/DONE/TASK-0229.007-two-state-anm-and-nonequilibrium-transport.md` |
| TASK-0237 | **Thread: Architect.** Collaborator-provided `.ai/reviews/2026-08-23 - hamiltonians/hamiltonians.py` — fidelity-checked line-by-line against this repo's own `hamiltonians.py`/`potentials.py` first (identical formulas/defaults, including a carried-over caveat comment; no bug found). **Scope narrowed same day to H14 only: does `H14_anm_pinv` help CTQW score better, checkable via this project's own methods?** Since the collaborator's H14 is formula-identical to this repo's own `H14_anm_pinv_trace`, [[TASK-0138]]'s own 2026-07-19 ceiling-search + permutation-null + trial-density scrutiny already answers it directly, no new run needed: **no** — CARDIAC_MYOSIN's ceiling is below its own floor (0th percentile null); KRAS_G12C's ceiling trails `H_new`'s; BCR_ABL1's ceiling does beat `H_new`'s (+0.050) but the margin sits at only the 79th null percentile (p=0.21 uncorrected) — not significant on any target. A ceiling result (answer key in hand); the blind actual score would be equal or worse. The broader collaborator-notebook question (other operators, still blocked on missing cells) is unaffected, not reopened here | Implementer | Done | P1 | 2026-08-23 | — | — | `.ai/tasks/DONE/TASK-0237-collaborator-hamiltonians-cell-validation.md` |
| TASK-0236 | DOI-verify [[TASK-0233]]'s two per-target mechanism claims (asciminib/myristoyl-pocket autoinhibition for BCR_ABL1; mavacamten/SRX-state stabilization for CARDIAC_MYOSIN), used as field-standard background without a checked citation. **Both verify, both stand as stated — no correction needed.** Asciminib: Wylie et al. 2017, *Nature* 543:733-737, doi:10.1038/nature21702 — confirms the myristoyl-pocket mechanism specifically (fragment-based NMR + conformational-NMR assay), not just the drug name. Mavacamten: Rohde et al. 2018, *PNAS* 115:E7486-E7494, doi:10.1073/pnas.1720342115 — confirms the SRX-state-stabilization mechanism directly. Both verified via Crossref structured metadata (not only search snippets), added to `REFERENCES.md`'s method/tool table; [[TASK-0233]]'s own Open Questions entry marked resolved with a dated note. Picked up as a stale claim (>1 day, zero progress, no matching active peer session, no COMMON.md trace) — force-claimed per protocol | Implementer C | Done | P2 | 2026-08-24 | — | — | `.ai/tasks/DONE/TASK-0236-verify-asciminib-mavacamten-mechanism-citations.md` |
| TASK-0241 | **Thread: Reviewer-filed, Implementer C picked up.** [[TASK-0235]]'s "BCR_ABL1 4/4 decisive ceiling flip" audited and re-established properly, not discarded or re-asserted. Root cause confirmed: all 4 original trials shared one byte-identical backbone (no `--seed` flag exists on vendored EvoEF2, checked directly) — effective n=1, spread was EvoEF2/fpocket seed jitter only. **Experiment A (pure jitter, N=20/method, one fixed BCR_ABL1 backbone)**: bare-bar rate old 4/20 (20%) vs new **15/20 (75%)**, Fisher p=0.0012, real and significant — but under the register's own strict `_is_hit` (overlap AND druggability), only 1/20 (5%) old vs 3/20 (15%) new, p=0.605, **not significant**; bare druggability substantially overstates the effect. **Experiment B (real re-run, all 3 targets × both methods, N=20/arm, genuine σ=0.15Å per-trial structural perturbation)**: BCR_ABL1 `_is_hit` 1/20 vs 4/20 (4× higher, same direction as claimed, Fisher p=0.342, not significant at this N); KRAS_G12C/CARDIAC_MYOSIN show no significant difference, matching their own prior honest characterization. **Mechanism (A)/(B) "contradiction" resolved as a false one**: `vdwrep` (clash) and `overlap_frac` (cavity shape) move independently across all 3 targets (confirmed via direct measurement, not assumed) — a method's effect on one is not evidence of its effect on the other. Net verdict: "decisive" downgraded to "real, same-direction, statistically unproven trend." [[TASK-0230]]'s Done section (Addendum 4) and `RESULTS.md` both updated with the resolved numbers, not left at the interim "contested" state. `_is_hit` recommended as this line's primary ceiling criterion going forward — no prior justification for the relaxed bar found in either task's record | Implementer C | Done | High | 2026-08-24 | — | — | `.ai/tasks/DONE/TASK-0241-task0235-ceiling-flip-does-not-reproduce.md` |
| TASK-0244 | [[TASK-0242]]'s two-stage dry-run (n=7 untuned) found CTQW and a trivial "mean BFS hops from seed" ranker tied on mean rank (5.71, Wilcoxon p=0.297) — hop reported only as a covariate, which can't settle whether CTQW does anything beyond proximity-to-seed. **Resolved: all 3 new controls point the same direction as TASK-0242's own hop-tie finding.** New `scripts/task0244_hop_competing_ranker.py`, reusing `task0242_two_stage_dryrun.run()` unchanged (extended additively with opt-in `return_state=True`; re-verified no regression — original script reproduces its own published numbers exactly). **Residual ranker** (hop partialled out of CTQW's own candidate scores): mean rank 6.29, *worse* than raw CTQW's 5.71 — hop explains real ranking power, not a nuisance covariate. **Seed-free control** (same candidates, CTQW re-run from 20 random matched-size seeds/target, no fpocket re-run): MRR collapses from 0.411 (real seed) to 0.145 (near chance) — the largest, most decisive-looking of the four controls, though p=0.109 at n=7. **Hop-matched candidate null** (±1 hop, ±50% size, within each target's own candidates): feasible 7/7 (unlike TASK-0143's own stricter null); true pocket ranks #1 in its own matched pool only 2/7. **None individually significant at n=7** (the same underpowered-sample problem TASK-0242 already flagged) — read as "current evidence favors proximity over a beyond-proximity mechanism, on every control tried," not as proof either way; apparatus built and validated, ready to re-run unchanged against [[TASK-0243]]'s future ≥12-target frozen set. `RESULTS.md` row 81 (**correction 2026-08-24, [[TASK-0253]]: this row was silently dropped from `RESULTS.md` by a later commit's own shared-file collision — recovered as row 82, findings unchanged**) | Implementer D | Done | High | 2026-08-24 | — | — | `.ai/tasks/DONE/TASK-0244-hop-as-competing-ranker-not-covariate.md` |
| TASK-0248 | `.claude/hypotheses/reference_register.md` still carried `STATUS: UNTESTED`/`PARTIAL` on hypotheses the [[TASK-0229]] family (.001-.007, all Done) had since tested with real runs — caused a reviewer to report tested hypotheses as untested to the user. **Resolved: register fully reconciled**, not patched line-by-line. New coverage-summary table (hypothesis → status → task → date) and a "how to keep this register in sync" process rule (update it in the same commit, or name the gap in the closing task's own Done section) added at the top. Per-hypothesis STATUS lines updated with precise, linked (not restated) wording — H9/H6.2/H4.1/H4.2/H1/H2.2/H7/H15/H10/H11 all move from UNTESTED/PARTIAL to TESTED or precisely-worded PARTIAL, each citing its real task and date; H6.1/H4.3/H4.4 stay UNTESTED (no task addresses them). **[[TASK-0226]] turned out load-bearing too** (outside the literal TASK-0229-family framing but directly relevant): it's the real-PDB retest of the exact confound numbers the register cites for H4/H8/H16.2, and the register was still citing the non-target-structure estimates despite the real retest existing in the repo — updated to the real numbers (H16.2 notably: 0.496±0.193 real vs. 0.292±0.218 non-target, escapes the confound *less* cleanly on real data). Also fixed a stale citation: H8/H16.2 cited "TASK-0210", which is this repo's own unrelated, already-completed task — the external drop was re-indexed to TASK-0226 on filing specifically to avoid that collision. **Genuinely open hypotheses confirmed by direct search, not inherited**: H5 (MWC/concerted transitions, zero coverage) and H16.1 (GNM-B-factor model-validity check, zero coverage — the filing task's own description of H16.1 named the wrong claim, corrected against the register's own actual text) | Implementer D | Done | High | 2026-08-24 | — | — | `.ai/tasks/DONE/TASK-0248-reference-register-status-reconciliation.md` |
| TASK-0253 | [[TASK-0249]] found `HIV_INTEGRASE_MUT871`/`916` (apo `1M9D`) have an empty active-site seed on every chain, contradicting [[TASK-0243]]'s own "zero fell back to a top-degree proxy, 20/22 uniprot" claim. Stage-1 recall (~64%) reproduces across two independent target sets — re-verify all 22 seeds, root-cause `1M9D`, decompose stage-1 failures into fpocket-miss vs. `MIN_HOP`-removal. | **Resolved: found a real, previously-undiscovered correctness bug silently corrupting committed results, not just a documentation mismatch.** New `scripts/task0253_seed_and_stage1_audit.py`. **Seed provenance corrected**: live `detect_active_site` re-run gives uniprot=18/ligand=2/none=2 (not TASK-0243's claimed 20/2/0). **`1M9D` root-caused, live-RCSB-confirmed**: it is Cyclophilin A + **HIV-1 Capsid** (P12497's own `pdbx_description`), not Integrase — curated via a shared Gag-Pol parent UniProt accession without checking domain identity against the genuinely-Integrase holo structures (`8CBS`/`8CBV`). **Verdict: drop the pair** (real replacement candidates named: 1IHV/1IHW/1K6Y/5OYM/1WJF). **Real bug found and fixed**: an empty seed made `hop_from_seed` return a constant sentinel and `time_averaged_ctqw_converged` divide by zero → all-NaN scores, silently `argsort`-ranked as if real — already baked into TASK-0243's own committed results (`HIV_INTEGRASE_MUT871` shows a fabricated `ctqw` rank of 1). Fixed at the source (`task0242_two_stage_dryrun.run()` now errors on `len(seed)==0`); corrected stage-1 recall **14/22=63.6%**, exactly matching TASK-0249's own independent number — no qualitative verdict flips on the corrected head-to-head table, now more decisive (p=0.0032 not 0.0157). **Failure decomposition: 5/6 genuine failures are `MIN_HOP` removing a real fpocket-found candidate** (one is a 100%-overlap match, removed for sitting at hop 1) — only 1/6 is a genuine fpocket miss; `MIN_HOP=2` measurably costs true-pocket detections on ~23% of the set. [[TASK-0243]]'s own Done section amended in place with both corrections. `RESULTS.md` row 83 (also recovered row 82: [[TASK-0244]]'s own row had been silently dropped by an unrelated later commit's shared-file collision, discovered incidentally during this audit) | Implementer D | Done | High | 2026-08-24 | — | — | `.ai/tasks/DONE/TASK-0253-frozen-set-seed-defect-and-stage1-recall.md` |
| TASK-0256 | The collaborating thread already reports `heat_kernel` matching `p_avg`; [[TASK-0210]]/[[TASK-0226]] tested the Markovian random walk classically — but no standing, pre-registered classical-diffusion arm exists in the two-stage apparatus itself, on the identical Hamiltonian, with PSD status honestly reported. | **Resolved: built and wired in — central finding is that a genuine classical-diffusion comparison is not achievable via this construction on real data at all.** New opt-in `include_classical=True` on `task0242_two_stage_dryrun.run()` (default False, verified byte-identical output unaffected): `propagators.ground_state_relaxation` on the same `H_new` the `ctqw` arm builds, plus per-target PSD status. **H_new is indefinite on 14/14 (100%) of TASK-0243's frozen-set scoreable targets** — `ground_state_relaxation` is never classical diffusion here (TASK-0095/P1-B), only ground-state relaxation, reported honestly, never relabelled. New `scripts/task0256_classical_diffusion_parity.py`, both designs: two-stage (ctqw mean rank 9.21/MRR 0.253 vs classical 11.50/0.221, 8/14 wins, p=0.431) and per-residue AUC (median AUC ctqw 0.564 vs classical 0.493, but ctqw wins only 6/14 targets — a disclosed tension, not smoothed over — p=0.778) — neither significant. Candidate-score parity (pre-registered ρ≥0.95 tolerance): median ρ=0.790, 14/14 "separate" beyond tolerance — but since H is never PSD, this is CTQW-vs-ground-state-relaxation divergence (expected), explicitly **not** the "quantum beats classical diffusion" finding the task's own Constraint anticipated — stated plainly, not conflated into a false positive. `haken_strobl`-at-large-gamma (the genuine PSD-independent classical-diffusion limit) checked for feasibility, found genuinely infeasible at this frozen set's real sizes (up to N=1223; 18.1s for gamma=200 at N=228 alone) — a costed, flagged follow-up, not silently skipped. Proposed for the (still-nonexistent) joint pre-registration: both propagators, not either alone. `RESULTS.md` row 84 | Implementer D | Done | Medium | 2026-08-25 | — | — | `.ai/tasks/DONE/TASK-0256-classical-diffusion-parity-preregistered-arm.md` |
| TASK-0260 | [[TASK-0259]] found the "unexplained" share in [[TASK-0254]]'s Shapley attribution is almost entirely apo crypticity (ρ=−0.771). Does a purpose-built cryptic-pocket predictor close it, or is something genuinely unmodelled? 4 candidates: PocketMiner, CryptoSite, P2Rank, FTMap/FTSite. | **Resolved: the strongest predictor we could install does not close the residual and behaves exactly like fpocket, not a cryptic specialist.** All 4 citations verified live via Crossref (PocketMiner: Meller et al. 2023 Nat. Commun., DOI 10.1038/s41467-023-36699-3; CryptoSite: Cimermancic et al. 2016 JMB, DOI 10.1016/j.jmb.2016.01.029; P2Rank: Krivák & Hoksza 2018 J. Cheminform., DOI 10.1186/s13321-018-0285-8; FTMap/FTSite: Kozakov et al. 2015 Nat. Protoc., DOI 10.1038/nprot.2015.043). **Constraint-3 ruling** (MD as an input we supply vs. a tool's own training provenance): P2Rank legal (no MD anywhere); FTMap/FTSite legal but web-server-only, excluded on installability; CryptoSite's full model runs its own MD-based AllosMod sampling *at inference* — excluded (qualitatively different from training-only MD, not a spectrum); PocketMiner reads legal (MD only in training, zero at our runtime) but is the closest call — flagged to organisers, [[TASK-0221]] item (f), not assumed. **Only P2Rank installed/run** (Java, prebuilt, no MD, unambiguous). PocketMiner attempted twice (plain + explicit OpenSSL flags via Homebrew), both times failed the same documented way (`_ssl` won't compile — CPython 3.9 predates OpenSSL 3.x support and `openssl@1.1` is removed from Homebrew) — a real, costed follow-up, not skipped. **P2Rank as a 4th Shapley block**: median share +9% (n=20, p=0.007) / +10% (n=13 clustered per [[TASK-0261]], p=0.021) — real but unexplained only moves 29%→27%/26%. **The decision statistic this task's own Scope specifies — added *last* on top of geometry+fpocket+CTQW — is ~0% and not significant** (p=0.881): once fpocket is in the model, p2rank adds essentially nothing further. **Crypticity-stratified, the pre-registered prediction fails cleanly**: p2rank's gain concentrates on already-open targets (median +30%) not cryptic ones (+8%) — same pattern as fpocket, the opposite of what a genuine cryptic-opening specialist would show. PocketMiner — the one method actually trained to predict opening — remains untested, for a documented environment reason. `RESULTS.md` rows 84 (recovered — found TASK-0256's own row silently dropped by a self-inflicted git-add-sequencing bug during this task, root-caused and fixed) + 85; `documentation/CTQW_CONTRIBUTION_BRIEF.html` §08/§09 updated | Implementer D | Done | Highest | 2026-08-25 | — | — | `.ai/tasks/DONE/TASK-0260-cryptic-pocket-predictors-against-the-residual.md` |
| TASK-0266 | Three independent looks ([[TASK-0259]], [[TASK-0260]]) show CTQW is the only Shapley block leaning toward cryptic targets — not significant, but consistent. Confound: buried=closed=cryptic; the geometry block's own burial proxy (`degree_centrality`) is worse than real SASA ([[TASK-0257]] R2), and SASA is in no block at all. Does the lean survive with real SASA controlled for? | **Resolved: shrinks by more than half, and was never significant to begin with.** New `scripts/task0266_sasa_burial_control.py`, real per-residue SASA (`Bio.PDB.SASA.ShrakeRupley`) added as an independent 4th Shapley block (geometry/fpocket/SASA/CTQW). Overall (n=20): SASA adds essentially nothing alone (Shapley median −0.4%, added-last −0.0%); unexplained 28.6%→27.2%, matching TASK-0260's own P2Rank finding in magnitude. Cluster-robust (13 clusters, [[TASK-0261]]'s exact permutation): none of the aggregate marginals were ever significant. **A real data wrinkle found and handled**: crypticity is per-ligand, clustering is per-apo-structure — `TRP_SYNTHASE_F6F`/`F19` share an apo structure but straddle the 80% crypticity bar on opposite sides; excluded from the stratified test only, kept everywhere else. **Central result, crypticity-stratified, cluster-robust**: CTQW added-last cryptic-vs-open gap goes from +5.53% vs. −0.10% (p=0.0736, no SASA) to +2.17% vs. +0.01% (p=0.2392, with SASA) — gap roughly halves, already-marginal significance weakens further. SASA's own Shapley share leans cryptic the same direction (p=0.311, not itself significant) — partial direct confirmation of the confound. Verdict: shrinks substantially, not a clean survive-or-vanish, but essential context is p=0.0736 uncontrolled never cleared even an uncorrected 0.05 bar. **Recommendation: [[TASK-0267]] (crypticity-matched null) not worth running** — no significant signal left to protect. `RESULTS.md` row 86 | Implementer D | Done | High | 2026-08-25 | — | — | `.ai/tasks/DONE/TASK-0266-burial-control-on-ctqw-cryptic-lean.md` |
| TASK-0267 | Gated on [[TASK-0266]]'s own verdict: only build the crypticity-matched null if the cryptic lean survives burial control; if burial explains it, redirect effort to [[TASK-0269]] instead. | **Resolved: not built, not run — gated off per this task's own Constraint.** [[TASK-0266]]'s own recommendation checked directly (not assumed): CTQW's crypticity-stratified, cluster-robust added-last lean was p=0.0736 uncontrolled (never clearing an uncorrected 0.05 bar) and p=0.2392 with SASA controlled — never significant either way. Explicitly considered and rejected reading the Constraint's "survives" literally by sign alone (a small positive residual did remain, +2.17% vs +0.01%) — the Constraint's own accompanying text ("a signal already known not to exist") gates on significance, not direction; a null built around a p=0.24 residual with no prior significance would characterise noise, not isolate a confound. Both of this task's own structural limits (candidate-based nulls are impossible here since cryptic pockets are absent from fpocket's own candidate list by construction, [[TASK-0253]]; decoys may not be true negatives per H9/[[TASK-0229.001]]) recorded for the record regardless, in case the gating condition ever flips on a future re-measurement. Effort redirected to [[TASK-0269]] (PocketMiner environment + run), already filed to a different implementer thread | Implementer D | Done | Medium | 2026-08-26 | — | — | `.ai/tasks/DONE/TASK-0267-crypticity-matched-null.md` |
| TASK-0239 | [[TASK-0238]] Leg B1 found the register's published floor/actual/margin triplets stale — [[TASK-0217.001]] fixed a live `labels.functional_indices` bug and re-pinned fpocket's own AUC but never refreshed the quantum floor/actual triplet cited as current across `EXECUTION_PLAN.md`, `ALGORITHM_REGISTER.md`, `COMPETENCE_MAP.md`, and `documentation/PHASE1_SUBMISSION_DRAFT.md:146`. **Resolved: fresh `run_challenge.py` (commit 821dbfb) independently reproduces TASK-0238's numbers to 3-4 decimals; all 62 citations (56→62, grew mid-task) classified historical (left, per `RESULTS.md`'s own no-overwrite convention) vs. live (corrected).** `PHASE1_SUBMISSION_DRAFT.md` Finding 3 corrected in place (the one document reporting these as current, not historical) + banner + a fixed §2.5 footnote (its own "TASK-0177 drift" attribution was incomplete — TASK-0217.001 was the dominant cause). `COMPETENCE_MAP.md`: 3 new CAVEAT blocks in its own established stacked-layer convention. `ALGORITHM_REGISTER.md`/`EXECUTION_PLAN.md`: superseding annotations alongside the existing TASK-0206 one. BCR_ABL1/CARDIAC_MYOSIN both flip sign (below-floor → above-floor); their `NO_SIGNAL_IN_APO` diagnosis now correctly attributed to the chance bar, not the floor gate, everywhere this was stated. Second inconsistency (CARDIAC_MYOSIN 0.7921 vs 0.5679) resolved as **not an error** — two legitimate historical vintages either side of TASK-0124's structural re-anchor. Guard proposed (pinned-golden triplet regression test, mirroring TASK-0206's fpocket pin), not built. Handled mid-task: HEAD moved 6 commits from other threads (shared working tree) — re-verified none touched the floor/actual path; RESULTS.md's Leg B1 section (initially another thread's uncommitted work) was independently committed before this task's own close, re-based row 80 onto the final clean HEAD; `PHASE1_SUBMISSION_DRAFT.md`'s own concurrent §2.3b addition (still uncommitted) preserved via surgical splice, not touched. `RESULTS.md` row 80 | Implementer D | Done | High | 2026-08-24 | — | — | `.ai/tasks/DONE/TASK-0239-stale-floor-triplets-across-five-documents.md` |
| TASK-0243 | Curate ≥12 (target ~19) new untuned apo/holo pairs for the joint two-stage experiment — [[TASK-0242]]'s own n=7 was too small to resolve anything (effect weakened as sample grew, Fisher p 0.053→0.164). Reused [[TASK-0215]]'s own live-RCSB VALID-rule pipeline, widened (deeper pagination + 5 new class-targeted query terms): 376 screened, 28 raw-valid, **6 excluded on direct inspection** (DTT/G3H misclassified as "drug" — lab reagent/metabolite; 2 eIF4E entries where the automated picker chose the orthosteric cap-analog cofactor over the real allosteric compound, re-verified to fail the VALID rule under the correct ligand) — same TASK-0169-style diligence, not a literature table trusted blindly. **22 pairs frozen** (`config/candidate_targets_task0243.yaml`), 14 distinct proteins incl. real approved-drug mechanisms (pyruvate kinase/mitapivat, MKK7/ibrutinib), 20/22 active-site seeds UniProt-sourced, 0/22 top-degree fallback. Curation-order log proves the freeze predates any score. **Stage-1 recall 16/22=72.7%** (found+fixed a real live bug along the way: TASK-0242's own script inherits [[TASK-0039]]'s exact altloc defect, patched locally in this task's own wrapper, not at TASK-0242's own source; the other 21 targets' numbers were bit-for-bit unchanged after the fix). **Full 5-ranker re-run: TASK-0242's own "CTQW leads every control" finding does NOT survive** — ctqw vs fpocket_drug reverses direction (5-9-2, not significant); ctqw vs hop_covariate still not significant (8-5-3); ctqw vs random is the one Bonferroni-surviving significant result (12-1-3, p=0.0157) — the weakest, least informative comparison of the three. Reported exactly as it landed per this task's own pre-registered Constraint. `RESULTS.md` continuation added directly after TASK-0242's own section. **Correction 2026-08-24 ([[TASK-0253]]): the 16/22=72.7% recall and its own Wilcoxon table above are contaminated** — 2 of the 16 "survivors" (`HIV_INTEGRASE_MUT871`/`916`) had an empty seed and a silently-fabricated `ctqw` rank (a real bug, since fixed at the source); real recall is 14/22=63.6%, matching TASK-0249's own independent number. No qualitative verdict flips on the corrected n=14, but the exact win/loss counts and p-values above should not be cited — see this task's own amended Done section. The "20/22 uniprot, zero fell back to top-degree" seed-provenance claim (not quoted in this row, but in this task's own Done section) is also wrong — real distribution is 18/2/2, see [[TASK-0253]] | Implementer C | Done | High | 2026-08-24 | — | — | `.ai/tasks/DONE/TASK-0243-untuned-target-curation-for-joint-two-stage-experiment.md` |
| TASK-0252 | The last 3 genuinely-open register entries no task addressed: H6.1, H4.3, H4.4. Citations re-verified live (Crossref). **H4.4 CLOSED ON ARGUMENT**: partition-function-derived coupling is a nonlinear transform of microstate energies, non-separable into pairwise graph-edge terms except when sites are independent (uncoupled) — consistent with [[TASK-0229.006]]'s own ρ≈0.5 (partial, not full) EAM-vs-propagation overlap. **H6.1 MERGED**: its "pathways are a high-flux subset, not causal channels" clause is empirically indistinguishable from H4.1 (already TESTED, same ρ≈0.5 signature) given this project's own inputs — folded in, not carried as a duplicate; its "pre-existing landscape" clause overlaps H5.1, independently folded into [[HYP-P13]] by [[TASK-0251]] the same day. **H4.3 UNDECIDABLE WITH CURRENT TOOLING — a real methodological finding, not a non-result**: built a real test reusing [[TASK-0229.006]]'s own validated κ_f/coupling machinery, found a strong, correctly-signed, distance-robust correlation (ρ=-0.83 to -0.94, both VALID targets) — but a decisive random-reference-site control shows the SAME strength regardless of which residues serve as "the active site," proving the existing COREX coupling metric can't currently distinguish real-site coupling from arbitrary-site coupling (disorder variation itself confirmed real via B-factors, so not a power problem). Added as a 5th HYP-P13 evidence row (`physics.md`) — the same "site-referencing quantity collapses to a site-independent property" signature recurring in a new observable class. New §2.3c in `PHASE1_SUBMISSION_DRAFT.md` reframes the project's own repeated CTQW/graph-observable negatives ([[TASK-0245]], [[TASK-0247]]) as consistent with, not merely undefeated by, the challenge's own cited literature. Register STATUS updated for all three; a stale self-contradictory leftover sentence in the register's own summary (found while reviewing, not this task's own defect) corrected too | Implementer C | Done | Medium | 2026-08-24 | — | — | `.ai/tasks/DONE/TASK-0252-h6-1-h4-3-h4-4-remaining-ensemble-claims.md` |
| TASK-0262 | An external model proposed adopting a "Type I-IV allosteric SITE" taxonomy (proximal/intra-domain/inter-domain/inter-subunit) onto [[TASK-0258]]'s own distance bins. **Outcome B — verified not to exist, not adopted.** Live search found no general allosteric-site Type I-IV classification anywhere (the one general scheme found, Tsai/del Sol/Nussinov 2009, is a 6-descriptor functional taxonomy, not distance-based); ASD itself defines no such classification (checked directly). **A real Type I-IV taxonomy exists but is a different thing**: kinase-inhibitor classification by ATP-pocket/conformational relationship (Dar & Shokat 2011 Types I-III + Gavrin & Saiah 2013's Type IV), both DOIs verified live — reusing the same "Type I-IV" label for both would be a false equivalence. **2 of 3 required exemplars fail on inspection, not close calls**: BCR_ABL1's cited exemplar (SH2/SH3-kinase interface) is [[TASK-0229.003]]'s own real *second* site, a different pocket than the myristoyl site this project actually scores (7.96 Å); CARDIAC_MYOSIN's "inter-subunit" label describes the true SRX/IHM biology ([[TASK-0251]]) but not the scope-truncated single-chain construct this pipeline measures (same_chain=True). Own bins kept undecorated, per this task's own Constraint against borrowed authority. Recorded in `task0258_allosteric_distance_taxonomy.py`'s own docstring; the contribution brief's existing correct line left as-is. 2 new REFERENCES.md rows | Implementer C | Done | Medium | 2026-08-25 | — | — | `.ai/tasks/DONE/TASK-0262-verify-allosteric-site-taxonomy-before-adopting.md` |
| TASK-0264 | Cryptic-opening instance hardness before any QUBO — [[TASK-0204]]'s own complexity closure (treewidth 2-5 at m=12) only checked FIXED-backbone rotamer packing, leaving cryptic-*opening* (backbone move coupled to rotamer choice) genuinely untested. **Verdict: formulation exercise only, same conclusion, now checked for the coupled case.** Real instance built from real structures: one shared backbone-choice variable (K=11, [[TASK-0228]]'s own `adaptive_anm_modes` convention, turned into real full-atom structures via [[TASK-0235]]'s own `local_rigid_reconstruction`) as a hub node connected to every rotamer variable (n=15, TASK-0204's own value); interaction graph = CB-CB proximity UNIONED across all K conformations. Sanity-checked first: single-static-backbone treewidth at KRAS_G12C m=12 reproduces TASK-0204's own published value exactly (tw=3). Measured on [[TASK-0243]]'s frozen set, prioritising the 11 genuinely-cryptic targets ([[TASK-0254]] Part B). **Median joint treewidth at realistic window size (m=12): 5** (vs. static 5) — never crosses 9 at the primary 8 Å cutoff at any tested size, well below the pre-registered ≥12 "real quantum target" bar. Coupling is real and measured (union tw exceeds static on every target, 0-2 higher at m=12, up to +2-3 at m=20) — a shift in the constant, not the growth regime; caveat reported not hidden: looser cutoffs (10-12 Å) at m=20 push one condition to tw=15. Exact bucket elimination (mixed-domain generalisation of TASK-0204's own solver, re-validated against brute force before trusting it) stays feasible to ~m=8 median before TASK-0204's own `MAX_FACTOR_ENTRIES=1e8` guard fires (reused verbatim) — the practical boundary moves down roughly half from fixed-backbone packing's own m~20+, driven by the added K=11 domain size, not by treewidth exploding. **Recommendation: do not build the coupled QUBO.** `PHASE_B_ROTAMER_QUBO.md` and `RESULTS.md` both updated with the new closure | Implementer C | Done | High | 2026-08-25 | — | — | `.ai/tasks/DONE/TASK-0264-cryptic-opening-instance-hardness-before-qubo.md` |
| TASK-0268 | Local energetic frustration ([[HYP-P13]]'s own most direct, pre-registered, falsifiable test — Jenik et al. 2012 Frustratometer formalism) — this project's own population-shift hypothesis predicts elevated native frustration at true pocket residues, more so on genuinely-cryptic targets than already-open ones. **Verdict: fails cleanly, reported with the same prominence a positive would get.** Citations verified live (Jenik et al. 2012 NAR doi:10.1093/nar/gks447; Miyazawa & Jernigan 1996 JMB doi:10.1006/jmbi.1996.0114). **Installability checked directly, not assumed**: the real PyPI `frustratometer` package's own `numba`→`llvmlite` build dependency fails — no system LLVM toolchain present (confirmed directly); installing LLVM system-wide judged out of scope, matching [[TASK-0260]]'s own PocketMiner precedent. **Built instead**: new `allostery/frustration.py`, a real single-residue mutational-frustration port (Jenik et al.'s own named variant) using the real 20×20 Miyazawa-Jernigan matrix (AAindex MIYS960101, fetched not hand-recalled, sanity-checked before use) — matching [[TASK-0229.006]]'s own COREX/EAM precedent for exactly this situation. Sanity-checked on KRAS_G12C apo (169/169 residues resolved, 35.5%/24.3% highly/minimally frustrated, both in literature-typical range) before trusting the pipeline. Added as a 5th Shapley block (geometry/fpocket/SASA/CTQW/frustration) on [[TASK-0243]]'s frozen set, [[TASK-0261]]'s exact cluster-permutation significance, [[TASK-0260]]'s own crypticity stratification, n=20/22. **Overall added-last indistinguishable from zero** (median +0.1%, cluster-p=0.727, 13 clusters); its real Shapley share (+6.5%, p=0.128, also not significant) reflects redundant information already captured by the other blocks, not new signal. **The pre-registered prediction itself fails on direction**: crypticity-stratified added-last is cryptic median −0.09% vs. open median +0.05% — the OPPOSITE of HYP-P13's own prediction (cluster-p=0.452, not significant either way); fraction highly-frustrated shows essentially no difference (36.0% vs 35.2%, p=0.537). `.claude/hypotheses/physics.md`'s own HYP-P13 section updated with a new dated subsection at the same prominence as its positive evidence. `RESULTS.md` new section | Implementer C | Done | Medium-High | 2026-08-26 | — | — | `.ai/tasks/DONE/TASK-0268-local-frustration-and-hyp-p13.md` |
| TASK-0272 | [[TASK-0270]]'s KRAS_G12C apo genotype fix (`4OBE`->`4LDJ`, organiser-sanctioned) was propagated to the live app and core docs, but the submission draft and CTQW brief still cited pre-fix numbers as current, and two independently-flagged results ([[TASK-0263]], [[TASK-0269]]) were still missing from the brief. **Document-propagation only, no new computation.** `PHASE1_SUBMISSION_DRAFT.md`: new dated banner entry (matching the doc's own convention); Finding 5 corrected in place (AUC 0.557-0.590 -> **0.514**, now below floor, `NO_SIGNAL_IN_APO`). **Finding 6 checked, not assumed stale**: its own "AUC 0.557" is a *holo*-structure (6OIM) measurement per [[TASK-0229.005]]'s own text, unaffected by the swap -- corrects the task filing's own premise. A second stale table found by this task's own grep-check (not in the original filing): SS2.5's rank-of-known-site/enrichment-at-k table -- flagged inline, not recomputed (never re-run on 4LDJ). `CTQW_CONTRIBUTION_BRIEF.html`: SS05's ENM-validity stat-row and R1 table's own *baseline* row recounted (6/5/4->5/6/4; 6/4/4->5/5/4) for KRAS's PASS->MARGINAL move; **the R1 experimental row itself left unaltered** (never re-run on 4LDJ, disclosed not fabricated). SS04 gained [[TASK-0263]]'s own terms-block-vs-CTQW result (0.751 vs 0.575, cluster-robust p=0.019; CTQW's own added-last once terms are in the model, p=0.973) -- that task's own "strongest single result in the brief" and missing until now. SS08's PocketMiner paragraph updated from "could not be run" to [[TASK-0269]]'s own real outcome (added-last median +0.4%, cluster-p=0.277, not significant; crypticity-stratified the right direction, not formally tested). **Historical entries** ([[TASK-0245]], [[TASK-0246]], [[TASK-0247]], `task0258_allosteric_distance_taxonomy.py` -- no task file to annotate, already confirmed missing by another thread): dated pointers added. **Checked, not assumed**: in both [[TASK-0245]] and [[TASK-0247]], KRAS_G12C's own row is not the median-determining value in any cited table (PTP1B is) -- headline conclusions do not turn directly on the KRAS row, though an exact-recompute check was not run, stated as a real limit. **Also found and fixed, beyond the task's own named scope**: `WORKFLOW.md`'s own "what this pipeline cannot do" section and KRAS walkthrough stated 4OBE/N=169 as current -- corrected to 4LDJ/N=170 (3 places); its own stale "10 structures, median AUC 0.482" claim dropped rather than repeated, since TASK-0270 separately found 8 of those 10 were actually drug-bound. `2026-08-26-organiser-clarifications.md` (the decision record TASK-0270 was filed from) gained a short dated update note, kept as the historical record it is. **Grep-check** (this task's own explicit Acceptance requirement): every live document swept for `4OBE`/`0.646` after every edit -- every remaining hit is the organisers' own Challenge Statement, a generic API-example PDB code, or explicitly dated/historical/disclosed text. `RESULTS.md` new section | Implementer C | Done | High | 2026-08-26 | — | — | `.ai/tasks/DONE/TASK-0272-kras-swap-blast-radius-and-pending-doc-updates.md` |
| TASK-0274 | Every attribution block ever tested is geometric or dynamical (geometry/fpocket/ctqw/sasa/p2rank/potential-terms/pocketminer) — checked directly, no sequence-conservation or residue-chemistry feature existed anywhere in `src/allostery/`. Test both before calling the 27-29% residual irreducible. | **Resolved: both built real, neither closes any of the residual.** Conservation: `backend.active_site`'s own UniProt-offset machinery (reused, not reimplemented) → InterPro Pfam domain match → Pfam **seed** alignment (curated, via InterPro's API, parsed with `Bio.AlignIO` — no MSA binary installed in this environment, checked directly) → per-column Shannon-entropy score → transferred via a BLOSUM62 pairwise-alignment nearest-homolog anchor. Citation verified live: Mistry et al. 2021, NAR, DOI 10.1093/nar/gkaa913. 20/20 targets got a real Pfam seed mapping (n_seed median 32, range 5-245; best-homolog identity median 97%, 4 targets shallow at 44-53%, flagged not hidden). Chemistry: Kyte-Doolittle hydropathy (DOI 10.1016/0022-2836(82)90515-0, verified live), charge, aromaticity, Zamyatnin 1972 volume (DOI 10.1016/0079-6107(72)90005-3, verified live). **Headline (added on top of the geometry+fpocket+CTQW baseline specifically, this task's own decision statistic)**: conservation median **-0.08%**, chemistry median **-0.35%** — both slightly negative. Cluster-robust ([[TASK-0261]]'s exact permutation, 13 clusters): conservation p=0.6946, chemistry p=0.6492 — neither distinguishable from zero. 5-block unexplained median 30.7% vs. 3-block baseline 28.6% — not lower, marginally higher. Crypticity-stratified pre-registered prediction (conservation should help function-regardless-of-openness) not confirmed: cryptic -0.66% vs open -0.07%, p=0.572, no signal in either regime. **Answered per this task's own Constraint, plainly: neither ordinary feature family closes the residual.** Closes 2 of 3 named remaining candidate categories ([[TASK-0273]] covers label noise, the third); "ligand-side properties" stays explicitly out of scope, "irreducible" is what remains. One per-target outlier noted not chased: DHPS_GC7 conservation Shapley +32.7% (single point, not a pattern). `documentation/CTQW_CONTRIBUTION_BRIEF.html` §08 not flagged (residual didn't drop). `RESULTS.md` new section | Implementer D | Done | High | 2026-08-26 | — | — | `.ai/tasks/DONE/TASK-0274-what-are-the-missing-contribution-categories.md` |
| TASK-0279 | [[TASK-0276]] found `V_C` (GNM dynamic cross-correlation centrality) significantly separates allosteric from orthosteric sites in two families -- the register's first real holo-side positive. [[TASK-0278]] flagged the gap it could not close: does `V_C` measure allosteric *efficacy*, or just occupancy? BCR-ABL1 gives the one controlled pair where the imprint confound cancels: `1OPL`/`MYR` (myristic acid, inert, no therapeutic autoinhibition) vs `5MO4`/`AY7` (asciminib, the real allosteric drug), same protein, same myristoyl cavity. **Verdict: no support for the efficacy reading, and the data land in the unfavourable direction more decisively than a null.** Reused [[TASK-0276]]'s own ligand-stripping unchanged; node sets matched via `align_apo_holo` (429 common Ca pairs, 16/16 pocket + 26/26 active-site residues survive) per [[TASK-0275]]'s own precedent (an unmatched comparison moved DHPS_GC7's own V_C by 0.19 in that task). Both arms doubly occupied (1OPL also carries P16; 5MO4 also carries NIL) -- stated explicitly, not treated as apo. **`V_C` is HIGHER on the inert myristate pocket than the efficacious asciminib pocket in 16/16 matched residues** (median 57.44 vs 53.63, ~7% relative) -- the opposite direction the efficacy reading required. `degree`/`hop` flat; `euclid` a small but consistent shift toward asciminib (0.6%); `V_B` shows a huge gap (57.4 vs 25.3) almost certainly a resolution artifact (1OPL 3.42 A vs 5MO4 2.17 A -- [[TASK-0250]] had already flagged 1OPL MARGINAL on ENM validity); `SASA` borderline (p=0.079). **A confound this task cannot rule out, disclosed prominently**: coordinate precision at 3.42 A is coarser regardless of ligand, so this n=2-structure gate cannot fully separate a real ligand effect from a structure-quality effect. **[[TASK-0278]] landed the same day and independently corroborates this**: its own MYR-stripping control on 1OPL found the pocket does not close computationally -- "the confound is the crystallized backbone conformation, not the ligand atoms' mere presence," the same open question from a different angle. [[TASK-0276]]'s own positive should now be read as a real holo-side structural pattern, not yet distinguished from occupancy -- recorded directly in that task's own Done section. **Extending the design (searched, not found)**: KRAS's ten drugs all efficacious (ruled out per the filing); checked live, HCV_NS5B's thumb/palm-site and glutaminase/GAC's BPTES-site literature turned up no specific inert occupant of the same site as this register's own curated ligands within this task's own bounded search -- a real follow-up, not attempted further. No structure-level significance test computed or reported (n=2, per this task's own Constraint); Wilcoxon p-values are explicitly residue-level, descriptive statistics on correlated residues, not a properly-powered test. `RESULTS.md` new section | Implementer C | Done | High | 2026-08-26 | — | — | `.ai/tasks/DONE/TASK-0279-ligand-selectivity-gate-occupancy-vs-efficacy.md` |
| TASK-0285 | Filed and completed same-session, per Bartosz's own direct request: "we want our agentic tools (evoef2, p2rank, pocketminer, etc) working using dockerization." [[TASK-0206]] already caught a real cross-machine reproducibility bug in fpocket (two hand-compiled binaries, different AUCs); [[TASK-0269]] had already proven Docker fixes exactly this class of problem (PocketMiner). This task generalizes that fix to fpocket/EvoEF2/P2Rank. **Wrapper pattern, zero call-site changes**: `tools/{fpocket/bin/fpocket, evoef2/EvoEF2, p2rank/p2rank_2.5.1/prank}` are now small Python scripts at the exact paths 12 existing call sites already reference -- every one works unmodified. EvoEF2 preserves a confirmed SIGSEGV-on-absolute-argv0 quirk via a relative in-container symlink deliberately not named `EvoEF2` (would overwrite itself via the live bind mount). **A real near-miss**: the first draft overwrote the shared native fpocket binary before the image was validated -- fixed by backing up every native binary before replacement from then on. **All base images pinned by digest, load-bearing not cosmetic**: the first fpocket build (tag-only pin) produced a THIRD, still-different AUC set (BCR_ABL1 0.8596/CARDIAC_MYOSIN 0.5345, matching [[TASK-0163]]'s own ORIGINAL numbers, not [[TASK-0206]]'s own re-verified 0.8618/0.5303) on the identical source tag -- real evidence Docker alone doesn't fix this without a digest pin; all three AUC sets kept on record. **EvoEF2 -- a clean positive by contrast**: native vs containerized GreedyRepack output is BYTE-IDENTICAL (107,203 bytes) on a real target. **Real end-to-end verification, every tool, through the unmodified real calling code** on KRAS_G12C's own real apo (4LDJ): fpocket 9 real pockets; EvoEF2 real 107KB structure; P2Rank 170 real per-residue rows; PocketMiner (existing image, not rebuilt) real inference confirmed still functional. **Not done**: a from-scratch multi-machine rebuild test of the now-digest-pinned fpocket recipe (does it converge?) -- flagged, not resolved. `RESULTS.md` new section | Implementer C | Done | High | 2026-08-28 | — | — | `.ai/tasks/DONE/TASK-0285-dockerize-vendored-agentic-tools.md` |
| TASK-0307 | Submission numbers have moved repeatedly in the last week and several are quoted in a brief already sent to the collaborator (pocket-level ceiling, Finding F, "classical ceiling beats CTQW", SVC figures, KRAS P@5) — audit every quantitative claim in `PHASE1_SUBMISSION_DRAFT.md`/`CTQW_CONTRIBUTION_BRIEF.html` against its committed source and flag staleness. **Verify-only, neither document edited.** Headline: `git log` confirms the draft's last commit is `3b3be11` (TASK-0272, 2026-08-26), the brief's is `34bf290` (2026-08-28) — **neither has moved since**, so every Done task from [[TASK-0288]] onward (13 tasks) is absent from both, not merely under-cited. This task's own 7-item table: 2/7 already correct (SVC withdrawal, KRAS P@5=0.000); 4/7 missing outright, not wrong-in-place (pocket-level ceiling, Finding F's count/p/%); the 7th ("classical ceiling beats CTQW") was **never written into either document** — nothing to edit, what's owed is telling the collaborator directly per [[TASK-0299]]'s own unchecked action item. All 5 explicit coverage items checked: Finding F is missing AND internally unreconciled ([[TASK-0291]]'s `max_A` restatement was computed against the pre-[[TASK-0297]] 9-member spike, never redone against the corrected 8-member one); found and separated **six** distinct "ceiling" values that must not be conflated; the pocket-level CTQW-vs-random result (p=1.000) is missing while the classical-diffusion-parity claim already in the brief is verified current; [[TASK-0304]] (Finding F generalizes to ASBench, 22.2% on 118 external structures) and [[TASK-0305]] (our own CTQW P@5 significantly *worse* than random on ASBench, p<1e-4) are both entirely absent, and the draft's own ASBench citation ("89.8%/98.1%") has carried its own "not yet auditable" flag since 2026-08-13, still unverified for one paper and superseded by TASK-0304's real recompute for the other; the attribution "median 29%" figure is itself current but five follow-on Shapley negatives since (SASA, frustration, [[TASK-0274]]'s conservation+chemistry) are absent, and [[TASK-0263]]'s own strongest result (0.751 vs CTQW 0.575, p=0.019) is in the brief but missing from the draft entirely. **Two more stale items found beyond the filing table**: the brief's §06c bimodality claim (silhouette 0.737) is missing [[TASK-0288]]'s own same-day, same-family correction (Finding B: LRT underpowered, p=0.137). Deliverable: `documentation/2026-08-31-submission-evidence-pack.md`, one table, line-by-line checkable, CURRENT/STALE/MISSING/UNVERIFIED per row. `RESULTS.md` new section | Implementer D | Done | Critical | 2026-08-31 | — | — | `.ai/tasks/DONE/TASK-0307-submission-evidence-pack.md` |
| TASK-0303 | [[TASK-0301]] ranked ENM mode shift (external Experiment D: open-stratum p=0.005-0.013) as the most independent untested signal after the rule family's own consensus/voting negative. Ported into this repo's own pipeline with the fix that experiment's own handover flagged as required first: this repo's own per-target `enm_cutoff` (replacing the external review's uniform 7.3 A). **2026-08-31 lane-collision amendment: evaluated standalone, NOT folded into `task0282_pocket_selection_sweep.METRICS`** -- Lane B owns that script's own re-run this window. **Cutoff fix checked not assumed**: real per-target field, but resolves to a uniform 8.0 A across all 22 targets -- a modest +0.7 A shift, not a heterogeneity correction, stated precisely. **Verdict: does not reproduce, on either statistic.** This register's own ranker-vs-random EH statistic (TASK-0261's exact cluster sign-flip test): raw mean delta=+0.021 (p=0.557), kfix +0.063 (p=0.204), open/cryptic strata both non-significant. Experiment D's own exact statistic added afterward for a genuine apples-to-apples check (percentile rank of the true candidate's own mode-shift value, Fisher-combined): open stratum p=0.306 (raw) / 0.319 (kfix) -- NOT 0.005-0.013. The only marginal signal (pooled-20 Fisher p=0.029-0.033) is explicitly not cluster-robust (Fisher treats 20 rows as independent, the same pseudo-replication TASK-0300 found breaks 4 other selection procedures) -- read as noise. **The one part of the premise that holds**: pooled Spearman(mode_shift_kfix, fpocket_drug) is real (rho=0.173, p=9.9e-7) but drops to rho=-0.044 (p=0.212) once pocket size is controlled for via OLS residualisation -- genuinely independent of druggability, confirming the one premise the filing named as the reason to add it, though independence alone doesn't translate into ranking power here. **Real shared-module bug found and fixed at the source**: `task0254_fpocket_variance_and_crypticity.crypticity` still assumed bare-int resnums; TASK-0298 changed `fpocket_candidates`'s own output to (chain,resnum) tuples without updating this consumer, crashing on first use. Fixed to handle both shapes; every existing caller (TASK-0260/0266/0268) ran before TASK-0298 landed, so their own published numbers are unaffected -- checked by date. **Not done**: task0282's own METRICS/sweep intentionally untouched per the amended Constraint. `RESULTS.md` new section | Implementer C | Done | High | 2026-08-31 | — | — | `.ai/tasks/DONE/TASK-0303-enm-mode-shift-as-ranker.md` |
| TASK-0302 | [[TASK-0301]] ranked sibling-conformer pocket persistence as the most independent untested lever (external handover: every target has siblings at >=95% identity, median 53, min 10). Tests whether a candidate cavity persists across independently solved sibling structures, with the circularity trap (most siblings are holo, some carry the drug) handled first: two filters reported per this task's own Constraint, `min_bar` (excludes only the target's own `drug_ligand`) and `strict_apo` (excludes any drug/ligand-category ligand, TASK-0278's own classification reused). `siblings.json` (external handover's own scan) reused, its own apo/chain keys verified to match this repo's own frozen 20 exactly. Candidate/truth apparatus mirrors `task0282_pocket_selection_sweep.build_target`'s own call shape as a separate, duplicated implementation (that script untouched, Lane B still owns it). Sibling pockets mapped onto target apo numbering via `allostery.labels._needleman_wunsch_map`, the same sequence-only primitive `holo_pocket_mask` already uses. Two disclosed proof-of-concept bounds (unfiltered sibling counts run to 493/target, 1,999 total): 30 siblings checked per target before the holo filter, 10 scored per filter variant after. Full run ~2 hours wall-clock. **Sibling coverage reported before scoring**: median 88 available, capped to 30 checked, median 29.5 survive min-bar, median only 3.5 survive strict-apo-only, and 2/20 targets (PKR_MITAPIVAT, PKR_AG946) have ZERO strict-apo-only siblings. **Verdict: no standalone ranking benefit, either filter.** Cluster-robust (TASK-0261's exact 13-cluster sign-flip test): min-bar mean delta=-0.0006 (p=0.969); strict apo-only (n=18, 2 zero-sibling targets correctly excluded) mean delta=+0.0033 (p=0.856). As a druggability tie-break: mean delta=+0.0000 exactly (p=1.000) -- persistence never actually broke a tie (real-valued druggability essentially never collides exactly). **Independence mixed, size the stronger correlate**: persistence vs `fpocket_drug` rho=0.129 (p=0.0003); persistence vs pocket size rho=0.190 (p=7e-8); persistence vs `fpocket_drug` controlling size, rho=0.061 (p=0.086, borderline) -- per this task's own Note, partially but not fully 'just size again.' **Not done**: exhaustive sibling coverage (disclosed caps); OVERLAP_JACCARD=0.2 not swept. Validation on a larger cohort remains TASK-0304's own scope. `RESULTS.md` new section | Implementer C | Done | High | 2026-08-31 | — | — | `.ai/tasks/DONE/TASK-0302-sibling-conformer-persistence.md` |
| TASK-0311 | **Premise corrected 2026-09-01 ([[TASK-0313]] Reviewer addendum): the Silverman leg of this row is WITHDRAWN.** That test has zero power below 3 SD separation at n=26/112/171 (measured); the data sit at 1.88 SD ([[TASK-0288]] Finding B), so p=0.81/0.14/0.11 are nulls from absent power -- uninformative, not negative. The pooled arm (n=171), never run until [[TASK-0313]], is MULTIMODAL (p=0.020 / p=0.005 excluding the covalent floor), but the three cohorts differ significantly in location (Kruskal-Wallis p=2.15e-03; medians 2.95/9.03/2.97), so that is plausibly a cohort-mixture artifact. **Modality is UNDETERMINED**; re-run tracked as [[TASK-0316]]. This task's own regression results are unaffected -- only the justification 'there are no classes' is. Original text follows. ~~[[TASK-0309]] settled it on 171 proteins: no discrete near/far populations (Silverman p=0.81/0.14/0.11 across cohorts, k-means best-k incoherent 3/4/2/5).~~ Every discriminator attempt in this register up to now was a classifier assuming classes the data does not support; this task tries regression instead, on the same pooled 171-protein cohort, LOPO-by-protein throughout with a permuted-target null (not R^2 alone), per its own Metric bullet. **One Scope correction found before building anything**: the task named "landscape features from TASK-0300", which never computed any -- TASK-0288's own fpocket landscape descriptors were used instead, clearly what was intended. **N/Rg/compactness use each cohort's own already-established scoping convention** (ours: TASK-0284's chain-scoped t0242.prep values; ASBench/CASBench: TASK-0306's whole-ASU structure_descriptors()) after confirming live that restricting to annotation-named chains would silently undercount multimers (CASBench's 1NXE names allosteric chains 'C'/'F' absent from the raw ASU file) -- disclosed cross-cohort mismatch, tested directly via a per-cohort robustness re-fit. TASK-0309's own literal-site-overlap rows (min_A=0 exactly, 15/171) excluded from log-scale tests only, never epsilon-padded, same convention. **Verdict, mixed as either outcome was expected to be**: (1) the floor/continuum split is itself real and predictable -- linear-probability-model AUC=0.657, p=0.004 for "is this protein's site pair at the covalent limit (<1.5A)"; (2) the continuous remainder is NOT predictable from N/Rg/compactness/chain-count/site-size, pooled (non-floor Spearman=0.127, p=0.096) or within any single cohort alone (ours p=0.098, asbench p=0.258, casbench p=0.074) -- the one nominally-significant pooled number (unsplit, rho=0.311, p=0.004) is a floor/continuum-conflation artifact, exactly what the task's own required two-part decomposition was designed to catch; (3) fold class (TASK-0306 addendum's own Alpha-Beta vs Mainly-Alpha) adds a small real ASBench-only signal, rho 0.116->0.215, p 0.194->0.028; (4) the strongest real result is TASK-0288's own "landscape spread" finding, now confirmed under genuine LOPO out-of-sample prediction with a permutation null (not just in-sample correlation) -- 5 of 13 fpocket features individually significant (max_d p=0.002, median_d/n_cand_far/size_wtd_d p=0.004, frac_cand_far p=0.012) but pairwise-correlated 0.64-0.95, confirmed one construct at five settings not five findings, and "ours"-only (n=25) -- extending it cross-cohort needs new fpocket runs, ruled out by this task's own "No new descriptors" Constraint. `RESULTS.md` new section | Implementer C | Done | High | 2026-09-01 | — | — | `.ai/tasks/DONE/TASK-0311-regression-not-classification.md` |
| TASK-0315 | Filed as TASK-0313 by Reviewer thread, renumbered by Implementer C after a genuine id collision with another thread's own TASK-0313 ("verify Silverman", already Done, kept its id -- first claim wins; see [[TASK-0045]], the collision this repo's own reserve-next was built to prevent). The register's own "strongest constructive number" -- potential-terms-of-H_new AUC=0.751 vs CTQW 0.575, p=0.019, in the submission brief -- had never been checked against proximity, the same confound TASK-0308 conditioned CTQW on one task earlier. Fresh recomputation on the frozen 20 (not a re-read of old JSONs) reproduces every cited base AUC to 3-4 decimals, but terms_block-vs-ctqw's own p reproduces at 0.049 not 0.019 -- traced to one target (DHPS_GC7) whose own values genuinely differ between this run and TASK-0263's original (0.596->0.363 terms_block, 0.739->0.846 ctqw), reproducible across two independent reruns, not chased further (disclosed, not this task's own Constraint). Positive control (euclid residualised on hop-proximity must land at ~0.5, per this task's own Constraint) did NOT pass cleanly at 0.61 -- root-caused via a synthetic self-check built into the committed script (confirms rank_residualize itself is correct: collapses a truly redundant predictor to 0.509, preserves an independent one) before trusting anything downstream: a genuine cohort finding, not a harness bug -- hop-distance and Euclidean seed-distance are correlated but not fully redundant on this register's own more heterogeneous, several genuinely-distal-labelled 20 targets (unlike ASBench's more proximal-skewed population). Fixed by residualising jointly on both proximity measures for the main test. **Verdict, mixed as measured**: (1) terms block (fitted, 5-column) retains a real cluster-robust signal beyond geometry AND both proximity measures jointly controlled for -- raw mean AUC 0.740 -> residual 0.660, p=0.027, survives; (2) but does not beat the geometry baseline (0.751<0.795, p=0.29 either direction); (3) V_C alone -- the brief's own "beats the whole walk by itself" line -- does NOT survive the same proximity control (residual p=0.31), no better than CTQW's own residual (p=0.68, independently reproducing TASK-0308's ASBench finding on this repo's own 20-target cohort and labels too); (4) the raw 0.751-vs-CTQW comparison is fitted-5-column vs unfitted-1-column -- the matched unfitted-vs-unfitted comparison (terms-sum 0.599 vs ctqw 0.575) shows most of the raw margin is the fitting itself, confirming TASK-0263's own "propagator not physics" conclusion did not follow from the comparison made, exactly this task's own "second, smaller defect." Per-term rho(term,proximity) on the frozen 20 reproduces the same signs as the Reviewer's own 14-target preliminary table (V_R/V_M close in magnitude, V_C weaker: -0.074 vs -0.237). Per-term cell distribution (n=100, 5 terms x 20 targets): median=0.543, the cited "0.81-0.93" range is the extreme max not a typical cell. `CTQW_CONTRIBUTION_BRIEF_V2.html`'s own Holds card corrected in the same commit -- a new Corrected card added after it (original left in place, append-don't-silently-edit), both tables, the fitted-vs-unfitted defect explained, and the credit-where-due bullet restated at the corrected size, V_C-alone framing dropped. **Not done**: DHPS_GC7 reproducibility gap (disclosed only); a third independent proximity measure to fully validate the positive control (none established in this codebase). | Implementer C | Done | Critical | 2026-09-01 | — | — | `.ai/tasks/DONE/TASK-0315-terms-block-was-never-compared-to-proximity.md` |
| TASK-0310 | [[TASK-0308]] found CTQW is ~80% proximity to the active site (ρ=+0.735; residualised AUC 0.5921→0.5184, no longer significant) — but every observable in TASK-0140-0157 was scored raw, never conditioned on distance. Re-score the whole family on TASK-0308's own 108-structure ASBench cohort: raw AUC, ρ vs proximity, and residualised AUC for each. Chiral circulation first (strongest prior: ρ roughly half CTQW's on the same measurement). **Runnable check**: 2 of 9 (vibronic [[TASK-0147]], two-boson HOM [[TASK-0157]]) were never implemented past a literature proposal — excluded, confirmed via their own Done sections. 7 scored. **Harness validated first**: reconstructed `ctqw_proximity_partial` from scratch (no surviving script) and reproduced its committed numbers to 4 decimals before trusting anything new; positive control (proximity residualised on itself) landed at exactly 0.5000. **Chiral circulation — reported first per this task's own Constraint**: ρ=0.365 (confirmed ~half CTQW's 0.735) but raw AUC 0.556 collapses to residualised **0.496 — below chance**. Full family ranked by residual, all 7 reported: persistent_h2_void +12.0% (best, but p=0.856 Bonferroni, cluster-p=0.101, n=45/108 usable — 63 legitimate "no void detected"); spectral_coherence +4.5%, transport +4.0%, chiral -0.8%, entanglement -1.9%, prs_low -6.4%, dcc_low -11.1% (only nominally-significant cell, p=0.0235 uncorrected, but negative and p=0.188 Bonferroni). Dephasing time-boxed to N<=300 (11/108, ~N^2.75 cost makes the full cohort infeasible, same treatment as [[TASK-0256]]): raw 0.431, below chance. Cluster-robust by protein required writing a generalised version of [[TASK-0261]]'s own sign-flip test (that one is hardcoded to its own 13-cluster set; 108 ASBench structures cluster into 76 mostly-singleton proteins, Monte Carlo not exact). **Verdict: nothing survives.** Every one of the 7 runnable observables, including the one with the strongest specific prior, loses its apparent signal once proximity is accounted for; none significant after Bonferroni, none holds up cluster-robust. With TASK-0308's own CTQW result, 8 of 9 nameable observables tested against this confound, none survives it — the register has no observable genuinely orthogonal to distance. `RESULTS.md` new section | Implementer D | Done | Critical | 2026-09-01 | — | — | `.ai/tasks/DONE/TASK-0310-observable-family-residualised-on-proximity.md` |
| TASK-0312 | Reviewer thread's standing 'CTQW propagates differently active->pocket than pocket->active' observation, rewritten from 2 hypotheses to 3. H2 (implementation defect) EXCLUDED: max\|M-M.T\|=1.11e-16, machine precision. H1 (\|S\| vs \|T\| normalisation) exact for the set-level scalar -- but the reported statistic was `background_spearman_rho`, and Spearman is scale-invariant, so the proposed fix is a NO-OP. H3, the live one: **category error** -- the statistic compares two different ROWS of a symmetric matrix against two different labels, which symmetry does not constrain. Directionality needs complex hopping, not a re-analysis. Also flags `coherent=True` as the propagator default (both reverse-direction scripts pass False and are safe; other call sites unaudited). | Reviewer thread | DONE | Medium | 2026-09-01 | — | — | `.ai/tasks/DONE/TASK-0312-*.md` |
| TASK-0314 | Two reporting defects in the submission brief, both found in the TASK-0259-0311 conclusions audit. (a) [[TASK-0305]] (P@5: CTQW significantly WORSE than random) and [[TASK-0308]] (mean AUC: CTQW +18.4%) run on the IDENTICAL 108 structures -- `task0308`'s KEEP set is loaded from TASK-0305's output -- and neither references the other; the shipped deliverable is a top-5 hit list at P@5=0.000 ([[TASK-0283]]). Mechanism found: the register has TWO incompatible 'AUC's -- `cv_auc` fits OLS per fold and silently flips sign on anti-correlated features (demonstrated: raw 0.2373 -> cv_auc 0.7581), so every solo cv_auc measures \|discrimination\|, not 'ranks pockets high'. (b) No family-wise error control across ~50 tasks; no surviving claim is below p=0.019. | Reviewer thread | TODO | High | 2026-09-01 | — | — | `.ai/tasks/TODO/TASK-0314-*.md` |
| TASK-0316 | [[HYP-P14]]'s continuum bullet withdrawn -- re-run modality with a test that has power at ~2 SD, per cohort, never pooled. Silverman has 0% detection below 3 SD at every cohort size we have (measured); the data sit at 1.88 SD. Reuse [[TASK-0288]]'s already-calibrated bootstrap LRT. Constraint: a null is only reportable with its power. Does NOT re-litigate [[TASK-0300]]'s direct finding that the distance category fails to determine the winning rule, which is independent of modality. | Reviewer thread | TODO | High | 2026-09-01 | — | — | `.ai/tasks/TODO/TASK-0316-*.md` |
| TASK-0317 | [[HYP-P14]]'s secondary route: does WHICH of Wu/Stromich/Yaliraki's six measures fires predict from the paper's OWN stated applicability conditions (read from PMC8767309, not recalled: surrogate-CI's own size/diameter-matched surrogate construction; the high-propensity test's uniform-QS assumption, not testable here -- no raw QS values in this repo; and an explicit caveat sentence naming site proximity, "large and complex multimeric proteins," and "structural water molecules"), rather than from the global scalars (N, chain count, site separation, fold class) [[TASK-0306]] already killed. Three descriptors derived reusing existing machinery (gnm_context, contact graph, HETATM parsing), none re-deriving TASK-0306's own failed globals: `surrogate_spread` (std of mean GNM MSF across 50 same-size compact random windows), `interface_frac` (site's own cross-chain contact fraction, not raw chain count), `water_density` (crystallographic waters within 5A of the site, size-normalised). ASBench `asbench_without_ligand` (TASK-0306's own primary condition), 117/118 joined, 79 protein clusters, LOPO throughout. **Bonferroni gate (18 tests, alpha=0.00278): NONE survive** -- every per-measure AUC sits in the same 0.05-0.57 band TASK-0306's own globals already occupied. Positive control passes (0.742-0.877, matching TASK-0306's own 0.74-0.88 range) -- harness confirmed working, the negative is real. **A genuine methodological finding along the way, root-caused before trusting it**: the n_fired regression initially showed `interface_frac` rho=-0.612 (p=2.2e-13) and `water_density` rho=-0.727 (p=1.7e-20) -- would have been the strongest results in this whole discriminator line. Neither is real: raw/pooled Spearman for both is ~0 (+0.015, +0.057), the opposite sign of significance. Traced directly to heavy ties in the raw feature (`interface_frac`: only 34 distinct values across 117 rows, 71 exactly 0.0) -- confirmed the per-fold OLS slope is STABLE (77/79 folds same sign, not ordinary overfitting instability); tiny per-fold intercept jitter arbitrarily reorders tied rows in LOPO output, and Spearman (purely rank-based) inflates that arbitrary reordering into an apparently enormous correlation. `surrogate_spread` (117/117 distinct, no ties) shows a much smaller LOPO-vs-raw gap (both non-significant) -- ties amplify ordinary regression noise into a false positive, they are not the sole source of LOPO/raw divergence. A real, disclosable risk in this register's own established `lopo_predict`+Spearman/AUC idiom whenever the input is low-cardinality -- flagged for [[TASK-0314]]'s own standing metric-choice concern; the script now reports raw rho + n_distinct alongside LOPO rho on every future run. **Corrected verdict: n_fired is null for all three once read correctly.** Combined with TASK-0306's own global-descriptor negative, every predictor tried against these six measures has now failed -- the register's own "a feature can be useless as a ranker and informative as a selector" hypothesis is not supported by anything tested to date. **Not done**: fpocket-landscape descriptor (available machinery, not attempted -- the paper's own caveat sentence mapped onto GNM/contact-graph/water, not pocket detection); condition 2 (uniform-QS, no raw propensity data); CASBench (same reason TASK-0306 excluded it). | Implementer C | Done | Medium-High | 2026-09-01 | — | — | `.ai/tasks/DONE/TASK-0317-discriminator-b-per-measure-applicability-features.md` |
| TASK-0318 | [[TASK-0310]] found 7/7 observables fail residualisation, chiral circulation included (0.5560 raw -> 0.4960 residual) despite being proximity-orthogonal by construction. All nine are functionals of ONE input (contact graph + seed) -- [[TASK-0301]]'s 'one signal at 61 settings' lesson one level up. Measure the input space's CEILING directly instead of inferring it from seven anecdotes. Pre-registered: ceiling ~0.50 closes the observable class with an argument and blocks [[TASK-0147]]/[[TASK-0157]]; ceiling >0.50 is the only condition making the structured bath rational. **Resolved: ceiling is meaningfully > 0.50, tested not just measured.** 19 reused features (contact-graph baselines, gnm_context, all five V_B/V_T/V_R/V_C/V_M potential terms, CTQW, [[TASK-0310]]'s own 7 observables) on the same 108-structure ASBench cohort (105 usable, 3 hit a real pre-existing disconnected-contact-graph diagnostic). HistGradientBoostingClassifier, LOPO by protein (74 proteins, 74 folds, OOF only). [[TASK-0310]]'s own residualisation harness reused, extended to a joint [hop,euclid] design per [[TASK-0315]]; self-check lands at exactly 0.5000, 105/105. **Headline**: raw ceiling mean 0.6558, residualised mean **0.5949**, Wilcoxon p=3.3e-6, cluster-robust (protein) p=1e-5. **The obvious objection tested, not waived**: top feature was `euclid_prox` itself (+0.113) -- refit with BOTH proximity features removed entirely (17 columns, zero proximity input): residualised ceiling barely moves, mean **0.6017**, p=2.3e-7, *higher* not lower. The model does not need proximity to reach the same ceiling -- rules out nonlinear-reconstruction as the explanation. Drivers (permutation importance, proximity-excluded): V_C (GNM dynamic cross-correlation) dominant at +0.135, then chiral_circulation (+0.053), persistent_h2_void (+0.042), degree (+0.032) -- notable against [[TASK-0315]]'s own finding that V_C alone does not survive proximity control on the frozen-20 (a different, richer object combined nonlinearly, not a contradiction). **Verdict, held to this task's own pre-registered calibration**: building [[TASK-0147]] is rational, with two concrete targets now on record (residual AUC ~0.60; V_C/chiral_circulation as what to check first) -- still honestly "a lead, not a result" per the Constraint's own framing (answers "is there information", not "can we build a clean observable that reaches it"). TASK-0157 recommendation unaffected (argued from propagator structure, not this result). `RESULTS.md` new section **Outcome table CORRECTED 2026-09-01 by the Reviewer thread that wrote it** (execution not in question; the pre-registration was wrong). The ceiling survives removing both proximity features entirely (0.5949 -> 0.6017, *higher*) -- a genuinely strong robustness check. But the pre-registered claim that ceiling>0.50 is "the only condition under which building [[TASK-0147]] becomes rational" **does not follow**: permutation importance shows the ceiling is built from `V_C` (+0.135), `chiral_circulation`, `persistent_h2_void` and `degree` -- **all features we already have**. That licenses fitting the existing combination, NOT building a structured-bath master equation to reach a number four existing features already reach. **Revised: do not build [[TASK-0147]]/[[TASK-0157]].** Also owed: a permuted-label null (permute within structure, refit the 74-fold LOPO) -- there is no negative control, the systematic omission [[TASK-0319]] documents. Until then the ceiling is a lead, not a result. **2026-09-02 addendum (Implementer C): the permuted-label null was run** -- 100 reps, within-structure label permutation, identical 74-fold LOPO pipeline reused from cached Phase-A features. Null residual-AUC-mean=0.4993+/-0.0110 (matches the ~0.50 the harness should give under a true null); observed 0.5949 sits ~8.7 null-SDs above it; permutation p=0.0099 (the 100-rep floor -- zero null reps came close). **The ceiling now clears both controls and reads as a result, not a lead.** | Implementer D | Done | High | 2026-09-01 | — | — | `.ai/tasks/DONE/TASK-0318-ceiling-of-the-contact-graph-input-space.md` |
| TASK-0319 | **The 1-vs-2 Gaussian LRT was miscalibrated in BOTH implementations, in opposite directions** -- measured on strictly unimodal N(0,1): `task0309.lrt` (imported verbatim by [[TASK-0316]]) rejected at **68% (n=26) / 52% (n=100)**; `task0288.lrt` (original) rejected at **0% / 0%**. **Resolved.** Root cause on the anti-conservative side (`task0309.lrt`) was NOT any of the three candidates originally flagged: `g1`'s `random_state` was a plain **int**, and sklearn's `GaussianMixture.sample()` RE-SEEDS fresh from an int on every call instead of advancing it -- every bootstrap-null replicate drew the **identical sample** (verified: 58/60 bit-identical at n=26). The null distribution collapsed to ~one point, so `obs` landed strictly above/below it almost every rep -- p in {~0.01, ~1.0}, never a real 5% test. Fix: pass `random_state=np.random.RandomState(seed)` (an instance, which sklearn reuses/advances, not an int). Root cause on the conservative side (`task0288.lrt`) was the filed two-independent-samples bug, confirmed; **retired, not fixed in place** (one implementation, not two) -- `task0288...py` now imports the corrected `task0309.lrt`. **Calibration validated**: N(0,1), alpha=0.05, 200 reps/n, B=100: n=26 4.0% (8/200), n=100 7.0% (14/200, 95% CI [3.5%,10.5%] includes nominal), n=171 5.0% (10/200) -- all at or statistically indistinguishable from nominal. **[[TASK-0316]] re-run with the corrected test, all six cohort-arms**: only 1/6 (asbench full, n=100, p=0.0033, power=100%) is both significant and adequately powered, and that cell already carries [[TASK-0309]]'s own point-mass-not-population caveat; the other 5/6 are genuinely UNDETERMINED for lack of power. TASK-0316's original "multimodal in every cohort" headline does not survive. **`task0288`'s own controls recomputed at its n=28**: negative control clean (0/5 FP) but positive controls now show essentially NO power up to 3.0 SD (1/5 significant even at sep=3.0) -- worse and more honest than the old (bug-generated) table implied; real-data p=0.1197 stays uninformative. **Silverman reconciliation**: asbench-full Silverman (n=112, well-powered) says unimodal p=0.139; the calibrated LRT (n=100, same cohort/arm, well-powered) says multimodal p=0.0033 -- not a contradiction, they test different alternatives (KDE mode count vs. any departure from one Gaussian, including a narrow point mass); the population-vs-point-mass question is answered by [[TASK-0309]]'s own component decomposition, not by either modality test. **Register position, now earned**: modality remains UNDETERMINED; the one well-powered significant result supports point-mass + continuum, not a second broad population. Full numbers: `results/tasks/0319_lrt_calibration/lrt_calibration_fix.json`. | Implementer D | Done | Critical | 2026-09-02 | — | — | `.ai/tasks/DONE/TASK-0319-lrt-calibration-defect-both-implementations.md` |

| TASK-0321 | The hypothesis register (`.claude/hypotheses/*.md`, 21 hypotheses) is write-mostly: cited by 9% of 339 task files, 2/12 outward docs (neither the submission draft). Triggering incident: [[TASK-0320]]'s brief claimed complex hopping was "open" when [[HYP-P9]] had recorded `FAIL`, current and correct, in a file the same session had already opened. 14/21 hypotheses have no dated status; the two most-cited (HYP-P1 16 citations, HYP-P13 8) are among them. Staleness is NOT the problem (2/21 outdated). Discussed directly with the repo owner; a third failure mode surfaced beyond the brief's own A (not consulted) / B (not maintained) split -- some unjudged hypotheses may already be decided by a Done task that never wrote its verdict back (same shape as [[TASK-0307]]'s 13-task gap, recurring in a second artifact). Split into three sequenced children rather than closed by rewriting any hypothesis | Architect/Planner | Done | High | 2026-09-03 | — | — | `.ai/tasks/DONE/TASK-0321-hypothesis-register-is-write-mostly.md` |
| TASK-0322 | Built `.ai/tools/hyp_register_check.py` + 16-test seeded-violation suite (staleness + uncited-claim classes) and `.claude/hypotheses/INDEX.md` (generated, 21 `HYP-P*`/`HYP-S*` ids -- `ceiling.md`/`reference_register.md` scoped out, the latter already has its own index over a different id namespace). Matched [[TASK-0319]]'s own `doc_parity.py` wiring exactly (standalone script+test, not a hook -- repo has no CI/pre-commit to attach to, checked). Real run found 2 staleness + 4 uncited-claim hits, incl. `REVERSE_CTQW_BRIEF.html`'s own corrected text still never citing `[[HYP-P9]]` -- confirms the gate catches [[TASK-0320]]'s actual incident shape. Findings recorded not fixed (0323/0324's territory); flagged a measurement-methodology discrepancy vs [[TASK-0321]]'s own citation/status counts, not corrected (past Done task) | Implementer | Done | High | 2026-09-03 | — | — | `.ai/tasks/DONE/TASK-0322-hypothesis-consultation-enforcement.md` |
| TASK-0323 | Pathway C of [[TASK-0321]] (surfaced in discussion, not in the original brief) -- audit Done tasks for hypothesis verdicts already reached but never written back, before any fresh backfill is commissioned. Pilot on the two highest-citation unjudged hypotheses (HYP-P1, HYP-P13) before the full 14. Proposes linking edits (task id -> status line), does not apply them -- reviewable before becoming the register's authoritative content. Feeds [[TASK-0324]], which must not start blind on a hypothesis this task hasn't checked **[relocated by TASK-0355, 2026-09-09]:** 14 findings (9 ready to apply, 4 NEVER TESTED, 1 confounded) -- applied by [[TASK-0324]] | Explorer -> Implementer | Done | High | 2026-09-03 | — | — | `.ai/tasks/DONE/TASK-0323-audit-tasks-for-unlinked-hypothesis-verdicts.md` |
| TASK-0324 | Pathway B of [[TASK-0321]] -- verdict back-fill, scoped to whatever [[TASK-0323]] reports as genuinely never tested, not the full 14 unjudged hypotheses blind. Hard-blocked on 0323; may close with nothing left to do if 0323 resolves everything via linking, which is the good outcome, not a failure to find work **[relocated by TASK-0355, 2026-09-09]:** 5 in-scope verdicts (P1-P4, S2) + 9 orphaned TASK-0323 linking edits applied (P8, P11, P13, S1, S3-S7). **Correction, 2026-09-03 (Architect): the "21/21 now dated" claim doesn't hold, and neither did my own first re-check.** Rebuilding [[TASK-0322]]'s never-regenerated index first showed only 11/21 dated -- but that was the checker's own bug talking: `STATUS_RE` didn't recognize `**Status update,`/`**Status confirmed,`/`**Correction,`/`**Resolved` (exactly the phrasing this task's own linking edits used for S1/S2/S3/S4/S5/S6/P6). Fixed the regex, added regression tests, rebuilt again. **True count: 18/21 dated.** Genuinely undated: HYP-P7, HYP-P13, HYP-P14 (the latter two are this week's newest, plausibly still open) | Implementer | Done | Medium | 2026-09-03 | — | — | `.ai/tasks/DONE/TASK-0324-hypothesis-verdict-backfill.md` |
| TASK-0326 | **344/344 tasks classified and landed** (ENG 204/59%, NO-HYP 82/24% across ~45 clusters resolved via the Architect's standing rule, UNCLASSIFIED 0). Landed in `physics.md`/`search_complexity.md`: `HYP-P8`/`HYP-P9`/`HYP-P13`/`HYP-P14`/`HYP-S1` extended with dated fold-ins; 10 new hypotheses (`HYP-P15`-`HYP-P24`); `search_complexity.md`'s stale "one surviving positive" claim struck through and corrected (TASK-0216/0217: it was a seed-construction artifact); the file's own open HYP-P13 complexity-conjunction question closed directly rather than becoming a new id. `INDEX.md` regenerated, checker re-run clean. Full ledger + disposition table + landing record: `.claude/hypotheses/TASK_CLASSIFICATION_LEDGER.md` | Explorer -> Architect/Planner -> Implementer | Done | High | 2026-09-04 | — | — | `.ai/tasks/DONE/TASK-0326-classify-tasks-against-hypothesis-register.md` |
| TASK-0327 | **NOT YET CRITIC-REVIEWED -- cite only the held-out numbers, not the full-cohort ones.** Read-only scoring pass over `allosteric` branch's stored artifacts (commit f257789; Owner: Oussema, cross-check still owed) implementing `PIPELINE_DESIGN.md`'s own S3 "best-residue-wins" pocket rule (checked-in `top_pocket()` contradicts it, unused -- flagged). Full (leaky) cohort: PASSer alone ranks the drug pocket #1 at 40.6%/55.2% (pre/post-veto) vs. CTQW's 9.3%/16.2%. **Confound found on direct reviewer feedback ("the obvious comparison is the wrong one, and it flatters PASSer")**: PASSer's own papers (Xiao/Tian/Tao *Front. Mol. Biosci.* 2022 doi:10.3389/fmolb.2022.879251; Tian/Xiao/Jiang/Tao *Nucleic Acids Res.* 2023 doi:10.1093/nar/gkad303, the "ensemble" model this cache is keyed on; Tian/Xiao/Jiang/Tao *J. Comput. Chem.* 2023 doi:10.1002/jcc.27193 -- all live-verified) train on ASBench (37.5% of the cohort) but treat CASBench (35%) as their OWN external test set, not training data. **First correction over-corrected by excluding CASBench too (n=18-25, "PASSer's advantage evaporates") -- wrong; fixed against the citations** (n=44-64, properly powered): PASSer alone = 28.1%/40.9%, clearing both chance (12.8%/27.3%) and random-through-veto (18.4%/36.3%); the pipeline clears neither (8.9%/15.7%, below its own random null). Smaller than the leaky headline but the same direction, decisively. Random-order closed form cross-checked against real Monte Carlo (max err 0.019). `ALGORITHM_REGISTER.md` updated with full citations + corrected numbers; no hypothesis-register entry landed pending Critic review | Oussema (owner) -> Reviewer thread (cross-check) -> Implementer A (claimed by explicit override) | Done | Highest | 2026-09-06 | — | — | `.ai/tasks/DONE/TASK-0327-passer-only-reference-arm.md` |
| TASK-0331 | [[TASK-0320]]/[[TASK-0325]] ran on the unfiltered ASBench cohort; collaborator's unified benchmark measured only 23%/7% of curated_allosteric/drug_contact sites are genuinely distal (hop>=2, >12A) -- a propagation method tested where there is nothing to propagate across. **Resolved.** Distal set vendored from `origin/allosteric`@f257789 `allosteric/datasets/pocket_distance.csv` (`source==asbench & truth_type==curated_allosteric & is_distal==True`, 49 PDBs, 44.95% of ASBench's curated_allosteric rows -- matches the filing's "45% distal"). No re-run of fpocket/the walk needed (both filter-invariant, [[TASK-0320]]'s own identity) -- post-filtered the existing per-structure/per-candidate dumps, same pattern [[TASK-0325]] used. 45/49 distal PDBs (43 distinct) present in the existing 105-row [[TASK-0320]]b dump; `is_distal` written onto every row for auditability. **Power-first finding, per this task's own Constraint: on the n=45/35-protein distal subset, proximity itself -- this register's own dominant, everywhere-detected confound -- does NOT clear significance** (median rho +0.064, 19/35 positive, Wilcoxon p=0.89; full cohort same statistic: +0.151, p=0.00035). **The subset cannot detect its own dominant confound, so its CTQW numbers (raw median -0.005 p=0.45, |proximity -0.051 p=0.15) are UNDETERMINED, not a sharper negative** -- distinct from the full-cohort negative, which stands and is well-powered. [[TASK-0325]]'s gate ablation re-run on the same subset is qualitatively unchanged (gate still does the retention work; CTQW still does not beat random-within-gate or druggability at any width). Ligand-contamination caveat ([[TASK-0329]], still open) carried forward, not resolved -- shared equally by subset and full cohort, so the comparison is not confounded by it, but neither number is a clean distality-only measurement. CASBench's own 46 distal structures untouched (task0320b/0325 never scored CASBench; extending is a new run, out of this task's scope). Submission sentence: report both numbers together, with the subset explicitly framed as underpowered, not negative. | Implementer D | Done | High | 2026-09-06 | — | — | `.ai/tasks/DONE/TASK-0331-rerun-ctqw-negatives-on-the-distal-subset.md` |
| TASK-0335 | Scope-corrected the standing CTQW negatives that were stated wider than measured. [[TASK-0320]]/[[TASK-0325]] (reverse-seeded/gate-ablation on ASBench): added dated correction sections stating the true cohort (unfiltered, ~45% distal, 100% ligand-bound) alongside the unchanged numbers/verdicts -- not weakened, per the task's own Constraint. `documentation/REVERSE_CTQW_BRIEF.html` (the collaborator-facing twin): added a matching scope-correction note box + 2 updated bullets; no `.md` twin exists for it so `doc_parity.py` does not apply. [[TASK-0327]]/[[HYP-P18]]: checked, already corrected by another thread before this task started (commits `9c6a1e7`/dated hyp fold-in) -- no edit needed. `PHASE1_SUBMISSION_V1.{md,html}`: checked, this finding family isn't cited there at all -- no edit needed, `doc_parity.py` not run (nothing touched). One "specific correction owed" bullet (TASK-0325 "recommended PASSer") found to target the wrong file -- TASK-0325 never recommends PASSer, that lives in TASK-0327, already fixed there; recorded rather than silently dropped. Flagged, not fixed: the submission's own Appendix A claims ledger carries other ASBench-cohort negatives (TASK-0304/0305/0308/0310 lineage) with the same potential defect, outside this task's named scope **[relocated by TASK-0355, 2026-09-09]:** 2 Done-task corrections + 1 outward-doc correction landed; 3 items checked and found already-handled or not-applicable | Implementer C | Done | High | 2026-09-06 | — | — | `.ai/tasks/DONE/TASK-0335-scope-correct-the-standing-negatives.md` |
| TASK-0334 | Turned [[TASK-0327]]'s below-chance pipeline result into a tested mechanism instead of an unexplained negative. Read-only join of TASK-0327's own stored `s14_r{1,2}_k10_h2_*.json` artifacts against `allosteric/datasets/pocket_distance.csv` (vendored; byte-identical between f257789 and current HEAD 8dcc6fa, `git diff` empty; 100% join coverage 96/96 + 67/67, [[TASK-0331]]'s independent 49-PDB asbench-distal count reproduced exactly). **Confirmed**: per-protein pipeline hit-rate anti-correlates with the truth pocket's own distance from the active site (Spearman rho -0.40 to -0.61, all 8 combinations of {2 rounds x 2 cohorts x 2 metrics} p<0.002, power checked and cleared at n=44-96) -- consistent with the S2 CTQW score being the same proximity-tracking construction [[TASK-0320]] measured directly (rho=+0.808). **Specificity control clears**: PASSer (distance-agnostic ML baseline) shows no such correlation (all p>0.17), ruling out "distal proteins are just harder for everyone." Validation finding: every scoreable protein is `is_distal==True` by `PIPELINE_DESIGN.md`'s own cohort definition -- this is a within-distal-cohort dose-response, not a distal-vs-proximal split (stated, not hidden). Did not measure arm (a) (distance to the pipeline's actual wrong pick on a miss) -- needs per-candidate-pocket PDB geometry, ruled out by "no PDB refetch." Landed as new hypothesis **HYP-P25** (genuinely distinct mechanism from [[HYP-P9]]'s gated/reverse-seeded finding, cross-referenced both ways), not a fold-in. Incidental fix: `reference_arms.py`'s stale "excludes asbench/casbench" print label (logic was already correct, label wasn't) corrected in the same pass **[relocated by TASK-0355, 2026-09-09]:** New hypothesis HYP-P25 landed; TASK-0332 (submission corrections, unclaimed) now has a citable mechanism instead of "subtracts value" | Implementer A | Done | High | 2026-09-06 | — | — | `.ai/tasks/DONE/TASK-0334-is-the-below-chance-pick-distance-anticorrelated.md` |
| TASK-0336 | **The 1022-protein classical-comparison headline does not survive matching.** `classical.py` scored its own re-derived, un-vetoed pockets with a single fixed ranking each, against CTQW's max-over-221-cells (884 with the MIN_HOP sweep) on round-2 veto-survivor pockets -- two different candidate sets, two different truth definitions, wildly different multiplicity. Matched: one object (round 2's own stored `pockets`, every arm -- 3/5 classical arms need no PDB refetch at all; `proximity`/`degree` refetched for the 80 distal structures only, 0 failures), one truth definition (`n_drug>0`, classical.py's own "any overlap" rule), one PRE-REGISTERED CTQW cell (`hnew\|full\|p_avg` + `binary\|adj\|p_avg` as a check, not a max), [[TASK-0328]]'s own pocket-block null applied to every arm (not just CTQW). Planned Validation passed first: bit-exact cell reproduction (r1 and r2 minhop2) + exact family-count reproduction vs. `per_family_full_minhop2.csv` for 3 spot-checked families. **Result: CTQW's pre-registered single cell clears 4/276 families (0/48 distal) -- fewer than 3 of 5 plain classical descriptors (5/276 each); the distal margin the README led with (19 vs 1) is exactly 0 for every arm, CTQW included, after matching** (chance alone predicts ~0.52 of one distal family clearing). Folded into [[HYP-P13]] as a new evidence-table row, not a new hypothesis -- same "classical beats CTQW" claim, now shown on the dataset that looked like the exception. Deviated from one stated Constraint (used the README's own flat MIN_HOP=2 basis, not the ~117-family principled per-distance mix) with the reason stated in the task file, not silently substituted | Implementer A | Done | Highest | 2026-09-06 | 2026-09-06 | Gates the "build the AI models" decision: the regression cannot be motivated by "CTQW already beats classical 69-to-25" -- that comparison does not survive | `.ai/tasks/DONE/TASK-0336-matched-multiplicity-classical-comparison.md` |
| TASK-0337 | **Cluster-robust correction to [[TASK-0334]] (filed from `.ai/reviews/2026-09-07/REVIEW-2026-09-07-adversarial-submission-audit.md` S3.2).** `distance_anticorrelation.py` computed Spearman rho over structures, never referencing `pocket_distance.csv`'s own `cluster` column, on a cohort where `CAS0002` alone is 20%+ of the rows -- "n=44" was 44 correlated structures. Planned Validation first: re-ran the original script unmodified, byte-identical to its stored result (the row-level numbers were never in question, only the counting). Fix: `cluster_robust_correction.py` collapses each cluster to its median before the Spearman, gets a p-value from a cluster-permutation test (B=10000, [[TASK-0328]]'s `det_seed` reused for the seeding discipline, not its pocket-block null itself -- different null object) and a cluster-bootstrap 95% CI (B=10000), applied to all 4 columns x 2 distance metrics x both specificity controls. **Result: 7 of 8 combinations survive (cluster rho −0.34 to −0.50, n_clusters 22-55, permutation p 0.004-0.038); one (round 2/held-out/median_euclid, the smallest-n+weakest-metric corner) is borderline (p=0.058, CI crosses zero).** PASSer control unweakened (still n.s. everywhere); random-arm control weakens from row-level-significant to cluster-level-non-significant (softened, not reversed). Appended to TASK-0334 under a dated `## Correction` heading (no overwrite); [[HYP-P25]]'s Status updated in the same commit with a dated correction paragraph **[relocated by TASK-0355, 2026-09-09]:** TASK-0332's §2 second-positive claim must cite the cluster-collapsed numbers (7/8 combinations, not the original row-level "every combination") | Implementer A | Done | BLOCKER | 2026-09-07 | — | — | `.ai/tasks/DONE/TASK-0337-cluster-robust-correction-to-task-0334.md` |
| TASK-0345 | **The apo vs stripped-holo delta: the number nobody reports, now measured.** No blocker found before pickup (all 4 related tasks Done). Cohort smaller than the filing assumed: `cryptosite_pairs.json` (21) + `pocketminer_pairs.json` (86) = 107 nominal, but 31 PocketMiner entries have no holo counterpart or an ambiguous chain (excluded before fetching), 13 more have a `drug_code` with no actual ligand contacts -- **63 validated pairs**, not the filing's assumed >=100/1042 (CryptoBench, which would supply the gap, is deliberately not vendored anywhere in this repo -- OSF download, not fetched, stated scope reduction). Planned Validation caught a real bug first: `task0242.fpocket_candidates` resolves its output dir relative to `cwd`, not the input file's directory -- fixed before the full run. Truth pocket derived fresh (4.5A ligand contact on the holo structure) and mapped onto apo via a checked (not assumed) residue-identity gate (>=80% agreement). **Result: computationally stripping a holo structure's ligand scores ~0.2 druggability points higher (mean +0.199, median +0.184) and ranks ~1 position better than a genuine apo structure of the same protein** -- bootstrap 95% CI [+0.102,+0.296], sign-flip permutation p<1e-4, Wilcoxon p=2.6e-4, same sign in both source cohorts. Positive control (deposited holo scores highest) holds in aggregate and 48/63 (76%) of individual pairs; 15 violations spot-checked, look like real fpocket score noise not a bug. Landed as new hypothesis **HYP-P26** (distinct mechanism from HYP-P21/HYP-P8/HYP-P9, not a fold-in); citations (CryptoSite, PocketMiner) live-verified before use **[relocated by TASK-0355, 2026-09-09]:** Converts a "not reported" submission row into a real, decisive result for SS2(b)/SS4; CryptoBench fetch flagged as the natural follow-up if a larger cohort is wanted. (Completed 2026-09-08, per the reused `Claimed By` cell.) | Implementer A | Done | High | 2026-09-07 | — | — | `.ai/tasks/DONE/TASK-0345-apo-vs-stripped-holo-delta.md` |
| TASK-0350 | **Asked the interference question properly: coherent vs decoherent, matched operator/seed/cohort -- the direct test this register never actually ran** (every prior attempt -- chiral circulation, frequency-domain, HOM -- changed the observable along with the coherence). No blocker before pickup. Reused TASK-0310's own 108-structure ASBench cohort/scoring verbatim (no new benchmark). Matched classical twin: `normalised_laplacian_alpha` -- H_new's own graph term before any potential, genuinely PSD, resolving HYP-P7's own "provided H_new is PSD" caveat. **Caught and fixed a real bug**: a first version redid `eigh(H)` 5x per structure and ran 10+ hours without finishing on the full cohort -- killed on a status-update check, fixed by sharing one eigh across every arm + `flush=True` progress logging, verified numerically identical on a smoke test, re-run in 4152s (~69 min). Planned Validation passed (bit-exact TASK-0308 reproduction). **Result: coherent-decoherent resid-AUC delta n=108, mean +0.0023, Wilcoxon p=0.919, cluster-p=0.834 (76 protein clusters) -- pre-registered "small and non-significant" prediction HOLDS decisively.** Separately: **50/108 (46%) coherent and 41/108 (38%) decoherent structures flip resid-AUC sign between T=15 and the converged limit** -- a real, large finite-T instability, not averaged away. Folded into **HYP-P7** (decisive result) and **HYP-P6** (finite-T instability at scale) -- both dated status updates, no new hypothesis **[relocated by TASK-0355, 2026-09-09]:** Directly answers the S2 interference question with the cleanest test in the register's history; the finite-T finding is a caveat every prior T=15-computed per-structure verdict in this project's history now inherits. (Completed 2026-09-09, per the reused `Claimed By` cell.) | Implementer A | Done | Highest | 2026-09-08 | — | — | `.ai/tasks/DONE/TASK-0350-coherent-vs-decoherent-matched-twin.md` |
| TASK-0339 | Submission hygiene before TASK-0332 drafting starts (source: 2026-09-07 adversarial audit). **ToC fixed to exactly 7 items** -- moved §8 + reviewer front-matter to new `documentation/REVIEW_TARGETS.md` (not shipped); answered its 01 ("what is quantum here") and 06 ("benchmark or paper") questions directly in §1/§2 -- 06 is a synthesis of existing content, flagged for human sign-off before freeze, not a new strategic call. **Page budget**: cut §7 709->522 words (kept the QA-provenance argument + adversarial-split table per the task's own protection); net +47 words on the ~2233-word body after funding the required c-Myc/Braket additions, document total -10% after removing front-matter/§8; no PDF renderer available in this environment, word-count proxy used and disclosed, real render recommended before freeze. **Added**: c-Myc (1NKP) paragraph in §1 (4-operator consensus, real artifact); corrected the task's own suggested Braket/Classiq sentence after checking [[TASK-0182]]/[[TASK-0221]] -- neither was ever run, both are Phase-2-only per the organisers, wrote the true sentence instead of the false one suggested. **§6**: drew a real pipeline diagram (ascii + SVG twins); kept the demo/report P@5 contradiction disclosure after confirming it live against `origin/main` (still true), reduced from the section's only content to a scoped note beside the new diagram. **Archived** `PHASE1_SUBMISSION_DRAFT.md` -> `ARCHIVE-PHASE1_SUBMISSION_DRAFT-v0.md` (already had a superseded banner; ~20 Done tasks' historical citations of the old name left unfixed, disclosed). **Repo stats pinned**: 359/328/580 as of `ffcfaca`. **Found and fixed its own regression**: deleting §8 removed the only source of literal "03"-"06" tokens in the .md, unmasking that the .html's decorative zero-padded section badges never really matched -- root-caused (confirmed via `git stash` this was newly introduced, not pre-existing) and fixed by dropping the leading zero, not by padding filler text **[relocated by TASK-0355, 2026-09-09]:** Unblocks [[TASK-0332]]'s drafting; `REVIEW_TARGETS.md` created as the register's standing place for reviewer-only content going forward | Implementer C | Done | High | 2026-09-07 | — | — | `.ai/tasks/DONE/TASK-0339-submission-hygiene-and-page-budget.md` |
| TASK-0340 | Review §3.1 called the container a BLOCKER (COPYs `results/` paths the `.gitignore` doesn't negate) and §3.10 flagged `doc_parity.py` as never wired to anything. **Both resolved.** §3.1: the three paths are `git`-tracked (force-added by [[TASK-0333]]) regardless of the ignore rule, so no negation was needed -- but that alone wasn't proof, so cloned the actual `origin` remote fresh to a scratch path, checked out `bartosz`, confirmed 105/105 `.npz` + both upstream JSONs present from the clone alone, then `docker build`+`docker run` from that clone, cold: `0.5948718035160693` byte-identical to [[TASK-0318]]'s stored value. The reproducibility sentence is now safe for [[TASK-0332]]'s draft. §3.10: new `.github/workflows/submission-parity.yml` runs `doc_parity.py` on `PHASE1_SUBMISSION_V1.{md,html}` on every push/PR touching either twin, plus manual dispatch -- matches the review's own draft, extended to `pull_request` so a GitHub-direct edit (`CLAUDE.md` convention 6) is covered too. Two other review findings (§3.6 Submission Guidelines "missing", §3.4 `no_proximity_feature_check.json` "missing") confirmed already-resolved/mis-scoped, recorded so they aren't re-filed | Implementer D | Done | High | 2026-09-07 | — | — | `.ai/tasks/DONE/TASK-0340-cold-clone-verification-and-parity-ci.md` |
| TASK-0338 | **Part A**: [[TASK-0336]]'s family-clearing table (CTQW 4/276, three classical descriptors 5/276) had `real_excess = obs - chance` and nothing else -- no CI, no p-value. Added exact Poisson 95% CIs (heavily overlapping: `[1.09,10.24]` vs `[1.62,11.67]`) and a paired exact test (McNemar on the identical 276 families every arm scores -- 1 discordant family, **p=1.0**), reusing the script's existing per-structure null draws, no re-run needed. **Honest reading: under matched multiplicity, no arm -- quantum or classical -- clears more than 5/276, and the arms are statistically indistinguishable.** [[HYP-P13]]'s Status updated with the correction; [[TASK-0332]]'s staged exclusion sentence rewritten from "classical beats CTQW" to the methodological argument (multiplicity, not a one-family scoreboard loss) so a referee can't turn it around. Also renamed `degree`/`proximity(-hop)`'s mislabeled `"ALL"` (duplicated `"distal"`'s 48-family count; those two arms are refetch-scoped to the 80 distal structures only) to `"distal_only"`. **Part B**: [[TASK-0318]]'s committed `no_proximity_feature_check.json` (the 0.6017 number) had no script producing it -- correcting the review's own claim that the file itself was missing (it isn't). Added `--exclude-proximity` to `task0318_input_space_ceiling.py`; reproduces the committed JSON byte-for-byte from the existing feature cache, no PDB refetch, ~2 min. Fixed an incidental feature-importance mislabeling bug (zipped against the full 19-name list instead of the kept subset) hit while wiring the flag. Every pre-existing number in both result JSONs verified byte-identical before/after **[relocated by TASK-0355, 2026-09-09]:** Both §2 inputs now carry real uncertainty/provenance before [[TASK-0332]] drafts them; TASK-0332's own text corrected in the same commit | Implementer B | Done | High | 2026-09-07 | — | — | `.ai/tasks/DONE/TASK-0338-uncertainty-on-the-two-remaining-section-2-inputs.md` |
| TASK-0332 | Submission v2: Berke's structural corrections, promote [[TASK-0318]], team §7. **Resolved, both twins, one commit, `doc_parity.py` verified.** MYR/1OPL reframed from "unexplained ligand" to the physiological myristate mechanism (Nagar et al. 2003), asciminib named as the existence proof, adjacent to it as instructed; GLUCOKINASE/PKR left genuinely unexplained. New cryptic/allosteric taxonomy table in §1 (BCR-ABL1 allosteric-not-cryptic, KRAS both, cardiac myosin allosteric-not-cryptic-not-even-single-molecule -> Appendix C) plus the collaborator's own median-hop=0 cryptic-datasets finding stated as "cryptic != distal, now measured." Clinical paragraph landed in §4. §2 restructured into "four positive results" in the Brief-update's specified order -- **[[TASK-0337]]'s cluster-robust CORRECTED numbers used for positive 2** (7/8 combinations, not the filing's own original row-level "-0.408/-0.614" table), TASK-0318 retitled away from "ceiling" language and its cold-clone byte-identical reproduction ([[TASK-0340]]) stated directly, TASK-0331/HYP-P21's three-way cohort defect, TASK-0328's pocket-block null (45/110->0/110). Propagated the corrected 0.5949/0.6203 numbers into §4's table and Appendix A (both previously stale at "0.595"). Team table/bios: Berke's label+role corrected, both bios added replacing the INCOMPLETE placeholder, condensed per instruction. Three new Appendix C disclosures (apo-ligand veto 4.2x leak, ASBench/PASSer training contamination, TASK-0328's unreproducible hash() seed). **Not done, disclosed rather than fabricated**: lifting Berke's "why classical detection fails" paragraph into S1 -- its actual text was never supplied into the repository, only referenced. **Page budget measured, not hidden**: body now 3174 words vs [[TASK-0339]]'s 2233 baseline (+42%), very likely over the 6pp limit at that task's own words-per-page estimate -- a further trim-and-render pass with an actual PDF renderer (none available here) is recommended before freeze | Implementer D | Done | High | 2026-09-07 | — | — | `.ai/tasks/DONE/TASK-0332-submission-v2-corrections.md` |
| TASK-0341 | Deterministic PDF render + page-budget gate for the submission (Toolsmith). **Done.** `.ai/tools/submission_build.py` — one command produces a numbered PDF in `__WORK_IN_PROGRESS__/documentation/_build/` (gitignored) + a screen-sized report; compliance runs as a side effect (A4, <=6pp body / <=3pp appendix, >=10pt font), plus a change report vs a git ref reusing `doc_parity.py`'s own extractors (figures/headings/identifiers/per-section word delta). Renderer: headless Google Chrome `--headless=old` (`--headless=new` deadlocks against a running Chrome on macOS) printing the CI-parity-enforced HTML twin — no `pandoc`/`weasyprint` install, no third artifact. Polls for a stable output file instead of waiting on Chrome to exit. Local-only by design (exits 3 cleanly if Chrome/pdfplumber absent — no new CI job); `pdfplumber` dev-only, not in `requirements.txt`. 16 tests + `doc_parity` regression = 30 pass; acceptance-bar cases ([[TASK-0319]]) are pure-function (seeded over-length FAILs, seeded number-change detected). **THE NUMBER (committed HEAD `b7a6a99`, injected print CSS applied): body 9/6 FAIL, appendix 8/3 FAIL — 17 pages vs a 9-page allowance; 1229 chars <10pt (chrome + table headers + chips). CORRECTED by [[TASK-0342]]: the split-detection regex had a false positive on an inline "(Appendix C)" body-prose citation — see TASK-0342's row for the real 12/6, 5/3 split; do not cite the 9/6-8/3 figures.** `CAPABILITIES.md` gains a "Submission Document Capability Set" section | Toolsmith | Done | High | 2026-09-07 | — | — | 17-vs-9 overage is the trigger in this task's own Consequence section: [[TASK-0332]]'s drafting plan needs structural cuts (owner/Reviewer), not word-count trimming — flagged, not acted on | `.ai/tasks/DONE/TASK-0341-submission-pdf-render-and-page-budget-tool.md` |
| TASK-0342 | Human review found the built PDF's tables silently clipped mid-word (`.tw{overflow-x:auto}` scrolls on screen, clips on paper). **Done, both findings fixed.** (1) Print-only reflow: `table-layout:auto`/`white-space:normal`/`overflow-wrap:anywhere` so tables wrap instead of overflowing; new `overflow:visible` + `overflow-wrap:anywhere` in `submission_build.py`'s injected CSS. Font floor extended to `code` (`.855em` relative sizing shrank it below 10pt even inside compliant containers — found by re-measuring, not by reading the source). New "no horizontal overflow" compliance check (0 words past the printable margin), documented as unable to see content some *other* not-yet-found overflow:hidden box clips to nothing. (2) **A second, more serious bug found while verifying the first**: [[TASK-0341]]'s appendix-split detector searched rendered prose for "Appendix" and false-matched S1's own inline "(Appendix C)" citation — confirmed live, reproduced in isolation — meaning TASK-0341's originally reported page-split (9/6 body, 8/3 appendix) was never a real measurement. Fixed with an invisible unique literal token injected as the first child of `#appendix`, searched for verbatim instead of matching prose (a `position:absolute` first attempt was itself wrong — confirmed live it never appeared in any page's text at all; fixed by leaving it in normal flow). Dated correction added to TASK-0341's own Done section, original text left unedited. **THE CORRECTED NUMBER (HEAD `5c9da67`, reproduced across 2 renders): body 12/6 FAIL, appendix 5/3 FAIL — same 17-page total as before, but the true split is 12/5, not 9/8.** Clipping now 0 violations; small text 424->195 chars, residual is the S6 SVG diagram's labels (vector-scaled, not CSS-floorable) and `<sup>` exponents (conventional), both disclosed, neither chased further. 38 tests (added: clipping FAIL/PASS/tolerance, appendix-marker unit + false-positive regression, CSS-fix-presence), full suite green | Toolsmith | Done | BLOCKER | 2026-09-07 | — | — | [[TASK-0332]]'s drafting still needs the same structural-cut decision TASK-0341 flagged; the true overage (12/5 vs the 6/3 limits) is worse on the body side and better on the appendix side than first reported | `.ai/tasks/DONE/TASK-0342-print-css-clips-every-table.md` |
| TASK-0343 | §4.1 Team Profile is a separate submission component; §7 Team Capability wrongly carried full biographies. **Resolved.** Verified §4.1's three bullets directly against the Guidelines, not from the filing's own summary. New `documentation/TEAM_PROFILE.md`: team name (AuraQu), full bios for all three members, prior quantum/domain experience stated explicitly per bullet 3, the full AI-workflow disclosure elaboration (adversarial-split table, repo/branch table, four-sentence rationale) moved there. **Lead contact details flagged as genuinely missing, not invented** -- needs the repo owner before portal upload. §7 rewritten 599 -> 209 words (member table kept, one short methodological/AI-disclosure paragraph, pointer to the Team Profile for the rest). **Checked and corrected the task's own assumption**: Berke's "why classical detection fails" paragraph is NOT in §1 as the filing assumed -- `grep` across both twins and REVIEW_TARGETS.md finds nothing; per [[TASK-0332]]'s own disclosure, the actual text was never supplied to the repo, so it was never written anywhere, not duplicated. `doc_parity.py` clean after the edit. **Planned Validation, measured honestly**: `submission_build.py` body pages 12 -> 11 -- a real one-page saving, smaller than the filing's own "single largest structural saving" prediction; closing the remaining 5-page gap is [[TASK-0344]]'s explicitly separate scope | Implementer D | Done | High | 2026-09-07 | — | — | `.ai/tasks/DONE/TASK-0343-team-profile-is-a-separate-component.md` |
| TASK-0344 | 17 pages against a 6+3 limit (12/6 body FAIL, 5/3 appendix FAIL, re-measured fresh after [[TASK-0342]]/[[TASK-0343]] landed, per this task's own sequencing). **Resolved: RESULT: PASS.** Free levers only, all print-only in `submission_build.py`'s injected stylesheet, source HTML untouched: body font 12.4->10.5pt, line-height 1.62->1.22, margins 16/15/18/15mm->11/11/12/11mm, section spacing 84px->8px, paragraph/list margins 1.05em->0.6em, h2/h3 1.72rem-> 1.3rem/1.0rem (rem-based, confirmed the body-font lever alone doesn't touch them). Two regressions caught mid-lever and fixed, not shipped: table font naively scaled with body font fell to 9.3pt (under the floor) -- reverted, left untouched; `h2 .sub` fell under 10pt once h2 shrank -- added to the existing small-text floor list. Content: Appendix A/B/C compressed to fragments (full methodology stays linked per §4.4); §2's (a)/(b)/(c) converted prose->table per the cut-order's own "tables over prose"; §1/§2/§4/§5/§6 tightened; **§3 grown 153->309 words per this task's own explicit protection** (added a real data/compute/software paragraph, Guidelines §4.3 item 3's own ask, not padding). **A real dangling cross-reference caught while compressing**: §1's taxonomy table cited "(Appendix C)" for cardiac myosin's single-molecule limitation but no such bullet existed -- added it. **Planned Validation's human-PDF-check actually run, not skipped**: rendered all 9 pages to PNG and read each one, found and fixed two real visual defects the numeric checks cannot see -- the S6 SVG diagram's rightmost text sat past its own viewBox edge (widened 760->800) and `overflow-wrap:anywhere` (TASK-0342's own fix) squeezed narrow table columns into mid-word breaks (team names, verdict chips, headers) -- fixed with three targeted min-width/nowrap rules rather than removing `anywhere` and reopening the bug it fixed. `doc_parity.py` clean throughout; 24+14=38 tests pass, matching TASK-0342's own count, no regression. **Left open, correctly not decided here**: whether Appendix A/B/C qualify as §4.4 supplementary material at all -- this task's own filing named it a repo-owner scoping question; compression alone reached compliance without forcing it | Implementer D | Done | BLOCKER | 2026-09-07 | — | — | `.ai/tasks/DONE/TASK-0344-cut-to-the-page-limits.md` |
| TASK-0346 | Run [[TASK-0209]]'s pre-registered blind validity rule (apo closed, holo open, ligand stripped) at scale, plus the endogenous-ligand audit generalized. **Resolved.** Did not fetch the ~1042-pair unified benchmark (never vendored here) -- reused [[TASK-0345]]'s own frozen 63-pair cohort (cryptosite 21 + pocketminer 42), same cached PDBs, same gates. **Result: the submission's "7 audited, 2 pass" (28.6%) headline is a property of the mandated target list, not of cryptic-pocket benchmarks generally** -- the same rule passes 52.4% of cryptosite pairs, 47.6% of pocketminer pairs, 49.2% combined (63 pairs / 59 distinct proteins; 47.5% cluster-collapsed per [[TASK-0337]]). A real bug caught by this task's own Planned Validation before trusting the run: first draft scored holo AS DEPOSITED instead of ligand-stripped, flipping CARDIAC_MYOSIN INVALID->VALID; fixed (both apo and holo ligand-stripped, matching this task's own filing text), plus a chain-letter fix for CASPASE1 (F1G sits on chain B of 2FQQ, not A). **After both fixes, all 7/7 register targets reproduce [[TASK-0209]]'s exact recorded verdict** -- the Planned Validation this task required. Endogenous-ligand audit: 10/63 (15.9%) apo structures carry a non-water HETATM near the site (vs ASBench's 40/40, [[TASK-0329]]) -- occupancy does not predict the apo-hit failure mode here (Fisher p=0.51, n=10, underpowered). Landed as new hypothesis **HYP-P27** (distinct from HYP-P26). **This changes a number the submission currently quotes -- flagged for the draft explicitly, not left implicit** **[relocated by TASK-0355, 2026-09-09]:** Submission's S1/S4 "7 audited, 2 pass" framing needs replacing with the narrower, correct claim (mandated targets specifically underperform a real benchmark population) before the next drafting pass -- not acted on here | Implementer C | Done | High | 2026-09-08 | — | — | `.ai/tasks/DONE/TASK-0346-blind-validity-rule-at-scale.md` |
| TASK-0349 | `--appendix-heading` broke the LaTeX compile ("tectonic exited 1") -- the insertion branch had never executed in a passing build before this (every prior build reported "no marker found"). **Resolved.** First step per the task's own required order: made the failure legible before guessing -- added `--keep-logs` (missing; tectonic writes no `.log` on failure without it, despite claiming to), re-ran the exact bisected command, read the real transcript: `Missing $ inserted` at the marker line. Root cause: the shared `_APPENDIX_MARKER_TOKEN` (`SUBMISSION_BUILD_APPENDIX_START_7f3a9c`) contains raw `_` -- harmless in the HTML route's `<span>`, but `_` is LaTeX's math-mode subscript operator in plain text. `xcolor` being loaded was correctly ruled out by the filing; the real defect was one line further in. **Fix: escape `_` to `\_` only in `submission_build_latex.py`** (the HTML route's literal underscores stay correct as-is -- the two routes need different escaping of the same logical token, not a different token); verified detection still works since `\_` typesets to a literal `_` glyph in the extracted PDF text. Re-ran the bisected command post-fix: compiles, PASS, appendix pages 2/3 reported correctly, split at page 5. `compile_latex()` now always passes `--keep-logs` and names the `.log` path in any failure message. **Ships with a fixture that actually exercises the split** (new end-to-end test, caught its own fixture bug on first run -- a title containing "Appendix" collided with the real search), plus a direct regression guard against the raw token ever reappearing. 48 tests pass (up from 22 for this file, 46 total before) **[relocated by TASK-0355, 2026-09-09]:** Unblocks moving the 15 references out of the 6-page body via the appendix split, freeing headroom before [[TASK-0345]]/[[TASK-0346]]'s results land | Implementer C | Done | Medium | 2026-09-08 | — | — | `.ai/tasks/DONE/TASK-0349-appendix-split-marker-never-compiled.md` |
| TASK-0347 | LaTeX/Tectonic two-column render for the concept proposal, replacing the CSS multi-column fallback after the page counter was measured wrong by >2x on it (3 physical pages reported as 7/6). **Done** -- Tectonic chosen (self-contained binary, fetches only the packages used), pandoc as the Markdown->LaTeX path so the `.md` stays the single authored source. | Toolsmith | Done | High | 2026-09-07 | 2026-09-08 | Numbering fixed separately by Reviewer thread (`secnumdepth 0`: pandoc maps `#`->section/`##`->subsection, so the mandated 1-7 were being renumbered as 1.1). Build now A4, 6/6 body pages, 10.5pt, no overflow, PASS. | `.ai/tasks/DONE/TASK-0347-latex-toolchain-for-two-column-submission.md` | — |
| TASK-0351 | Document the structure rationale the organisers explicitly asked for (*"Please document the rationale in your submission"*, 2026-08-26) -- currently absent from the draft, which names `4LDJ`/`1OPL`/`1NKP` and cites the clarification nowhere. Also carries the one message still to send: narrowed **(e)**, still-unanswered **(d)**, and two items worth reporting -- **`6C1H` contains no mavacamten** and **`8S8C` is holo** (MK-1084-bound, RCSB-verified live, [[TASK-0270]]). | Reviewer thread / next drafting pass | TODO | High | 2026-09-08 | 2026-09-08 | **(f) is answered and in our favour** -- PocketMiner permitted, [[TASK-0269]] unblocked. **(e) answered only for ENM**, not for the minimisation/Monte-Carlo question actually asked -- section 2 component (c) depends on the difference, so not blanket permission. | `.ai/tasks/TODO/TASK-0351-organiser-requested-structure-rationale.md` | — |
| TASK-0352 | Three-way consistency check (locks / on-disk task files / this registry) + a decision-index scoping call -- deliberately not a search tool: a search tool makes a negative result authoritative, and an incomplete confident negative licenses the assumption it was built to prevent. **Done.** Part B shipped: `.ai/tools/task_reconcile.py`, 19 tests. Part A investigated, not built as specified -- see [[TASK-0354]]. **[relocated by TASK-0355, 2026-09-09]:** Real first run: **147 disagreements** -- 26 `malformed_registry_row` (this row was one of them: fixed to 9 columns in this same edit), 108 `file_no_registry_row` (roughly TASK-0095-TASK-0353, checked against this file's own "must not silently drift" rule, no exemption found), 1 concrete row bug (TASK-0316's Path cell is a literal un-expanded `*`). [[TASK-0354]] (Part A reframing) and [[TASK-0355]] (the Claimed-By/At-cell-misuse this row itself exhibited, 19 rows, real data at risk from a plain `sync` run) filed as follow-ups | Toolsmith | Done | Medium | 2026-09-09 | — | — | `.ai/tasks/DONE/TASK-0352-decision-index-and-registry-reconciliation.md` |
| TASK-0353 | A submission version ledger -- one row per change: what changed, why (task or review id), and what must not regress. Filed after V2, a ground-up rewrite, silently dropped c-Myc, a mandated minimum-set target [[TASK-0339]] had added to V1 four days earlier; caught by the repo owner rather than by us. Seeded with the known V1->V2->V3 entries. | Reviewer thread / each drafting pass | TODO | High | 2026-09-09 | 2026-09-09 | Internal artifact, not shipped. Pairs with [[TASK-0352]] Part B rather than duplicating it: that reconciles task state, this records document decisions. Prior version files stay on disk -- deleting them destroys the comparison the ledger exists to make. | `.ai/tasks/TODO/TASK-0353-submission-version-ledger.md` | — |
| TASK-0356 | `commit-guard --expect-empty` cannot pass after `claim.py move`, because `git mv` stages both sides of the rename while `move` is deliberately exempt from needing GIT-COMMIT -- closing a task then committing it (the normal order) made step two of the documented sequence fail every time, so threads bypassed the guard; already caused one wrong commit (another thread's staged file swept in) and one independently-invented workaround. **Done.** Revised sequence: the ordinary post-`move` case skips `--expect-empty` entirely (still correctly fails there, not weakened -- `stage --expect`'s own self-verification gives the same check one step later). New `commit-guard --expect ... --commit --message-file PATH` fuses check+commit atomically, closing the separate, worse defect found in the same filing: `pipefail` off by default in this shell meant piping `commit-guard`'s output through `tail -1` before the `&&` tested `tail`'s exit status, not the guard's, and committed anyway on a *failing* guard -- twice. Found and closed a gap the filing didn't anticipate: `--commit`'s own `git commit` subprocess is invisible to `git_commit_guard_hook.py`'s PreToolUse match (child process, not the matched Bash command) -- new `verify_git_commit_session()` re-checks GIT-COMMIT session identity in-process before committing, so `--commit` isn't a silent bypass of TASK-0042's own protection. 8 new tests (134 total). This commit dogfoods the new flow | Toolsmith | Done | High | 2026-09-09 | — | — | `.ai/tasks/DONE/TASK-0356-expect-empty-is-incompatible-with-move.md` |
| TASK-0354 | [[TASK-0352]] Part A's own scoping decision, filed rather than built: a passive decision index risks the same "confident negative" failure Part A's own task argued against, since "decisions" have no structural tag anywhere in this repo the way `HYP-Pxx` hypotheses do. Recommends build-time assertions instead (per [[TASK-0352]]'s own addendum), starting with a `config/targets.yaml` genotype/apo-verdict assertion. **Done.** Candidate 1 built: `allostery.clean.assert_genotype_identity`, wired into `clean_from_config`, new `genotype_check` field on KRAS_G12C, 8 new tests incl. the real seeded TASK-0270 mistake (4OBE) caught live. Found and fixed a live regression of the exact class this task guards against: `test_kras_g12c_anchors` had pinned the wrong (pre-TASK-0270) apo id, red for 2+ weeks unnoticed. Candidates 2/3 audited (their own lighter bar): candidate 2's premise was itself wrong -- [[TASK-0352]]'s table cited TASK-0130, not TASK-0140 (the real chiral-walk task); corrected via a dated addendum on TASK-0352, then built the real hook (a `propagators.py` docstring pointer to [[TASK-0350]]). Candidate 3 re-checked and found already resolved by [[TASK-0348]] since this task was filed -- noted, no action needed | Toolsmith | Done | Low | 2026-09-09 | — | — | `.ai/tasks/DONE/TASK-0354-decision-assertions-not-a-passive-index.md` |
| TASK-0355 | `claim.py sync`'s next real (non-`--check`) run would have silently deleted unique content hand-misused as free-text result/handoff summaries in the `Claimed By`/`Claimed At` cells of this file's own rows -- found while validating [[TASK-0352]]'s numbers against `sync --check`. **Done.** Scope grew from the filed 19 rows to 22 by pickup (2 more rows, including [[TASK-0352]]'s own, had made the identical mistake since filing; 1 of the 22 was this task's own live claim, not misuse). 15 of the 21 real-misuse rows carried genuinely unique content (not duplicated elsewhere in the row) -- relocated into `Description` under a dated marker; 6 were exact duplicates of `Assigned To`/`Last Active` already in the row, needing no edit. Ran a real `sync`: `sync --check` now reports 0 claim-column changes. Retracted the never-real "ninth-column handoff-note" convention this task's own filing had cited (it pointed at a `malformed_registry_row` `sync` can't even parse) -- added a corrected `Current Rules` bullet naming `Description` as the one real home | Toolsmith | Done | Medium | 2026-09-09 | — | — | `.ai/tasks/DONE/TASK-0355-claimed-by-at-cells-hold-unique-content-sync-would-delete.md` |
| TASK-0348 | **The mandatory centrality ablation this project had never run.** Mohtashim/Sajjan/Kais (JACS 2026, DOI 10.1021/jacs.6c08053) publish a near-identical CTQW construction and report rho≈0.95 vs eigenvector centrality, no quantum advantage -- making that comparison mandatory here too. Added `eigenvector_centrality`/`closeness_centrality` to `baselines.py` (a real bug caught by the new unit tests: `nx.eigenvector_centrality_numpy` raises on a disconnected graph in this networkx version, fixed per-connected-component). Ran on TASK-0318's own 105-structure/74-protein cohort; Planned Validation passed 105/105 exact-match against the feature cache before trusting the new centralities. **Result does not simply confirm the paper**: median rho=0.41 vs eigenvector centrality (not ~0.95), and AUC is not uniformly null -- CTQW beats degree/GNM-alone/eigenvector centrality (cluster-permutation p<0.05) but ties betweenness/closeness (p=0.50/0.93). Reading is consistent with HYP-P13, not contrary to it: CTQW beats exactly the seed-blind arms. Folded into HYP-P13 as a dated Status update (sharpened, not new); corrected `PHASE1_SUBMISSION_V2.md`'s own draft sentence, which had claimed our measurements were "consistent with" the published rho. **Process incident, filed as a standing rule**: the first run sat silent for 2+ hours (fully-buffered stdout) -- killed and rewritten with flushed per-structure progress/ETA and incremental checkpointing, which also surfaced and fixed a real 2x inefficiency (an expensive computation called twice per structure). New rule in Current Rules below **[relocated by TASK-0355, 2026-09-09]:** Both DOIs live-verified via Crossref, added to `REFERENCES.md`; 8 new baseline tests | Implementer B | Done | Highest | 2026-09-08 | — | — | `.ai/tasks/DONE/TASK-0348-mandatory-centrality-ablation.md` |
| TASK-0357 | **The only live candidate for a positive quantum measurement before 2026-09-15 -- replicated, and it collapses.** External `allosteric`-branch claim: +0.114 distal AUC (p=0.004) for `O(r)=2*Re<r|exp(-iH tau)|a>`. Implemented (`_quantum_initial_coeffs`'s own coherent seed-state convention, not a new one), Planned Validation passed twice (harness reproduces TASK-0308/0350's committed converged numbers exactly; time-averaged `O(r)` vanishes toward 0 as the window grows, matching TASK-0130 -- caught a real bug in an earlier version of that check first: averaging `\|O(r,t)\|` instead of the signed quantity tests the wrong thing). **Sign pre-registered before scoring**: no defensible physical argument survives at the operating `tau` scale (the only tractable closed form is short-tau perturbative, and `tau=min_adequate_t_max` is deliberately large, not small) -- `\|O(r)\|` unsigned primary per the task's own fallback, raw signed secondary with no data-driven sign fit (a majority-sign fit would itself be the same "sign chosen to fit the instability" leakage the task warns against). **Result on TASK-0350's own 108-structure/76-protein ASBench cohort** (Planned Validation's own code path; the 276-vs-399-family cohort-match to the external number is explicitly out of scope, not resolved here): at the pre-registered `tol=1e-2`, resid-AUC excess +0.029 mean, cluster-permutation **p=0.036** -- nominally significant. A post-hoc (disclosed, not pre-registered) sensitivity sweep across nearby `tol` (1e-3/5e-2/1e-1) gives p=0.46/0.52/0.96 -- **the result is isolated to the single pre-registered tolerance and does not survive one order of magnitude either direction: a tau-selection artifact, not a signal.** Folded into [[HYP-P6]] as a dated Status update (same mechanism, not new): the "principled" clock has its own free knob. Doc edit done, reflecting the actual (collapse) result, not the hoped-for one: `PHASE1_SUBMISSION_V3.md`'s "closed nine candidate advantage routes" line now names the tested tenth and its outcome, with TASK-0157's ceiling in the same sentence. Secondary arm (amplitudes vs probabilities, spectrally-matched control) deferred -- the Reviewer's hold stays in place, not lifted | Implementer B | Done | High | 2026-09-09 | 2026-09-09 | Closes the "only live candidate for a positive" question decisively as a clean methodological negative, not an open lead; cohort-match to the external number and the secondary +0.031 arm remain for whoever picks those up next | `.ai/tasks/DONE/TASK-0357-finite-delay-phase-observable-principled-clock.md` |
| TASK-0358 | **[[TASK-0357]]'s tau rule was wrong; a proper scan closes the tenth candidate route cleanly instead of ambiguously.** `min_adequate_t_max` is a *convergence* window, not a point delay -- 0357 evaluated the observable exactly where its own phase content is smallest; and `tau` depends on **log** tolerance, so 0357's own 100x tolerance sweep was only a ~3x sweep in `tau`. **Fixed**: scanned `tau(protein,f) = f*min_adequate_t_max(protein, tol=1e-2)` on a pre-registered per-protein-relative grid, `f in {0.001..100}` (11 points, log-spaced, ~5 orders of magnitude in tau), reusing TASK-0357's own script/cohort by direct import. Planned Validation: `f=1` reproduces TASK-0357's committed numbers to 5 decimal places. **Result: SIGNED arm (the external claim's own reported observable) -- 0/11 grid points significant, cluster-p 0.24-0.98 throughout, a flat null across the entire range, not a near miss anywhere.** UNSIGNED arm's earlier p=0.036 -- now shown to be exactly the isolated-spike artifact 0357 could not itself distinguish from a real band: significant at exactly 1/11 points (`f=1`), zero support at either neighbour. Corroborating detail: unsigned `rho` vs proximity falls monotonically 0.92->0.24 across the scan, matching the short-tau perturbative prediction and confirming the implementation behaves as expected across the whole axis. **Verdict: the finite-delay observable carries no tau-stable signal anywhere -- the tenth route is closed, not deferred.** Folded into [[HYP-P6]] as a further dated Status update. `PHASE1_SUBMISSION_V3.md` rewritten again in the same commit: no longer "the route we carry into Phase 2" -- states the clean-null scan result, TASK-0157's ceiling kept in the same sentence. Not resolved, disclosed not dropped: cohort-matched comparison to the external number, and the held `+0.031` secondary claim -- both holds unchanged | Implementer B | Done | Highest | 2026-09-09 | 2026-09-09 | Closes the "only live candidate for a positive quantum measurement" question decisively; cohort-match and the secondary arm remain for whoever picks those up next | `.ai/tasks/DONE/TASK-0358-finite-delay-observable-in-the-phase-alive-band.md` |
| TASK-0359 | **Extended the cohort by protein, not structure -- and the protein-identity leakage the task set out to test is real, on our own cohort, not just the external one.** Added **54 new distinct proteins** (CASBench, hand-adjudicated dedup against the existing 76 -- caught two real duplicates a naive name match missed, incl. PTP1B under an alias), 129 clusters total (up from 76). Contamination audit (4.5A of the truth site, [[TASK-0329]]'s own convention): extension is CLEANER than the original (42.6% vs 80.6% -- the latter doesn't reproduce TASK-0329's own quoted "40/40" because that was a 40-structure sample checked for ANY HETATM anywhere in the file, a looser test; disclosed, not reconciled further). **Re-run nulls both hold/strengthen on ~70% more independent proteins**: TASK-0350's coherent-vs-decoherent delta cluster-p=0.425 (was 0.834); TASK-0358's f=1 signed arm cluster-p=0.822 (flat, as before), and critically the UNSIGNED arm's earlier marginal p=0.036 washes out to p=0.276 exactly as TASK-0358's own "isolated spike" diagnosis predicted. **The new measurement fires**: `protein_baseline_auc` (a "classifier" using ONLY which protein a residue came from, zero within-structure position info) reaches **AUC 0.65, p<5e-5** pooled on the original 108, **independently reproduces at AUC 0.60, p=0.0006 on the 54 brand-new proteins alone**, and does not shrink on the union (0.65 -> 0.65). This does NOT indict this register's own headline numbers (per-structure-averaged AUCs are immune by construction) but proves the CAS0002-style pooled-statistic leakage the task named is a real, sizeable (AUC-0.65-scale) channel, not a hypothetical one -- large enough to explain the external branch's own +0.107/+0.114 claims without any genuine localization behind them. Landed new **HYP-P28**; folded into HYP-P6/HYP-P7 as dated Status updates. Caught and fixed a real hang (bare `urlretrieve`, no timeout, on GroEL) within seconds of it stalling, per the standing progress-visibility rule -- not left to run 2 hours | Implementer B | Done | High | 2026-09-09 | 2026-09-09 | The register now knows its own pooled-statistic vulnerability is real, not just the other branch's -- relevant to any family-level number either side quotes going forward; root-causing the mechanism and auditing which published numbers are pooled vs. per-structure-averaged remain open | `.ai/tasks/DONE/TASK-0359-cohort-extension-by-protein-not-by-structure.md` |
| TASK-0360 | **[[TASK-0306]]'s 84%->17.8% decomposition, reproduced in a second domain -- and it took all FOUR detectors, not the mandatory three, to see it.** fpocket/PASSer/p2rank run on one structure set/truth/overlap rule ([[TASK-0336]]'s lesson): 100 ASBench structures (96 distinct PDBs), zero coverage failures for any of the three (96/96 each). Alone, they look shallow (>=1 of 3 = 95.0%, all-3 = 77.0%, pairwise Jaccard 0.81-0.97 -- the deflationary reading this task's own pre-registration named). **Adding PocketMiner (92/96 coverage, 4 genuine model failures, ran within its own pre-registered 30-min time-box) flips the picture: >=1 of 4 = 94.8%, all-4 = 24.0% -- a ~4x gap closely matching the field's own 84%->17.8% shape.** PocketMiner's own hit rate is 27.1% against 93.8%/92.7%/78.1% for the other three, Jaccard with each just 0.28-0.31 vs 0.80-0.97 among the other three -- **not noise, a different question**: the other three detect static geometric cavities, PocketMiner predicts which residues participate in a pocket that OPENS (dynamics-based, closer to this project's own cryptic-pocket framing). Both cascades reported, neither substituted for the other. fpocket provenance re-checked before trusting anything (Docker image SHA256 matches its own most recent TASK-0285 pin -- not drifted). **Two real bugs caught before shipping a wrong number**: PocketMiner's output format (`.txt`, not `-preds.npy`, assumed from a different call site) and a skip-docker optimization that accidentally skipped output-collection too -- both found because a same-input 0% coverage was treated as suspicious, not accepted, fixed, and every detector's raw output now cached to disk so neither cost was re-paid twice. No quantum claim; no hypothesis-register entry (classical benchmark finding, same scope TASK-0306's own six-measure result sat in without one) | Implementer B | Done | High | 2026-09-09 | 2026-09-09 | Materially strengthens the certifying-benchmark case in the submission with a second, independent domain; PocketMiner's own 4 reshape failures and its single-chain export convention are a disclosed, unresolved gap for whoever revisits it next | `.ai/tasks/DONE/TASK-0360-detector-agreement-cascade.md` |
| TASK-0361 | **[[TASK-0359]] drew the right boundary; nobody has checked that it holds.** [[HYP-P28]] is real -- a score with zero within-structure position information (every residue replaced by its own protein's mean) reaches pooled AUC **0.6500** / **0.5975** on 54 brand-new proteins / **0.6549** union, p <= 0.0006 -- and 0359's write-up correctly argues this register's per-structure-then-average convention is **immune by construction** (a per-structure-constant score ties inside that structure's own ROC, contributing exactly 0.5). **That argument is sound; whether it actually applies to each number we publish is unverified**, and 0359 lists the audit as explicitly not resolved. Classifies every in-scope number as per-structure-averaged (immune) / pooled (needs a protein-identity floor) / unclear, citing `file:line` for each -- a verdict without a citation is not a finding, and an unestablished provenance is recorded as unclear rather than guessed. Reads the producing code, re-runs nothing; any pooled number gets 0359's own `protein_baseline_auc` computed on its cohort and reported against that floor the way [[TASK-0094]]'s proximity floor already is. **Priority order is load-bearing**: the submission's own numbers first, since that is what ships. **Validates the auditor before the audit** -- the procedure must separate two numbers of known aggregation ([[TASK-0310]]'s 0.5921 per-structure vs 0359's 0.6500 pooled) or it stops. Pre-registered expectation: the submission is clean, and the deliverable is then the right to state "every AUC here is per-structure, never pooled" as a **checked fact rather than an expectation** -- worth a line in a document arguing the field does not check things like this. **Done, 2026-09-09, Implementer A: prediction HOLDS.** Planned Validation passed (correctly separated TASK-0310's per-structure 0.5921 from TASK-0359's pooled 0.6500). Full `file:line`-cited table over every AUC-type number in `PHASE1_SUBMISSION_V3.md` (walk occupation, per-target verdicts, chiral/spectral/entanglement arms, centrality ablation, TASK-0350's coherent-decoherent delta + its TASK-0359 cohort extension, TASK-0358's finite-delay arm + extension, the LOPO combined-readout ceiling) -- **every one is per-structure-or-per-cluster-averaged; none pooled.** Several other quoted numbers (BH-FDR survivor counts, family-clearing counts, detector-agreement disjunctions, the apo/holo pass rate) are count/rate statistics the HYP-P28 pooling mechanism doesn't apply to -- noted as N/A, not silently skipped. Item 2 (HYP-P6/P7/P13/P28 arms) substantially covered as a byproduct; HYP-P28's own pooled `protein_baseline_auc` is confirmed **not currently quoted anywhere in the submission** (recorded as a drafting fact, not a defect). Item 3 (`RESULTS.md`) not started, budget spent on item 1 per this task's own priority order. Folded into HYP-P28 as a dated status update -- the audit that hypothesis's own filing named unresolved is now resolved | Implementer A | Done | High | 2026-09-09 | 2026-09-09 | The submission can now state "every AUC here is per-structure, never pooled" as a checked fact | `.ai/tasks/DONE/TASK-0361-pooled-vs-per-structure-aggregation-audit.md` |
| TASK-0362 | **The submission promises a measurement we already delivered, and reads as "quantum failed" when what we built is an instrument.** [[TASK-0345]] ran component (b) on 2026-09-07/08 -- 63 pairs / 59 distinct proteins, stripped-holo minus true apo **mean +0.199**, Wilcoxon p=2.6e-4, sign-flip permutation p<1e-4, bootstrap 95% CI **[+0.102, +0.296]**, same sign in both source cohorts -- yet V3 still says "we expect to report it before Phase 2 begins." Promote it to a Phase-1 result with its "so what" stated (leading methods report 89.8%/98.1% on ligand-removed holo, which is ~0.2 AUC easier than the apo case those numbers are read as solving) and **carry the n=63-not-100 caveat rather than dropping the >=100 criterion to look complete**. Reframe around four independent demonstrations that field headline numbers don't mean what readers assume ([[TASK-0306]] 83.9%->17.8%; [[TASK-0345]] +0.199; [[TASK-0359]]/[[HYP-P28]] pooled-AUC floor 0.65 with zero site information; [[TASK-0360]] detector cascade, pending) -- **a reordering, not a softening; any edit that weakens a null is out of scope by definition**. Names the protein-identity floor as a contribution while keeping its reach exact: it bites on pooled AUC across proteins, **not** on P@5 or per-structure-averaged metrics, which are immune by construction -- overstating it would be the precise error we have criticised elsewhere. **Hard-blocked on [[TASK-0360]]** (item 3 folds its result in either way, pre-registered two-sided). Body is at 5/6 pages so additions must be paid for; the (b) row in the Phase-2 table becomes redundant and roughly funds the promotion. No number changes -- framing only | Reviewer or Implementer | Done | High | 2026-09-09 | — | — | `.ai/tasks/DONE/TASK-0362-submission-improvements-instrument-spine.md` |
| TASK-0363 | Two follow-ons enabled by [[TASK-0361]]'s audit, plus one exposure the external branch surfaced the same evening. **(1)** The floor bullet argued per-structure metrics are "immune by construction"; 0361 classified every number in the document with `file:line` citations and found **no pooled AUC among our own claims**, so the text now reports the audit instead of the argument -- the difference between an argument and a check is the whole point of the section it sits in. **(2)** The external branch found its 399 "families" are **not one clustering** but labels inherited from five source datasets (CASBench `CAS####`, UniProt accessions, free-text names), CASBench grouping far more aggressively -- and **[[TASK-0336]]'s 276 derives from that same artifact**, so we had been quoting an inherited five-way convention as a clustering rule. Caveated in one clause at the fuller technical statement, keeping both halves: the absolute count depends on the convention, the McNemar comparison does not, because it is paired over the same families. Deliberately **not** done: citing their 0.771 pooled identity floor (their cohort, unverified by us -- merge-only); any family recount (a cohort decision for both branches); chasing CAS0002, which supplies 26 of the external pipeline's 69 wins and is the only large family whose wins are distal -- a real mechanism lead and **n=1 at the family level**. Build PASS, body 5/6, net +56 words | Reviewer | Done | High | 2026-09-10 | — | — | `.ai/tasks/DONE/TASK-0363-state-the-aggregation-convention-and-caveat-family-labels.md` |
| TASK-0364 | Read [8] (Zhang et al., *Adv Sci.* 13:e13641) in full via its open arXiv preprint (2506.22677; Wiley blocks automated fetch) -- DOI/metadata cross-checked first. **Our sentence was correct, not wrong: sharpened, not corrected.** [8] is VQE-based 3D backbone prediction for short (5-14-residue) fragments at *already-located*, orthosteric binding sites from PDBbind complexes, on the sponsor's own IBM-Cleveland Clinic 127-qubit processor -- load-bearing (the quantum step produces the structure), not a demo. It never addresses pocket detection, apo-state structures, or allosteric sites anywhere in the text. Sentence in `PHASE1_SUBMISSION_V4.md` (V3 already superseded, not edited) now states precisely what [8] does and does not cover, defensible against its method/evaluation sections rather than its abstract. Kept separate from the [7] prior-art comparison -- different quantum paradigm, different problem, not a competing method | Explorer or Reviewer | Done | High | 2026-09-10 | 2026-09-10 | Sentence sharpened, `SUBMISSION_VERSION_LEDGER.md` V3-to-V4 row added | `.ai/tasks/DONE/TASK-0364-read-the-sponsor-group-quantum-binding-site-paper.md` |
| TASK-0365 | **V4: put the AI where the rubric actually looks, and check the borrowed numbers at source.** "AI workflows" is item 6 of the *Submission Guidelines* (not the Challenge Statement, whose own section 6 is Data and Resources) and asks how the **method** integrates AI -- scored 5% in the Phase-1 table. The document's existing AI paragraph is about the *authoring* workflow, so an external claim that "there is no AI anywhere in the document" was **factually wrong** (corrected in-meeting), but Section 6's architecture genuinely had no AI stage. Added: the two learned models, leave-one-family-out over 597 proteins / 380 families. **Reading `origin/allosteric`'s own files rather than the summary found three discrepancies, all understating their work**: (1) the AI result is not purely negative -- Model 1 (topology->MIN_HOP) is a partial positive, near/distal AUC **0.793** and **0.924 vs 0.901** running at its predicted distance, while only Model 2 is negative (0.625 vs 0.630, 13 vs 15 families); the quoted "0.107 vs 0.137" is labelled **"earlier tests"** in their own README; (2) the ML cohort is 597/380, not the 630/399 used elsewhere; (3) the funnel ablation's second half was never quoted -- unconstrained, closeness falls **48 proteins to 6** while its **AUC rises** 0.576->0.626, because the base rate drops 0.33->0.070, so AUC *conceals* the collapse. Also trimmed Section 7's "not as a credential" defence -- it argued with an imagined objector. V3 preserved; V4 is a new file. Build PASS, body 5/6 unchanged | Reviewer | Done | High | 2026-09-10 | — | — | `.ai/tasks/DONE/TASK-0365-v4-the-ai-stage-and-the-funnel-ablation.md` |
| TASK-0366 | **Full inventory of `origin/allosteric`'s own results, every positive verified against its raw artifact -- and it found a large, currently-unreconciled contradiction between the two branches' own headline coherence claims, not just a compression accident this time.** Headline: on their 630-protein/399-family cohort, coherent CTQW (`e^{-iHt}`) beats its exact classical twin, the heat kernel `e^{-Lt}` (same graph/seed/time-grid, probabilities not amplitudes) at **p=2.3e-12** -- independently recomputed here from all 8 raw `sc_*.json` shards (630/630 scored, mean 0.5999 vs 0.5124, matches their README exactly). **This directly contradicts our own HYP-P7/[[TASK-0350]]** (classical diffusion 0.6012 beats both CTQW arms on our 108-structure cohort) -- and their own `seeded_classical/README.md` flags the contradiction unprompted ("must be reconciled before submission"), which has not happened; zero mentions in `PHASE1_SUBMISSION_V4.md`. Three smaller absent positives found and verified: BCR_ABL1/asciminib AUC 0.900 P@5 0.80 hit (caveated: best-of-6, 1 of 3 targets attempted, KRAS blocked); site-level pocket ranking best top-3 86%/best mean rank; a 440-config residue classifier AUC 0.654/35 families (caveated: no null-correction, the same selection-bias risk their own register flags elsewhere for the 884-config sweep). Also found and flagged: `proximity_floor/README.md` still states a distal win (0.617 vs 0.227, p=5.8e-09) **the branch's own later documents already retracted** ("I read an AUC backwards") -- the stale writeup was never fixed; recorded so nobody re-cites it. Planned Validation (re-deriving [[TASK-0365]]'s own three numbers) passed exactly before the sweep began. All 11 required directories covered, none skipped. Read-only, no submission edit -- Team Lead's call what travels into V4/V5 **CORRECTED 2026-09-11 (Reviewer): finding #1 is not a coherence result and not a contradiction.** `seeded_classical.py:76` shows the CTQW arm is never computed there -- it is read from precomputed ranks for the fixed cell `gauss|sym|neg_dE` (line 25), while the heat-kernel arm is computed fresh on plain `L`, so **two variables differ, operator and score**, not "amplitudes vs probabilities". And `neg_dE` is **phase-free by the source branch's own measurement** (`ALL_RESULTS_SUMMARY.md:99-104`: reproduces at rho=1.0000 from the diagonals of H and H², "no phase, no interference, no long-range coupling", same formula on the classical operator gives the same numbers) -- so the comparison cannot be evidence about coherence whatever its p-value. `p=2.3e-12` stands as a real **operator/score-choice** result. **[[HYP-P7]] unchanged, no reconciliation owed, the "highest priority" flag withdrawn.** Process lesson: the sweep *did* recompute this from the raw shards and matched to 4 decimals -- **reproduction verifies arithmetic, only reading the producing code verifies what was measured**; second time this register has been caught by that distinction after [[TASK-0348]]. | Explorer or Reviewer | Done | High | 2026-09-10 | 2026-09-10 | The heat-kernel-twin contradiction is the one item that needs a decision before the next submission version, not just an inventory entry -- whoever owns HYP-P7 next should read finding #1 first | `.ai/tasks/DONE/TASK-0366-sweep-the-allosteric-branch-for-under-reported-positives.md` |
| TASK-0367 | **The page-count check cannot see the violation it exists to catch, and our build sentinel ships inside the PDF.** Verified from the shipped PDF's page-7 text layer: Section 7's member table and the repo line are **body content on page 7**, while the tool reports "body = pages 1-6" because it counts the page on which the appendix *begins* as the first appendix page -- so body spill onto that page is invisible. **Every `body 6/6 PASS` this session, once the body reached page 7, measured the wrong thing**, and Guidelines section 5 says an over-length submission may be assessed on the first 6 pages only, which would cut Team Capability (10% criterion). Second defect: `SUBMISSION_BUILD_APPENDIX_START_7f3a9c` appears in the extracted text -- invisible when rendered, survives copy-paste and automated screening. Fix both, plus a regression test each (a fixture whose body spills onto the appendix page must FAIL; no output may contain `SUBMISSION_BUILD`). **Blocks 0369/0370/0371** -- all end in "rebuild and check", and trimming text against a measurement we know is wrong is worse than not trimming. **Done.** `analyze()` now detects a page shared between body/appendix (real content before the heading's own occurrence, not just "any text before the split") and counts it in both totals; the injected marker is retired for the LaTeX route entirely, replaced by locating the real heading text at >=11.0pt (empirically measured: body 10.5pt, real heading 11.96pt). Planned Validation re-run against the real current source (V4.md): **7/6 FAIL**, was 6/6 PASS; zero `SUBMISSION_BUILD` occurrences in the rebuilt PDF or .tex. 49 tests pass. Found in passing, not fixed: `DEFAULT_MD` still points at V2 (stale); and an unrelated machine-level `git`-via-x86_64-python breakage (`xcrun`/Command Line Tools architecture mismatch) blocking `test_claim.py`'s own scratch-repo suite -- flagged for the user, needs a system-level fix outside this session's authority | Toolsmith | Done | Blocking | 2026-09-11 | — | — | `.ai/tasks/DONE/TASK-0367-page-count-defect-and-sentinel-leak.md` |
| TASK-0368 | **An external reviewer claims our shipped five residues per target point at the wrong pockets, and it needs Berke, not an agent.** Six specific claims to confirm or refute, each with the shipped list: BCR-ABL1's 402/311/310/301/338 read as the **ATP site** under 1OPL's Abl-1b numbering (1a+19) rather than the myristoyl pocket; `1OPL` carries **myristic acid *and* an ATP-site inhibitor (P16)**, where we mention only the myristate; `8QYR` may be the **mavacamten-complexed, bovine** structure while our apo input is `8QYP`; the cardiac five may be **one site**, not five; c-Myc's 943/246/925/226/243 **cannot be UniProt numbers** (943 > c-Myc's 439, 226/243/246 > Max's 160) and have no chain column, and no seed is stated for a protein with no active site; KRAS's five are switch I plus the NKCD rim with **no switch-II residues**, where the G12C pocket is. **If the reviewer is right this is our own thesis demonstrated on our own output** -- one honest sentence beats a quiet correction. Shipped residues must not change; the question is what they are. **Blocks the hit-list parts of 0369/0370** | Berke Turkaydin (team member) | TODO | High | 2026-09-11 | — | — | `.ai/tasks/TODO/TASK-0368-berke-consultation-on-hit-list-biology.md` |
| TASK-0369 | Concept Proposal corrections from the external adversarial review. **The decision that is not ours: the repository is private** (404 unauthenticated, not among the 11 public repos on that profile), and Section 7, the Team Profile, the problem-statement file and both READMEs all rest on it -- **every verifiability claim is currently unsupported** until it is published or every such sentence is rewritten. Factual: name the Table-1 cardiac pair `5TBY->6C1H` and the substitution reason (**with `4OBE` wild-type and `1OPL` doubly liganded, every mandated pair has a verifiable defect -- the strongest evidence for section 1's thesis, currently omitted**); "Appendix B" does not exist; coupled search is "CLOSED" in [[TASK-0213]] and "survives screening" in section 2; 169 is `4OBE`'s N and we ship `4LDJ` at 170; 0.4960 is *at* chance; "we followed it in each case" is wrong since we rejected `8S8C`. Framing, where **the reviewer's fix strengthens the thesis**: `NO_SIGNAL_IN_APO` could not have come out otherwise (needs score 0.80-0.89 to flip -- bootstrap paired and relabel with a detection limit); the "well-powered null" is powered for a ~77% non-distal cohort while the hypothesis is about distal pairs; "no arm clears more than 5 of 276" states no criterion (~14 expected uncorrected); two Phase-2 success criteria are already met by our own baseline column; the per-structure-not-pooled sentence cannot cover the protein-level 0.793. **Blocked on 0367 for anything changing length** | Reviewer | TODO | High | 2026-09-11 | — | — | `.ai/tasks/TODO/TASK-0369-submission-text-and-framing-corrections.md` |
| TASK-0370 | **Fixed at the generator, per this task's own Constraint, then hand-applied to the four shipped `report.txt` files without touching any shipped number.** `report.py`'s confidence statement now says "unverified" in all cases (was "high" whenever operators agreed); `verdict_template` gained an optional target/structure header and a CI-overlap reconciliation on the DECISION-SUPPORT block so a "meaningful"/"genuinely helps" DAUC line is qualified whenever the same file's own CI-overlap diagnosis says otherwise -- 41/41 tests pass. `artefacts/README.md`: `8QYR` relabelled apo->holo, `8QYP` is the true apo (independently confirmed three ways, incl. `PHASE1_SUBMISSION_V4.md` §1's own deviation table); matrix-entry formula and diagonal-heatmap warning added (item 23); seed residues shipped per target, re-derived from this repo's own `build_labels`/`functional_indices` call path -- **MYC_MAX's seed is bit-identical to its own top-5 hit list, disclosed as circular, not corroboration**; BCR's failed site-level result and cardiac's one-cluster-not-five both disclosed (items 20/21); CSV got a `chain` column (item 22); `V_B/V_T/V_R/V_C/V_M` defined from `potentials.py`'s own docstrings, since **the Concept Proposal names none of them** -- checked against both V1 and the actual current V4; package-size line corrected 6.3->8.8 MB. **Self-caught mid-task**: draft text assumed `PHASE1_SUBMISSION_V1.md` was still current; it is not (V4 is, via other threads' `9f1d521`/`48033c8`) -- every CP cross-reference was re-verified against V4's real text, and V4 has no numbered "Finding N" statements at all, so four "Finding 3" citations across `report.py`/`test_report.py`/`MYC_MAX/report.txt`/the README were corrected to cite §2 by content instead. Planned Validation passed: no shipped file asserts a result the CP disclaims. No shipped number or residue changed | Implementer | Done | High | 2026-09-11 | 2026-09-12 | — | `.ai/tasks/DONE/TASK-0370-deliverable-artefact-corrections.md` |
| TASK-0371 | **Work we did that the proposal never mentions, plus one real strategic risk.** Three clauses: we transpiled against `FakeSherbrooke` (538-1486 two-qubit gates, fidelity ~0.015) but report only analytic counts, where Challenge Statement 4.1 says participants **must build a quantum circuit**; we tested noise resilience and **falsified** the phase-free robustness hypothesis, which is absent (a falsified answer to a secondary objective still beats silence); and the main-branch 3Dmol app answers 4.2's 3D-visualisation ask **if the repo is public**. The strategic item: **Phase 2 contains no quantum execution** -- defensible, since we measured the quantum stage adding nothing, but it exposes Technical (25%) and Feasibility (20%). Option: a small pre-registered Braket/Classiq arm on the challenge's **own** ref [11] log encoding (~10 qubits + 1 ancilla; 10^5-10^6 gates), framed as a **noise measurement and instrument test subject, never an advantage claim** -- it will not find signal and we must say so in advance. Team Lead's call; record it either way, including a decision not to. Every number here is the reviewer's and **unverified by us** -- check before quoting | Reviewer | Done | Medium-High | 2026-09-11 | — | — | `.ai/tasks/DONE/TASK-0371-rubric-coverage-gaps.md` |
| TASK-0372 | **E0 — the falsifiable gate on the aromatic-enrichment idea, with the confound the external reviewer missed.** They report F/Y/W/H at allosteric sites **15.7% vs ~10.2% background, ~1.5x** across ASBench's 1,413 annotated allosteric residues, and name the **burial** confound themselves, pre-registering this gate before anything else. Background checks out (F+Y+W+H ~10.8% across proteins), so the arithmetic is real. **The confound they did not name is ours**: ASBench annotates allosteric sites as **ligand-contact** residues, our own section 1 reports **40 of 40 sampled ASBench structures carry a bound ligand at the scored site** ([[TASK-0329]]/[[TASK-0345]]), and aromatics dominate ligand binding generally -- so 1.5x may be measuring "aromatics touch ligands", which is neither allostery nor electronics. Their paired active-site control is the right instinct but **matched on burial alone it passes while testing the wrong thing**; match on **burial AND ligand contact**, paired within-protein, cluster-robust. Pre-registered two-sided: most likely it vanishes, which is a clean negative; the interesting outcome is allosteric sites being more aromatic than **equally-buried, equally-ligand-contacting active sites in the same protein**. **DO NOT RUN BEFORE 2026-09-15** -- three days, body over limit, and a rushed E0 missing the stratum would be the proximity confound in a new costume | Implementer | TODO | High (Phase 2) | 2026-09-12 | — | — | `.ai/tasks/TODO/TASK-0372-aromatic-enrichment-gate-with-ligand-contact-control.md` |
| TASK-0373 | **Two sentences that turn our central null from a dead end into a diagnosis.** *"The contact graph discards the degrees of freedom where quantum effects would live, so a quantum walk on it cannot find them -- by construction."* An 8A Ca graph cannot represent a pi system; to it Phe and Ala are both "a residue". So [[TASK-0350]]'s +0.0023/p=0.92 and [[TASK-0358]]'s 0/11 flat scan are **what you would predict if the input had no quantum content to begin with** -- every number stands unchanged, and this explains them rather than hedging them. Also gives Phase 2 the target [[TASK-0371]] records as missing. **Must be stated as a hypothesis about why our null is a null, never as a result: we have not measured electronic coupling**, and it must not borrow [[TASK-0372]]'s ungated enrichment. **Blocked on [[TASK-0369]]** -- body is 7/6 and FAILING now that [[TASK-0367]] reports it correctly, so two sentences in means two or more out | Reviewer | TODO | High value per word | 2026-09-12 | — | — | `.ai/tasks/TODO/TASK-0373-reframe-the-null-as-a-diagnosis.md` |
| TASK-0374 | **Phase-2 programme on the electronic-structure route, and the best answer yet to [[TASK-0371]]'s "Phase 2 contains zero quantum execution".** Cohort already in our repo: **42 of ASBench's 118 entries (36%)** carry a cofactor or effector where electronic structure is plausibly load-bearing -- 9 metal/redox, 9 PLP/ThDP, 10 NAD(P), **13 aromatic effectors**, 1 heme peroxidase (**tier assignment is the reviewer's and unverified by us**). Four arms: **E1** a second graph on aromatic ring centroids with real stacking geometry, on which our CTQW machinery runs unchanged but the input finally encodes what the Ca graph discarded; **E2** DAHP synthase `1KFL`/`1OF6` as a built-in natural experiment -- effectors differing by one hydroxyl, isoforms effector-specific, sharpest item in the list and **n=2**; **E3** fragment electronic coupling `dH_AB` between allosteric- and active-site fragments, apo vs holo, on P450 3A4 or Cu nitrite reductase -- **a quantity no force field can produce**, which is a far better hardware arm than re-running something we showed adds nothing, and a **capability** claim not an advantage claim; **E4** PCET hydrogen-bond chains with ribonucleotide reductase (`2R1R`/`3R1R`) as the positive control that says whether the detector works at all. **[[TASK-0372]] hard-gates E1/E2/E4; E3 survives its failure** | Architect/Planner | TODO | Phase-2 planning | 2026-09-12 | — | — | `.ai/tasks/TODO/TASK-0374-phase-2-electronic-structure-programme.md` |

## Current Rules

- Additive bootstrap only. Do not rewrite `.claude` broadly in this phase.
- Stable policy belongs in `.github/`.
- Live execution belongs in `.ai/tasks/`.
- Named capabilities belong in `.ai/reference/CAPABILITIES.md`.
- Durable scaffold learnings belong in `.ai/memory/shared/*` after review.
- Whoever creates or moves a `TASK-XXXX` file must update this registry in
  the same edit — it must not silently drift out of sync (see Task Ledger
  Boundary above for the `.claude/TASKS.md` split).
- **Claim before you start (TASK-0017, hardened by TASK-0024).** Before
  starting or resuming work on a registry row — or editing this registry
  itself, the single most contended file in the scaffold — run
  `python3 .ai/tools/claim.py claim <TASK-ID> "<your label>"` first. This is
  an atomic, whitelisted local operation (an `O_EXCL`-created lock file
  under `.ai/tasks/.locks/`), not a hand-edit of this table's `Claimed By` /
  `Claimed At` cells — **do not hand-edit those two cells anymore**; run
  `python3 .ai/tools/claim.py sync` to regenerate them from the lock files
  (everything else in a row — Description/Assigned To/Status/Priority/Last
  Active/Path — is still hand-maintained exactly as before). If the row is
  already claimed, `claim` fails loudly and prints the current holder:
  either pick a different unclaimed task, or override with
  `--force --reason TEXT` if you judge the claim stale (see below) — the
  reason is recorded in the lock file for audit. Free-text claim labels are
  still fine (e.g. "Implementer (this thread)").
- **Staleness override (TASK-0017).** A claim with no corresponding file
  activity (check the task file's own mtime / `git log -- <path>`) for
  longer than one working session is stale and may be taken over via
  `claim.py claim <TASK-ID> "<label>" --force --reason "..."`. No numeric
  TTL is enforced — a human or agent reading the table makes the call, same
  as everything else in this scaffold.
- **This table's claim columns are no longer edited by hand (TASK-0024).**
  `.ai/tools/claim.py sync` is the only writer of `Claimed By` /
  `Claimed At` — it reads lock files and rewrites only those two cells per
  row, serialized through a short-lived `.ai/COMMON.md.synclock` file, so
  concurrent `sync` runs can't clobber each other's claim data. Two such
  whole-table collisions happened in one session before this landed.
  Adding, removing, or reordering *rows* (new tasks, moved tasks) is still a
  manual edit with the original whole-file-write risk — `ls .ai/tasks/TODO
  IN_PROGRESS DONE` immediately before such an edit and reconcile against
  that, not against a prose read that may already be stale.
  **Do not put result summaries or handoff notes in `Claimed By`/`Claimed
  At` (TASK-0355).** 21 rows did this — a real `sync` run silently deletes
  whatever's sitting in those two cells the moment a row has no live lock.
  Put a summary in `Description` instead (append it, don't overwrite); it's
  free text and `sync` never touches it. There is no working free-form
  ninth column — `Path` is always a literal file path.
- **Moving a task file between folders is no longer a manual `mv` + two
  hand-edits (TASK-0027).** Run `python3 .ai/tools/claim.py move <TASK-ID>
  <TODO|IN_PROGRESS|DONE> --as "<your label>"` — it relocates the file,
  rewrites its own `- Status:` line, and updates this registry's `Status`/
  `Path` cells for that row, together. Refuses on a claim mismatch the same
  way `claim` does (`--force --reason TEXT` to override); moving to `DONE`
  auto-releases the claim by default (`--keep-claim` to opt out). `--as` is
  always required.
- **Claim `GIT-COMMIT` before staging anything you intend to commit**
  (TASK-0028, workflow completed by TASK-0029). **As of TASK-0042
  (2026-08-03), `git commit`/`git push` Bash calls are hook-enforced, not
  merely advisory**: a `PreToolUse` hook (`.ai/tools/git_commit_guard_hook.py`,
  registered in `.claude/settings.json`) denies the call outright unless
  `GIT-COMMIT` is currently claimed **by this exact session** — locks now
  record `session_id` (from `CLAUDE_CODE_SESSION_ID`) alongside the
  free-text `claimant` label specifically so the hook can check identity,
  not just existence; a stale/foreign claim, a legacy lock with no
  recorded session_id, or no claim at all all deny, with a distinguishing
  reason each. See `.ai/memory/questions/toolsmith/answered/Q-0001-*.md`
  for the real incident (a non-honoring thread's `git add` landing in
  another thread's staged index) that motivated hardening this in the
  first place. **What the hook does *not* cover**: it verifies the *claim*,
  not your *file list* — `commit-guard --expect <paths>` below is still a
  manual step and still the real defense against staging the wrong files;
  a held claim only proves you're allowed to commit *something* right now,
  not that what's staged is what you intended. Full sequence:
  1. `python3 .ai/tools/claim.py claim GIT-COMMIT "<your label>"` before
     the *first* `git add` of a commit-bound change, not right before
     `git commit`. If another thread holds it, `claim` refuses and names
     the holder; do not proceed, and do not pass
     `--force --reason --hitl-override` yourself — that combination
     requires an explicit human instruction in the current conversation,
     not an agent's own judgment call, unlike the advisory task-row
     override above.
  2. **Only if nothing has been staged yet this session** (no prior
     `move`/`resolve`/`stage`): `python3 .ai/tools/claim.py commit-guard
     --expect-empty` — fail fast if the index isn't actually clean, before
     you touch it. **Skip this step if you already ran `move`/`resolve`**
     (the ordinary "close a task, then commit it" order) — `git mv` stages
     its own rename regardless of GIT-COMMIT, which was never required for
     `move`, so `--expect-empty` *correctly* fails there; it is not a false
     alarm to route around by chaining past it, it is simply the wrong
     check for that moment (TASK-0356). Step 3's own self-verification
     gives the identical contamination check one step later either way.
  3. `python3 .ai/tools/claim.py stage --expect <path> [<path> ...]` for
     any `.ai/`/`.claude/` files — include any rename pair from an earlier
     `move`/`resolve` in the same `--expect` list. Stages exactly those
     paths and self-verifies. Files outside `.ai/`/`.claude/` still need a
     plain `git add`, which correctly prompts (deliberately not
     whitelisted — see TASK-0029's Intent Contract for why an unscoped
     stage would have been a whitelist-bypass in disguise).
  4. **`python3 .ai/tools/claim.py commit-guard --expect <path> [<path>
     ...] --commit --message-file <path to the full commit message>`
     — the check and the actual `git commit` in one atomic call, no
     separate shell step between them (TASK-0356).** Write the message
     with the Write tool first, never pass it inline. Real incident this
     replaces: the former two-step `commit-guard --expect ... | tail -1 &&
     git commit ...` idiom silently committed anyway on a *failing* guard
     twice, because `pipefail` is off by default in this shell and `&&`
     after a pipe tests `tail`'s exit status, not the guard's — this
     command has no pipe/chain boundary for that to happen to. Re-verifies
     GIT-COMMIT session identity itself before committing, since its own
     `git commit` subprocess is invisible to the PreToolUse hook below
     (which only ever inspects the one literal Bash command it was
     matched against, never a child process that command goes on to
     spawn). The old two-step form (`commit-guard --expect ...` with no
     `--commit`, followed by a separate `git commit` Bash call — still
     hook-checked normally) still works for anything that wants the check
     without the action.
  5. `python3 .ai/tools/claim.py release GIT-COMMIT`.
- **`scq-enter` as soon as you know your file list, not when you're ready
  to commit (reviewer feedback, 2026-09-06).** With five-plus concurrent
  threads on one `GIT-COMMIT` lock, SCQ only reduces contention if other
  threads can see a commit coming *before* it's staged, not just when it's
  refused. `python3 .ai/tools/claim.py scq-enter --as "<label>" --files
  <path...> --message "<one-line>"` the moment you know what you're going
  to touch — while still writing/reviewing, well before the actual
  `claim GIT-COMMIT` in step 1 above. Re-entering updates your own ticket
  in place; it's removed automatically once you claim `GIT-COMMIT`.
  Visibility only (nothing enforces order) — but a thread that never
  enters until it's blocked has given every other thread zero warning.
- **Task-number collisions keep recurring — use `reserve-next`, and check
  before filing by hand.** At least two so far this project (TASK-0045 →
  built `reserve-next` for exactly this; TASK-0313/0315, documented in
  TASK-0315's own filing). `python3 .ai/tools/claim.py reserve-next --as
  "<label>"` atomically allocates the next id — use it instead of eyeballing
  the highest number in `.ai/tasks/` and incrementing by hand, especially
  when filing several related tasks in one batch (a likely occasion for two
  threads to file at the same number independently). If a collision is
  found anyway, the existing precedent is first-claim-wins; the second
  filer renumbers and documents the renumber in its own file, not a silent
  overwrite.
- **Seam gate (TASK-0050).** A task may not move to `DONE` if it opens a
  seam (its output is consumed by another unit, or it splits a
  responsibility a previous unit held whole) without a corresponding
  `.ai/seams/SEAM-XXXX` record with a named owner and seam-test (may be
  `xfail` while `OPEN`). The repo is not green if any `OPEN` seam has no
  seam-test at all; `WAIVED` requires a reason and expiry. See
  `.ai/seams/README.md`.
- **Invariance gate (TASK-0051).** Before a quantity is reported, its
  GAUGE/KNOB/SIGNAL transformation table must exist in `.ai/invariants/`
  — no table, not reportable. "Invariant on our test set" is a trigger to
  widen the transformation group, not a green light — see
  `.ai/invariants/README.md` and [[pitfalls#P-0001]].
- **Merge conflict protocol (TASK-0202).** `git fetch` + `git branch -vv`
  before assuming you're in sync — a real divergence sat unnoticed for
  two days before being caught. Before rebasing, preview non-destructively
  with `git merge-tree --write-tree <local> <remote>` (exit 0 = clean, no
  guessing). Always rebase onto the remote, never a merge commit — keeps
  this repo's one-task-one-commit history intact. On a genuine conflict in
  `COMMON.md`'s registry or `RESULTS.md`'s findings/open-questions table
  specifically (both append-only ledgers): **keep both sides, never
  `--ours`/`--theirs`** — verify by row/section count increasing, not by
  "no conflict markers remain" (a clean-looking merge silently dropped
  `RESULTS.md` rows 47-53 once already, recovered in `61b8096`). This
  ledger-file rule does not generalize to ordinary source-code conflicts.
  `GIT-COMMIT` claim covers the whole rebase-through-push window, not just
  one commit. Full protocol: `.ai/reference/MERGE_CONFLICT_PROTOCOL.md`.
- **`RESULTS.md` claim + citation convention (TASK-0195).** Two confirmed
  silent losses in `RESULTS.md` (open-questions rows 47–53, recovered
  `61b8096`; a [[TASK-0167.003]] section lost the same way, never recovered,
  see [[TASK-0191]]) both traced back to **concurrent uncommitted edits in a
  shared working tree**, not a real git-history divergence — every commit
  involved was correctly based on its true immediate parent throughout,
  so nothing about git ancestry ever looked wrong. Two changes follow:
  1. **Claim `RESULTS.md` (and `COMMON.md`, same reasoning) before editing**,
     the same discipline `GIT-COMMIT` already gets: `python3
     .ai/tools/claim.py claim __WORK_IN_PROGRESS__/RESULTS.md "<your label>"`
     — [[TASK-0024.001]]'s general resource-id support (any claim argument
     that is not a clean task-id shape — `24`/`TASK-0024`/`26.1` — e.g. a
     bare filename or a `path/with/slashes.md`, even one with a digit like
     `PHASE1_SUBMISSION_V3.md`, is namespaced as a `RESOURCE-*` lock
     instead of erroring or being misread as a task id). **This especially
     covers any working-tree-level operation a normal row edit wouldn't —
     `git checkout -- .ai/COMMON.md`, a multi-step edit spanning several
     tool calls, a manual restore, a `sync`-recovery reset — claim
     `COMMON.md` as a resource first so a concurrent thread sees "hands
     off" (three independent incidents on this file so far).**
     Then, immediately after claiming and before making any edit, run
     `python3 .ai/tools/claim.py check-staleness <same path>` to confirm
     nothing changed since your claim's content snapshot — the same
     "verify before you touch it" position `commit-guard --expect-empty`
     already occupies for `GIT-COMMIT`. Content-hash-based, not
     git-ancestry-based, specifically **because** ancestry was proven
     (by replaying the actual incident) not to have caught the real
     failure — see that task's Done section for the full replay. Release
     when done, same as any other claim.
  2. **Cite `[[TASK-XXXX]]`, never a bare "row N."** Row numbers renumber on
     every collision (exactly what caused loss #1) and silently invalidate
     every bare pointer to them with no way to detect it. Where a row number
     is still useful as a locator, pair it with the task ID
     (`[[TASK-0177]] (row 55)` — the pattern already used almost everywhere
     in the document) so a renumber degrades to stale-but-traceable rather
     than silently wrong. `python3 .ai/tools/check_references.py` checks
     both this pairing and that every `[[TASK-XXXX]]` citation in
     `RESULTS.md` resolves to a real task file on disk — run it after any
     `RESULTS.md` edit that adds or touches a citation.
- **`COMPETENCE_MAP.md` currency rule (TASK-0205).** Any task whose result
  changes a per-target verdict, floor, actual, or headline number updates
  `COMPETENCE_MAP.md` in the same commit — mirrors `CLAUDE.md` convention
  #7's shape for `ARCHITECTURE.md`. Stated after that document went stale
  three times in three weeks (TASK-0192, TASK-0193, TASK-0205 itself) —
  the document carrying the submission's central per-target claim has
  systematically been the last thing updated. **Judgment recorded, not
  just the rule**: convention #7 was itself missed five times on
  `ARCHITECTURE.md` before TASK-0194 caught up, which argues convention
  alone is not enough — a [[TASK-0042]]-style hook or a test asserting
  every `RESULTS.md` finding citing a per-target verdict has a
  corresponding `COMPETENCE_MAP.md` mention is the likely next step if
  this recurs a fourth time. Recommended, not built, by TASK-0205 — see
  that task's own Done section.

- **A p-value quoted outside its own task must carry provenance (TASK-0314).**
  From [[TASK-0261]] onward, the cluster-permutation/Wilcoxon/Mann-Whitney
  battery is this register's standard instrument and has been run **59
  times** across TASK-0259–TASK-0311 alone (call-site count, from the
  scripts, not an estimate — `wilcoxon(`/`mannwhitneyu(`/
  `cluster_sign_flip_test(`/`cluster_permutation_two_group(`/
  `cluster_permutation_correlation(`, excluding definitions and imports).
  Bonferroni is applied *within* individual tasks ([[TASK-0284]],
  [[TASK-0287]], [[TASK-0288]]) but nothing spans tasks, and the register's
  three strongest surviving claims sat at p=0.019–0.042 — exactly the band
  a large uncorrected family produces by chance. **Whenever a p-value from
  one task is cited in another task, in `RESULTS.md`, or in a submission
  document, state (a) whether it was pre-registered as that task's own
  single declared hypothesis or found exploratorily among several tried,
  and (b) which family of tests it belongs to for correction purposes.**
  An exploratory p-value may still be reported, but never presented
  unlabelled beside a pre-registered one, and never summed into a
  "several independent p<0.05 results converge" claim without the family
  size that produced them.

- **Long-running scripts must print visible, flushed progress — this has
  now cost real time more than once (TASK-0348, 2026-09-08).** A script run
  via `nohup ... > log.txt 2>&1 &` sits fully stdout-buffered by default
  when Python isn't attached to a TTY — `print()` calls accumulate in an
  internal buffer that may not flush until exit, so a genuinely-healthy,
  hours-long run and a silently-hung one are **indistinguishable** from the
  log alone (`tail`/`cat` both show nothing either way). [[TASK-0348]]'s own
  centrality-ablation run sat for 2+ hours with zero bytes of output before
  being killed and rerun instrumented — not because the computation was
  wrong, but because there was no way to tell working-slowly from stuck
  without `sudo`-gated tools this environment doesn't have. Required for
  any script expected to run more than ~1 minute unattended, before it is
  ever backgrounded:
  1. **Unbuffered/line-buffered stdout, always** — `python3 -u script.py`
     *and* `sys.stdout.reconfigure(line_buffering=True)` at the top of the
     script itself (the second is the one that actually matters if a
     caller forgets `-u`; do both, don't rely on the invocation alone).
  2. **One progress line per real unit of work** (per structure/protein/
     fold/batch — not per algorithm step inside one), printed the moment
     that unit finishes, carrying at minimum: an index (`i/N`), elapsed
     time, and an ETA extrapolated from the mean per-unit time so far.
     Silence for more than a few seconds between lines on a script that
     claims to be almost done is itself informative — a `[1/108] ... eta=
     114s` line at the start is what turns "no idea" into "here is the
     actual expected wall-clock."
  3. **Checkpoint incrementally to disk** (append one result per completed
     unit to a JSONL file, `flush()`ed after each write — not one
     dict-in-memory written only at the very end). A kill/crash then loses
     at most the one unit in flight, and a rerun resumes from the
     checkpoint instead of repaying the entire cost. [[TASK-0348]]'s own
     rerun does this: `checkpoint.jsonl`, one line per structure, loaded on
     startup to skip already-done work.
  4. **Never pay the same expensive derivation twice per unit** if it can
     be paid once and reused — a real, avoidable inefficiency in
     [[TASK-0348]]'s first version (calling a 19-feature computation twice
     per structure, once to validate and once to score, when the validated
     result could simply be reused) roughly doubled that run's own cost on
     top of the visibility problem. Cheap to catch by reading your own loop
     before backgrounding it: count how many times the expensive call
     appears per unit of work.
  A script an agent cannot see progress on is a script its own author
  cannot tell "slow" from "stuck" on either — this is not just about what
  a human watching the terminal needs; the agent that launched it hits the
  exact same blind spot, which is what actually happened here.

- **Before you assume, check the register — the answer is very often already
  there (2026-09-08, at the repo owner's direct instruction).** When you are
  about to state something you have not verified this session — a structure's
  properties, whether a comparison has been run, what an external result implies
  for us, why a past choice was made — **stop and search first.** Not "consult
  the register if convenient": search before the sentence is written, because an
  assumption reads exactly like a finding once it is in a file.

  **This is evidenced, not exhortation.** Four times in a single week a thread
  reached for a conclusion the register already held, and every time the recorded
  answer was both *different from* and *more favourable than* the assumption:

  | assumed | already recorded |
  |---|---|
  | "we should just run the organisers' suggested `8S8C` for KRAS" | `8S8C` was RCSB-verified live and is **holo** (MK-1084-bound); `4LDJ` was found and used instead ([[TASK-0270]], `config/targets.yaml`) |
  | "our KRAS residue numbers look like an index-array bug" | `4LDJ` genuinely numbers its chain 0–169, and residue 12 is `CYS` — canonical, and the mutation proves it |
  | "our measurements are consistent with the JACS ρ≈0.95 centrality result" | not measured at the time; when run, median ρ = **0.41** and CTQW beats the seed-blind baselines ([[TASK-0348]]) |
  | "the chiral walk is the only arm that tested interference" | the converged limit was **proven phase-free** ([[TASK-0130]]) and *three* observables were built to reach past it ([[TASK-0140]], [[TASK-0146]], [[TASK-0157]]) |

  Note the direction. The register's habitual failure mode is over-claiming, so
  the reflex is to guard against optimism — but three of the four above were
  assumptions that *under-sold* our own work, and one of them was written into a
  submission draft. **An unverified concession is as much a defect as an
  unverified claim**, and it is harder to catch because it sounds like rigour.

  **How to actually do it**, cheaply: `grep -rn "<the thing>" .ai/tasks/ .claude/hypotheses/ __WORK_IN_PROGRESS__/config/ __WORK_IN_PROGRESS__/documentation/`
  costs seconds. `config/targets.yaml` and `documentation/2026-08-26-organiser-clarifications.md`
  in particular carry decisions whose *reasons* are recorded inline and are
  invisible to a task-file-only search. If the search returns nothing, say
  "not found in the register" rather than filling the gap with inference —
  that is a useful signal to the next reader, and it is the honest state.

  This is [[TASK-0321]]'s write-mostly finding restated as a working habit: the
  problem was never that the register lacks answers. It is that the default is
  not to look.

## Open Questions

- When should root-level expert briefs be introduced instead of staying reference-first?
- Which existing `agents-tools` providers should be wrapped first behind generic capability contracts?
- What should be the first post-bootstrap pilot after commit packaging?
